# Explorer × September MVP-C0 convergence

**Status:** bounded design/reconciliation evidence; no browser, participant, production, conformance or freeze result
**Date:** 2026-09-04
**Role/session:** data-explorer-pm · Codex · explorer-convergence-20260904 · task `01a02a24-0348-7c50-81fd-2a4ac43c62af`
**Authority:** James's overnight “Engage” delegation via EFS v2 PM permits design and narrow disposable experiments, not shipped UI, public deployment, durable user data or a V2-C1 ruling.

## Checkpoint

Keep the September five-seam contraction. Reuse August's **preservation laws**,
not its result bytes, Type/query identity or completed-test label. File Browser
is September's first thin guest/write journey and must work with Explorer
absent. Explorer supplies requirements for a shared Inspector and remains a
separate general typed-data workspace; it owns neither a second write stack
nor a route gateway. This is review support for that disposable split, not
approval of permanent packaging or a default App.

The smallest next Explorer deliverable is a **disposable fake-input shared
Inspector harness** with the five cases below. It closes the missing UI
projection/interaction evidence after E0's copy/wireframe review. It does not
repeat the completed August source-lock test or claim E1b/MVP0 completion.

## Exact sources and evidence boundary

All source labels below mean the committed `planning` object, obtainable with
`git show <commit>:<path>`; do not substitute the same path on another branch.
The September and August profiles are not interchangeable because both say C0.

| Ref | Exact commit and relevant paths | Use here |
|---|---|---|
| S1 | `12ef4c5b929759c87fcf4886a1619734a6f9a044` — `Designs/sdkv2/mvp-interface.md`, `Designs/sdkv2/README.md` | September five seams and operation-specific result families |
| S2 | same September commit — `Designs/efsv2/disposable-mvp-profile.md` §§1–2, 6; `Designs/efsv2/mvp-c0-genesis-manifest.md` G12 and first post-genesis write | B0 bundled control, point/result law, run/basis and activation evidence |
| S3 | same September commit — `Designs/web-client-os/mvp0-acceptance.md`; `Designs/data-explorer/README.md` | M0-01–M0-09 and File Browser/Explorer separation |
| A1 | `8d90ecbf85390f1151fa1b2dbf93852a1bfc8448` — `Reviews/2026-08-25-data-explorer-exp-c0-consumption/README.md`, `explorer-consumption-v0.json`, five `inputs/` files | Retained serialized-only Inspector evidence and explicit evidence ceiling |
| A2 | `b9088d6a24f4d40bcca6ba300523b25cc7c608d2` — `Reviews/2026-08-25-efs2-exp-c0-v0-control/trace-coverage.json` | Core source actually locked by A1; partial invariant controls, X1 submission/recovery still design-only |
| A3 | A1 Explorer commit — `Designs/data-explorer/experiments-and-stop-conditions.md`, `architecture-and-state.md` | E0/E1/E4/E6 questions retained as product evidence gates, not September wire requirements |

Coordinator-refreshed August readiness `2573f08b170bf3eb855ad5a68c31ee7b0215272d`
and SDK `57d04f85ae2687ee8ea63d945378df5a9a6492a5` were verified as remote refs.
They are comparison context, not substitutes for A1's older A2 source lock.
S1 cites that SDK revision as input but explicitly excludes its wire shapes.
This pass does not re-audit either entire branch.

A1 records nine positive/adversarial tests and a static consumption pass. Its
literal ceiling is `E1a=NOT_PROVEN_BY_THIS_CONTRACT`, `E1b=NOT_RUN`, runtime
dependency trace `NOT_RUN`. No old tests were rerun here. A1's guest booleans
are serialized dependency claims, not observed browser/network behavior.

## Reconcile semantics without translating the ABI

| August evidence | September consumption rule |
|---|---|
| Literal `ResultV0` bytes and separate presence/coverage/etc. codes | Retain them as August raw evidence only. September imports `PointResult` with `FOUND / ABSENT_PROVEN / UNKNOWN / CONFLICT` plus orthogonal facts; no numeric-code or field-name substitution. |
| Split Type/Query controls and opaque Type envelope | September decodes only its `BUNDLED_B0_C0` arm. `SPLIT_FUTURE` is not a supported C0 value. Unknown raw data remains exportable; no implicit schema conversion. |
| Combined August byte grades, lifecycle/admission and selection evidence | September exposes integrity, availability, returned-byte state, authority, currentness/finality and effect independently. Preserve original evidence; do not infer missing axes from an enum label, admission, or a retained payload alone. |
| Source-lock status `commit-ready`, or generic effect-unknown legality | Neither is a save receipt. September planning/authorization/submission add stage-specific evidence without synthesizing a point verdict; only independent matching read-back may establish committed effect. |

Both products need the same non-loss Inspector contract, not a universal DTO:
exact canonical input/output bytes or retained handles; profile/run and domain;
committed basis; coverage, support, validation; authority, currentness, finality;
integrity, availability, returned-byte state, effect; evidence commitment,
typed reasons and attempts. Source observations, canonicality, proof/receipt
scope and causal history availability remain inspectable **when obtained**;
missing evidence is not a fabricated pass. The SDK owns their interpretation
and verification. A view may summarize, never recompute or upgrade them. [S1–S2]

## Compact facts / UX matrix

Only the quoted human-facing phrases are proposed UI copy. Uppercase outcomes,
stages and effect terms are imported disposable September C0 terms, not a new
enum. Every row retains the full contract above; “minimum distinction” names
its sharp edge.

| Journey / seams | Minimum distinction | Proposed visible behavior and hard failure | Evidence carried forward |
|---|---|---|---|
| **Guest opening** — 1 exact read, 2 scoped page; 3 only for requested bytes | Exact route, run/profile, domain/basis and qualified outcome; zero wallet/account/Commons/profile/OS/Explorer dependency for September File Browser | Show useful qualified data plus a reachable Inspector. A missing required basis says “Cannot establish this read,” not “Not found”; no silent latest/Realm fallback. Exact File Browser still opens with Explorer code absent. | A1 only declares guest dependencies. S3 M0-01/M0-04/M0-09 still require actual fresh-browser traces; no browser run is claimed here. |
| **Unfamiliar Type** — 1 exact read | Unknown **presentation** is not unsupported **semantic read**. Preserve an upstream `FOUND` if only a rich view is unavailable; an unsupported required SDK/profile evaluation instead follows `UNKNOWN` with `support=UNSUPPORTED` and its cause. | “No rich view for this Type — inspect raw evidence.” Keep exact identifiers, raw bytes/unknown fields and qualifications. Never drop the item, call it corrupt merely for being unfamiliar, or fetch a renderer/catalog automatically. | A1 preserves an opaque unsupported Type exactly; it does not prove rendering, semantic reconstruction or multiple-family usability. |
| **Partial folder** — 1 container read + 2 scoped page | Container or observed entries may be `FOUND`; incomplete inventory coverage remains `PARTIAL/UNKNOWN`. An unresolved name is not proved absent. Scope/order/high-water/revision/basis constrain continuation. | “N loaded; listing incomplete” with reason and resume. Zero filtered rows says “No matches among loaded entries,” not “Empty folder.” Reject mismatched cursors; a new basis opens a new read/comparison. Only the complete-domain rule supports `ABSENT_PROVEN`. | A1's HELLO Inspector is not a directory/page execution. A2 has partial invariant controls, not the literal completed page journey. S3 M0-03 is the required September scenario. |
| **Bad bytes** — 3 verified byte read | Semantic File presence is separate from attempt integrity. A corrupt attempt can be `availability=AVAILABLE`, `bytes=RETURNED`, `integrity=FAILED`; later verified fallback preserves the same selected identity and both attempts. | “First source failed integrity; verified fallback used.” With fallback removed: “Preview unavailable — bytes did not verify”; no corrupt content reaches a trusted preview. Preserve bounded attempts and safe raw evidence access; do not relabel the File absent or the corrupt attempt unavailable. | A1 carries ordered bad-primary/good-fallback evidence; A2 calls it a partial invariant control. Live acquisition, terminal all-bad/unavailable behavior and presentation still need S3 M0-02/M0-05. |
| **Save pending → independently read-back-verified** — 4 plan/authorize/submit, then 5 read-back using 1–3 | Preserve exact plan digest, operation kind, predicted effects, source-read handles and separate receipts. `SUBMITTED` or `INCLUDED` does not prove the write: canonical effect remains unknown. Only independently matching every required planned effect at a committed basis permits `READ_BACK_VERIFIED` and `effect=COMMITTED`. | Before match: “Submitted — checking saved state,” with stage and basis. After match: “Saved — read-back verified at [basis].” Missing evidence stays pending/unknown; mismatch/revert gets its own supported explanation. Never infer finality, future durability or currentness from “saved.” Retain prompt/setup accounting supplied by the shared journey. | No completed August Inspector write journey: A2 X1 is design-only. S3 M0-06–M0-08 specify the three exact operations and independent read-back; an Explorer timeline may simulate display only, not award this evidence. |

## Gates that still answer a real question

| Gate | Keep / narrow for September | Evidence still owed |
|---|---|---|
| **E0 — comprehension/accessibility** | Keep the five-state copy and small Inspector wireframe; do not require the entire future workspace. Preserve the original participant/threshold rules for any claim of E0 pass. | Real participants, keyboard/screen-reader and narrow/zoom paths; no participants or usability observations in this pass. A desk review may refine copy, never count as participant evidence. |
| **E1a — deterministic fake-input UI** | Keep. Exercise summary → raw/attempt detail → safe export, partial-page resume, and the staged save timeline with explicitly simulated inputs. Mark every frame/report simulated. | Actual interaction and semantic-preservation assertions. August's serialized checker is only a prerequisite; do not call it an E1a run. |
| **E1b — integrated cold path** | Keep the real-adapter/independent-read question. September's local resettable MVP-C0 browser gate is M0-01–M0-09, with File Browser independent of Explorer. Reuse those traces only for their exact profile/claim. | Fixed-basis pages, verified acquisition, cold reconstruction and dependency/network traces through real disposable seams. A localhost or local-chain run is not the original public-Realm E1b pass. |
| **Original Explorer direct/OS-hosted parity** | Retain for later Explorer hosting/product claims; do not make OS hosting or Explorer packaging a prerequisite for September MVP0. | Two genuinely cold applicable Explorer hosts and public-Realm evidence under separately authorized conditions. No public deployment is authorized tonight. |
| **E4 / E6** | E4 faults accompany each applicable fake/integrated read. E6's “acknowledgement is not success” law applies to shared save UI; wider Explorer write certification remains separate. | M0-03–M0-08 do not become passed by rendering their expected states. E2 rich tables and E5 extensions remain outside this smallest deliverable. |

## One next implementation-sized deliverable

Build **one disposable, profile-labelled shared Inspector harness** after the
E0 copy/wireframe review. It takes explicitly fake operation-specific September
inputs; shows summary, raw/attempt detail and an operation timeline; and emits
an exact presentation/evidence-reference transcript. Keep the August packet as
a separately labelled raw comparison, not converted September input. No Core
codec, verifier, resolver, planner, signer or network source belongs in it.
Use SDK-supplied, profile-qualified raw samples when available; otherwise mark
that evidence unavailable/simulated. Do not invent a September wire encoding or
claim that fixture-authored bytes are canonically validated.

Acceptance: all five matrix rows and their negatives preserve the full input
qualification and raw handles through view switch, retry and export; unfamiliar
views do not alter semantic support; empty partial pages stay incomplete; bad
attempts never become verified previews; save success cannot appear before a
**simulated** matching read-back event. A missing/changed plan, operation or
basis invalidates that match. Keyboard access and readable text status are
required. The fake evidence ledger remains separate from any later earned
read-back receipt. No framework/package/default route is selected here.

This is one UI-consumption slice, not a full Explorer, a second File Browser or
an SDK implementation. File Browser can reuse the same presentation contract
without importing Explorer navigation/workspace modules. A later real trace
must satisfy S3 with Explorer absent; a stub-host rendering test cannot prove it.

**Owner choices now:** none. SDK/Files/Core must supply a profile-qualified
input/evidence fixture and independent effect oracle for the integrated arm;
Web Client/File Browser supplies the real route and trusted action ceremony.
These are bounded coordination dependencies, not requests to freeze a profile.

## Verification of this pass

Read the exact source revisions above and independently reviewed A1's evidence
ceiling. No source-lock test, Core/SDK experiment, browser execution or
participant session was rerun. New work is this matrix and gate reconciliation
only; structural/link checks and planning validation are recorded in the
commit handoff. Existing August design files and receipts remain unchanged.

## Upstream delta consumed — 2026-09-04

**Delta source:** September `5d7530993339a0786aa41e1dbb0fd786cd450f32`,
`codex/mvp-c0-coherence`. The S1–S3 comparison above remains pinned to
`12ef4c5`; it is not retroactively relabelled. This acknowledgement consumes
the two draft repairs and the coordinator's
`Reviews/2026-09-04-mvp-convergence/README.md` / `acceptance-map.md` at the
delta commit. No branch merge or rebase is involved.

- **G4 ordering:** declared left-to-right member order and zero-based indexes
  are fixed before Type/group hashing; `SELF` / `GROUP_REF(k)` retain their
  sentinel meaning. Explorer must preserve those source indexes and raw
  references; a display sort cannot reorder the canonical group or rederive
  its IDs. Group parsing/hashing still belongs to the shared codec.
- **Grant-bound lanes:** normal/direct EOA uses lane zero. Every nonzero
  session lane is permanently bound to one grant per bootstrap Principal,
  including after expiry/revocation. Inspector/action evidence must retain the
  exact grant, nonce lane and admission basis; retry or grant renewal cannot
  silently substitute a grant or recycle a lane. Historical receipts retain
  their original authority basis. Enforcement remains with SDK/Core, not UI.

The inspected delta does not change the five seams, point-result law or
`WritePlan` field list. The facts/UX matrix and next deliverable therefore stand;
their next September fixtures must name the repaired source. An affected run
must follow the existing new-run/profile-commitment rule, not overwrite old
evidence. The save timeline still cannot award committed effect from a grant,
signature, nonce reservation or transaction acknowledgement.

The coordinator reports independent repair review and **8/8 synthetic law
tests**; those tests were not rerun here. They establish neither full Type
parsing/signatures/nonce-budget enforcement nor Core/browser execution. The
nine MVP0 journeys remain unrun in that checkpoint. In M0-05 reporting, no
verified bytes for presentation must not relabel a corrupt attempt's
`availability=AVAILABLE`, `bytes=RETURNED`, `integrity=FAILED` axes.

The Web Client/OS lane's superseding owner assignment is respected; no retask
or dependency on its response is introduced. Explorer's handoff is reconciled
through this exact delta, not a claim about later branch heads. No new owner
choice or production authority follows.
