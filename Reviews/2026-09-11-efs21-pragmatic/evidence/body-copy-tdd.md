# Bounded RecordBody.slice TDD evidence

Control:24d74076d38d7b7758fc1eb222c8d7183da85c02. This is a disposable same-semantics copy experiment; helper runtime identity changes.

1. Complete control source archive and canonical artifacts were retained before the production change. Archive SHA25691a4e9f6b4a438f7e59706d3a65e05a613be647b45b1ae076d3664aa259bf78c. Exact old helper creation/runtime/ABI/metadata are in body-copy-control-helper.json.
2. Actual old helper received two identical cold paid prepareRecord calls for a valid8192-byte BYTES body. Both succeeded, each gas2501400, with byte-identical returns. The predeclared strict-reduction assertion failed as intended. Signed transactions, receipts and stopped-node cleanup are in body-copy-helper-red.json. An earlier incorrect four-field KernelIds selector failed setup; it is NOT the intended RED. The actual tuple has three fields.
3. BodyCopy.t.sol was written before replacing the byte loop. A private-to-internal visibility-only test seam exposed the actual function, and5/5 tests passed against the original loop. It is not the performance RED. The oracle remains the original allocation-first byte loop. Boundary/nonzero/unaligned/last-span/8192 cases,128 fuzz runs, exact Panic32/Panic41 ordering, empty huge starts, source/padding/free-pointer/zero-slot and adjacent allocation checks pass. The function allocates and writes atomically: the harness has a pre-existing preceding nonzero canary and a following allocation after the call, not a pre-write following canary. No such stronger test is claimed.
4. Only actual RecordBody.slice changed: allocate first; return immediately for zero length; subtraction-based bounds; exact array-bounds panic; MCOPY exactly n. Focused candidate BodyCopy/RecordBody/Preparation passed31/31. No TypeGroupParser or StateKernel copy loop changed.
5. Actual changed helper paid calls use identical calldata/cache/returns to the old-helper RED and each consume183993 gas. This is helper-only evidence, not full operation savings. Candidate runtime19032 versus18953 control; U3 remains24141 and admission22392.
6. A separate finite world deployed the exact frozen old helper beside the candidate. Five groups (four retained groups covering16 Types plus a nested/MAP/range group) produced byte-identical caches.102 public prepareRecord calls across both bodyOnly modes agreed byte-for-byte, including reference/index/effect outputs, trailing/nested/MAP-order/duplicate/UTF8/range refusals and a labelled forged-descriptor private helper input.44 succeeded and58 refused. The differential runner asserts both actual calls; the evidence retains the common bytes. Two harness setup failures involved ethers Result proxy conversion, not contract mismatches; both worlds were automatically stopped. Final world PID16419 exited0 and removed its cache. Existing structural parser tests separately retain literal expected error codes.

Helper probe and differential receipts precede the final source freeze and are explicitly test evidence. The separately named full-operation pair is generated only after the source/support commit. Old evidence and hashed support files are unchanged. Legal large-cache/aggregate-return limits,64-unique ordinary-cap refusal and unmeasured paid receipt-library costs remain explicit gates, not claimed solved by the helper probe.
