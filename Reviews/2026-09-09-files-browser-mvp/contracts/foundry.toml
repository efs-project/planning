# Disposable local files-browser router experiment.
[profile.default]
src = "src"
test = "test"
out = "out"
solc = "0.8.30"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
via_ir = true
ast = true
build_info = true
