# R1-efsv2-spine — EFS 2.0 spine map (README, constitution, candidate, rulings, gates, transition, pass syntheses)

Lane: R1-efsv2-spine · Reviewer date: 2026-09-02 · Vault: the planning vault (read-only)

Method note: git history for this vault begins at commit `c48f252` (2026-08-13, "design: make Stage A preflight sequential"), in which every spine file appears as a **new file**. Later commits touching my lane: `02bdae9`, `da5fcc3`, `5d1242e` (all 2026-08-14). Therefore every "Last touched" date earlier than 2026-08-13 is **UNVERIFIABLE** from git; I report them as the files state them. No file in this lane has a commit after 2026-08-14. Neighbour files I opened for cross-checks: `Decisions.md`, `Open-Decisions.md`, `Retirements.md`, `Glossary.md`, `Kanban.md`, `Designs/web-client-os/README.md`, `Designs/web-client-os/mvp-and-acceptance.md`, `Designs/efsv2/layered-type-system-and-data-abi.md`, `Designs/efsv2/hierarchical-files-and-folders.md`, `Reviews/2026-08-13-claude-evidence-round/README.md` + `CORRECTIONS.md`, `Reviews/2026-08-13-efs2-stage-a-corpus/{STATUS.md,stage-a-report.md,pm-stage-a-directive.md,chapters/*,corpus/proposed-spine-edits.md}`.

---

## 1. Headline

The spine has a clean three-document core (`README.md` → `system-constitution.md` → `core-architecture-candidate.md`) plus an authority ledger (`owner-rulings.md`) and an evidence-gate queue (`owner-decision-inbox.md`), all dated 2026-08-12/14. Those five are internally consistent on the big shape (standalone Core / optional Commons / direct guest client / optional OS; Record vs Occurrence vs Admission; mandatory indexing; honest `UNKNOWN`). The spine is, however, three weeks behind its own inputs: the 2026-08-13 Stage A corpus produced a BLOCKING sequencing contradiction and 16 proposed spine edits that nobody integrated; the owner's 2026-08-14→23 client directions (write-capable File Browser MVP, uniform `PrincipalId`, 64-entry Lens, Sepolia dev Commons) live only in `Designs/web-client-os/README.md` and contradict the constitution's open questions and the inbox's V2-E6 wording; and the "current Type-system proposal" the README points at reintroduces `TypeRevisionId` with a different `RecordId` preimage than the candidate. Eleven of the sixteen docs in this lane are July material; six of them carry `#status/draft` with no historical banner and are not classified by the README at all. Nothing here is unsound at the concept level; what is missing is one agreed sequence, one vocabulary, and a spine that reflects what the owner and Stage A already said.

Maturity: **draft-unreviewed** for the constitution/candidate (both `Reviewers: —`, no `#status/review` round, Stage A red-teamed *its own* B0, not the spine); **reference** for the rulings ledger and inbox; **historical-evidence** for the eleven July docs.

---

## 2. Per-document summaries, standing, and defined terms

| # | File | Own status line | README standing | My standing | Defined terms / IDs / ABI words |
|---|---|---|---|---|---|
| 1 | `README.md` | none (no header block; `#status/draft` in §Status text only) | phone summary / entry point | **current**, stale vs Stage A and vs web-client-os directions | `TypeSchema` (current name), `TypeRevision` (older), Core/Commons/Web Client/OS layer names, formula "Type Schema + Record + Envelope/Context = Occurrence + admission receipt, indexes, Binding, Lens" (§Current technical candidate) |
| 2 | `system-constitution.md` | draft, Last touched 2026-08-12 | deep-pass doc 1 | **current** (requirements center) | `PrincipalId` (bytes32 full width), `FOUND/ABSENT/conflict/unsupported/UNKNOWN` (Lens), `COMPLETE/PARTIAL/UNSUPPORTED/UNKNOWN` (enumeration), Resolution Plan, Locator, Envelope/Context, Occurrence, Realm, Commons; "TypeRevision" listed as *not frozen* (§What is deliberately not frozen) |
| 3 | `core-architecture-candidate.md` | draft, Last touched 2026-08-12 | deep-pass doc 2 | **current** (comparison target) | `RealmId`, `RealmRevision`, `TypeSchema{…}`, `TypeSchemaId`, `TypeId/ShapeId/IndexProfileId` (Variant B), `Record{typeSchemaId, canonicalBody}`, `RecordId = H(domain, typeSchemaId, canonicalBody)`, `ObjectGenesis/1`, `PublicationEnvelope`, `AdmissionIntent?`, `leafIndex`, `Occurrence=(EnvelopeId, leafIndex)`, `AdmissionReceipt{…}`, `PositionKey`, `BindingKey`, `Binding`, Withdrawal, `AccountPrincipal/1`, `PrincipalId = H(domain, canonical(AccountPrincipal/1))`, `IndexSpec[]`, `ResolutionPlan{…}`, `Locator/1`, `ByteDigest/1`, `ArtifactClosure/1`, `RepresentationBinding/1`, `ArtifactRelease/1`, modules Codex/RecordStore/Admission/Index/Binding/LensResolver, "Recognition Record" (named once, undefined) |
| 4 | `owner-rulings.md` | reference, append-only, Last touched 2026-08-12 | deep-pass doc 3 (authority) | **current reference** — ends 2026-08-12; owner directions 2026-08-14→23 are not in it | Rulings 2026-07-10 (chains don't die; KEL design it; lenses; public-by-default; storage), 07-15 (18-item on-chain list A–G, 12–18; KEL persona), 07-16 (recovery; portability D-1/2/3), 07-22 (three-host mount; research-before-MVP), 07-23 (correction), 08-07 (Git direction), 08-12 (greenfield boundary) |
| 5 | `owner-decision-inbox.md` | reference, Last reconciled 2026-08-12 | deep-pass doc 4 | **current reference** — V2-E1…E8, F1, F2 waiting on evidence; P-1…P-23, LP-1…10, N2–N6, Q1–Q4 superseded | V2-E1..E8, V2-F1, V2-F2; superseded P-/LP-/N-/Q- IDs |
| 6 | `confidence-and-open-decisions.md` | draft, Last touched 2026-07-07, banner "Historical snapshot — not current design authority" | **not listed** in evidence map | historical-evidence (banner is pre-greenfield: routes to `assumptions-and-requirements`/`human-overview`, which are themselves now evidence) | tag-core kinds TAGDEF/DATA/LIST/PIN/TAG, `tagId`/`dataId`, `EFSBytes`, tiers 0/1/2/blob, `contractReadable`, Codex |
| 7 | `freeze-gates.md` | draft, Last touched 2026-07-12, "2026-07-12 reconciliation block" | **not listed** | historical-evidence (block predates 08-08/08-12; says "adopt a short system constitution … then replace or regenerate this gate list" — never regenerated) | A1–A10 ratification list, B executable gates (incl. dead-chain fire drill, HomeRegistry, EIP-170 skeleton), C one-final-freeze pledge scope, D thirteen-contradiction reconciliation, `seq→order`, `claimedAt`, `admittedAt` |
| 8 | `efs-v2-transition-plan.md` | draft, Last touched 2026-07-01, **no banner** | **not listed** | historical-evidence by content; reads normative | Phases 0–6, EAS behavioral pin, `@efs/ids`, v1 Sepolia disposition / re-attestation, abort triggers |
| 9 | `efs-v2-holistic-redesign.md` | draft, Last touched 2026-07-01, banner "Historical umbrella" | **not listed** | historical-evidence (banner pre-greenfield) | freeze bundle §1, conventions §2 (dirnodes, move doctrine, `web3://efs.eth/<addr>/<path>`), workstreams §3 |
| 10 | `joined-pass-synthesis.md` | draft — "ruling record of the 2026-07-25 joined reconciliation pass", **no banner** | **not listed** | historical-evidence by content; reads normative ("this list wins over any lane sentence") | JR-1…JR-10, D-1…D-15 ledger, `PORTABLE-EVIDENCE`/`AUTHORITY-ADMITTED`/`EVIDENCE-ORDERED@N`, `ABSENT_PROVEN` four sources, FS-LENS/1, `.efs-bundle` |
| 11 | `fable-efs2-core-engineering-kickoff.md` | reference — copy-ready kickoff, Last touched 2026-08-12 | "Focused next-pass prompt" | **current reference, but consumed** — Stage A ran 2026-08-13 | labels `owner ruling/derived invariant/proposal/hypothesis/rejected`; seven bakeoff axes; outputs 1–8 |
| 12 | `fable-next-pass-scope.md` | draft (scope LOCKED by James 2026-07-08), Last touched 2026-07-08, **no banner** | **not listed** | historical-evidence by content; reads normative | Pass 1/2/3 plan; "settled decisions every pass must respect: native kernel, author-from-signature, no cross-chain currency, first-attester-wins lenses, string-only properties, permissionless byte pool" |
| 13 | `ops-doctrine.md` | draft, Last touched 2026-07-07, **no banner** | **not listed** | historical-evidence by content; "Adopted core (unamended)" reads normative | lens conformance C1–C5, `/.well-known/lens`, `/.well-known/relayer`, `expiresAt`, STALE/EQUIVOCAL/UNKNOWN-CURRENCY grades, amendments 1–10 |
| 14 | `os-pass-handoff.md` | draft — input to the next (OS) pass, Last touched 2026-07-10, **no greenfield banner** | **not listed** | historical-evidence by content; "Reserved surface you can rely on" reads normative | `admittedAt`, `claimedAt`, `act`, `lang`/`dir`, `encryptionKey`, WHITEOUT `/.well-known/whiteout`, `clientId = f(author, deviceBits)`, P1–P13 adjudications |
| 15 | `efs-substrate-decision.md` | draft, Last touched 2026-07-02, banner "Historical carrier decision" | **not listed** | historical-evidence (banner pre-greenfield: "Use assumptions-and-requirements for current classification") | three-way decomposition (single-chain guarantees / portable authenticity / portable currency), §3 reservations, §5 mission ruling (permanent archive, no free tier — RULED 2026-07-02), §7 experiments |
| 16 | `multichain-dependency-map.md` | draft — deliverable 3 of the 2026-07-25 pass, **no banner** | **not listed** | historical-evidence by content; points at superseded P-5/P-3 as live decision sites | R-XC1/2/3, R-CR4, R-AU8, L1–L10 local-mode losses, H1–H3 |

The README's `## Status` line "The two active docs are `#status/draft`" is wrong on its face: the README itself names seven current docs (constitution, candidate, rulings, inbox, kickoff, `layered-type-system-and-data-abi`, `hierarchical-files-and-folders`), and 40+ files in the folder carry `#status/draft` (my tag sweep). `hierarchical-files-and-folders.md` is `#status/review` while the README calls it "a draft experiment target" (`README.md` §Current technical candidate, last paragraph).

---

## 3. Lane question 1 — constitution vs candidate: naming and promise differences

| Concept | `system-constitution.md` | `core-architecture-candidate.md` | Other current docs | Verdict |
|---|---|---|---|---|
| Type identity name | "the name `TypeRevision` or any exact Type schema" is *not frozen* (§What is deliberately not frozen) | `TypeSchema` is the "Working replacement for the confusing name `TypeRevision`" (§Type Schema); Variant A `TypeSchemaId` vs Variant B `TypeId`/`ShapeId`/`IndexProfileId` | `README.md` §Current technical candidate: "`TypeSchema` is the current plain-language name; older files call similar concepts `TypeRevision`". **But** `layered-type-system-and-data-abi.md` (README-designated "Current Type-system proposal", 2026-08-14) uses `TypeRevisionId` as its primary identity (lines 200, 344–348, 380) built from `SemanticSpecId + LogicalShapeId + RepresentationId`, plus `ViewRevisionId`, `QueryProfileId`, `ViewQueryProfileId`. Stage A B0 pins Variant A with `TypeSchema` (`chapters/b0-overview.md` §1, arm 4). `Glossary.md` line 240–242 defines `TypeSchemaId` and calls `TypeRevision` a "similar object". `Designs/web-client-os/README.md` direction 12: "The Type/query-identity axis remains open." | **Three vocabularies among "current" docs; no crosswalk.** See finding F4 |
| `RecordId` formula | "deterministic, domain-separated, versioned" (§Universal identity) | `RecordId = H(domain, typeSchemaId, canonicalBody)` (§Record) | `layered-type-system-and-data-abi.md` line 380: `RecordId = H(DOM_RECORD, TypeRevisionId, H(canonicalBody))` — nested body hash, different preimage shape | Different preimage shapes in two current docs, not flagged anywhere. F4 |
| Record / Occurrence / Envelope / Context | Record = exact typed content; "signed Envelope may carry a subset or amortize context" (§One transaction); "Immutable descriptors or batch context may safely share repeated data" (§Minimal typed data) | `Record{typeSchemaId, canonicalBody}`, `PublicationEnvelope{…}`, `AdmissionIntent?{realmId, action, occurrenceRefs, nonce/expiry, authorization witness}`, `Occurrence=(EnvelopeId, leafIndex)` | Stage A B0 arm 3 = portable Envelope + Realm-bound AdmissionIntent; `mvp-and-acceptance.md` dependency table row "Authored publication plus Realm admission — PublicationEnvelope plus Realm-bound `AdmissionIntent` — Candidate requirement from B0/Files reconciliation" | Consistent on Record/Occurrence. **`AdmissionIntent` exists in the candidate and downstream but the constitution never names a Realm-bound intent**; its open question "Are authored Occurrences portable across Realms … or deliberately Realm-bound?" is the same axis. Not wrong, but the constitution should name the concept it is gating. |
| Principal | one `PrincipalId` author surface; EOA/smart account as zero-setup account Principal; full `bytes32` end to end; KEL not frozen into MVP (§Authorship and authority) | `AccountPrincipal/1 = {authorityKind, originIfRequired, accountOrKey}`, `PrincipalId = H(domain, canonical(AccountPrincipal/1))`; "contract-account authority is Realm-qualified"; multi-actor/rotation/recovery = "Later managed Principals" (§Principal) | web-client-os direction 7 (one Principal, mutable default controller account, three controller keys) and 8 (keys don't consume Lens entries; "key authorization belongs inside Principal verification") | Consistent between the two spine docs; **the owner's client direction needs a multi-controller Principal that the candidate defers past the MVP** (F11) |
| Realm | "qualifying EVM Realm" used at lines 38, 60, 303, 353 — **never defined**; Commons gets CROPS criteria, Core's "qualifying" gets none | `RealmId = H(immutable profile + chain reference + genesis/deployment commitment)`, `RealmRevision`; descriptor is a bakeoff target | Stage A `chapters/b0-realm-admission.md` §4 "Qualifying-Realm assumptions QR-1..QR-8" — proposal only | Naming consistent; **"qualifying" undefined in both** (F8) |
| Binding | mentioned as a Principal-bearing thing (line 145) and in the not-frozen list (line 286); nearest requirement is "moving selections are distinct" (line 103) and "explicit predecessor/CAS rules" (line 154); never defined or required as a primitive | `PositionKey = H(purpose, subject, fieldRole)`, `BindingKey = H(principalId, PositionKey)`, `Binding{positionKey, targetRef|tombstone, predecessorOccurrence, revision}`, Withdrawal | `hierarchical-files-and-folders.md` builds on `PositionKey`/`BindingKey` (lines 395–398, 622–623) and adds `BindingScope` (§5, line 658) which `README.md` says is "neither … current B0" | Candidate defines; constitution assumes. **Complete enumeration of Bindings in a directory is required by the constitution (lines 205–209; Files/mount traces lines 316–317) but has no Core primitive in the candidate** (F3) |
| Lens / Resolution Plan | "Lenses are required"; "may compile to an immutable Core-compatible Resolution Plan"; resolver returns `FOUND`, proved `ABSENT`, conflict, unsupported, or `UNKNOWN` (§Lenses) | product term Lens; `ResolutionPlan{purposeAndScope, orderedOrTieredPrincipalIds, boundedPointCombiner, limits}`; "The result is typed and may be `UNKNOWN`"; "`UNKNOWN` is reserved for unsupported profiles, partial replicas…" (§Contract Resolution Plan) | `hierarchical-files` makes it a Record Type `ResolutionPlan/1` (lines 92, 504–506); Stage A B0 uses `FOUND/ABSENT/CONFLICT/UNSUPPORTED/UNKNOWN`; Stage A contradiction ledger #2 flagged "constitution five vs candidate unsupported-inside-UNKNOWN" | **Outcome vocabulary differs** (five vs UNKNOWN-with-causes); whether a Plan is a Record is unstated in the candidate (F9) |
| What a contract Lens resolves | "point resolution over Principal-qualified claims" (line 195) | "probing admitted Binding heads" (line 314) | — | Constitution says claims; candidate says Bindings only. Minor wording gap, worth one sentence. |
| Revocation vs Withdrawal | "revocation" is a Realm-qualified state (lines 107, 156) and "revocation-aware current counts" are a costed gate (line 181) | defines only **Withdrawal** ("its issuer no longer maintains it"; "does not delete the Record, retract another issuer's Occurrence, or rewind a Binding") — the word "revocation" appears once (line 64) with no primitive | owner-rulings 2026-07-15 item E: "revocation-aware state (count drops when endorsements are revoked) is core to EFS" | **Promise without primitive** (F6/F10) |
| Baseline reads | exact Types, Records, Occurrences, receipts, global admission order, unique Records by Type, Occurrences by Type/Record/Principal (lines 165–169) | same list **plus** "current Binding point reads with complete Realm-local absence at a basis" (line 272) | — | Candidate's baseline is a superset; fine, but the constitution should say Binding point reads are baseline since Lenses depend on them |
| Costed-gate outcomes | reverse membership/cited-by, content-digest lookup, author enumeration, **revocation-aware current counts**, **deterministic best-locator selection** (lines 180–184) | `IndexSpec[]` = scalar equality, typed reference equality, target-first backlink, optional compound key (lines 274–279); **no counts, no locator ranking** (grep: no "count", no "best") | Stage A `chapters/b0-indexes.md` §6 "Revocation-aware current counts" and §7 "Deterministic best-locator selection" — proposal only, unintegrated | **Constitution promises what the candidate does not design** (F6) |
| Enumeration status words | `COMPLETE`, `PARTIAL`, `UNSUPPORTED`, `UNKNOWN` + Realm/basis/high-water mark/cursor/coverage (lines 206–209) | "cursor, coverage, and completeness"; `PARTIAL` (lines 290–298); page-result ABI is an open question | — | Consistent in intent; ABI unspecified |
| Reviewer set for the engineering pass | "long-horizon, database, EVM-security, and standards" (§Freeze discipline 4) | — | README build order 4: "database, EVM/security, standards, privacy, and long-horizon"; Stage A ledger #7 flagged the divergence | Minor; union list proposed by Stage A B1, unapplied |
| Equivocation / challenge window | silent | silent | owner-rulings 2026-07-15 item F "RULED — SIGN THE LIMITATION" with wording "to ratify"; `pm-stage-a-directive.md` line 24 "Preserve the equivocation/TOCTOU lesson and challenge-window safety requirement"; Stage A A1 proposes restoring it | **Attributed ruling absent from both spine docs** (F7) |
| EAP fixture | "must be backed by a durable EAP brief before freeze" (line 312) | fixtures list EAP (line 453) | no EAP brief exists in the vault (`find -iname '*eap*'` empty; grep finds only the constitution); PM directive: "Treat EAP as provisional until Codex supplies a durable brief" | Missing artifact (F16) |

Net: no direct contradiction on the core algebra (Record ≠ Occurrence ≠ Admission ≠ Binding ≠ Lens), but the constitution promises five things the candidate has no primitive for (complete enumeration, revocation, counts, best-locator, equivocation non-guarantee), and the "current Type proposal" uses a different Type name and a different `RecordId` preimage than the candidate.

---

## 4. Lane question 2 — current vs July evidence, and whether banners agree with the README

The README evidence map (`README.md` §Evidence map) lists 13 rows. **None of the eleven July docs in this lane is listed** (items 6–10, 12–16 in §2). Their own status/banner state:

- Banner present but pre-greenfield (route the reader to docs that are themselves now evidence): `confidence-and-open-decisions.md` ("Start with owner-rulings, assumptions-and-requirements, and human-overview" — the latter two carry 2026-08-12 "not the current normative entry point"/"no longer the current system summary" banners), `efs-v2-holistic-redesign.md` (same pointer), `efs-substrate-decision.md` ("Use assumptions-and-requirements for current classification and solana for the current substrate-portability pressure test"), `freeze-gates.md` (2026-07-12 block: "adopt a short system constitution, re-cut the joined specifications, and then replace or regenerate this gate list" — the constitution exists; the list was never regenerated; still `#status/draft`).
- **No banner at all, `#status/draft`, normative voice** (README calls nothing about them; the brief's layering rule cannot be applied by a reader who opens them cold):
  - `efs-v2-transition-plan.md`: Phase 1 "EAS behavioral pin (bytecode hash + semantics)"; Phase 5 "**Sepolia v1 disposition:** … offer a re-attestation courtesy under v2 IDs … banner the v1 public write path for the transition"; header "Supersedes: … the v1 UID set in `contracts/docs/SEPOLIA_FREEZE_TABLE.md`". Every one of these is a v1-compatibility/migration/coexistence step the 2026-08-08 ruling forbids (`Decisions.md` line 23; `owner-rulings.md` 2026-08-12). `Retirements.md` shows phrases `v1 coexistence`/`EFS 1.5 bridge` "Cleared" — the scanner is phrase-based and this text uses other words.
  - `joined-pass-synthesis.md` line 17: "[[owner-decision-inbox]] now carries the revalidated, decomposed, dependency-ordered P-1…P-23 packet"; line 73 open question "James: the owner-decision-inbox packet (P-1…P-23; the hold's lift is his call)". The inbox lists P-1…P-23 under "Superseded questions — never revive silently".
  - `multichain-dependency-map.md` §Open questions: "P-5 and P-3 in [[owner-decision-inbox]] are where the conditional rows above get decided" — both superseded.
  - `fable-next-pass-scope.md` §How to read this: "Settled decisions every pass must respect (don't re-litigate without loud cause): native kernel, author-from-signature, no cross-chain currency, first-attester-wins lenses, string-only properties, permissionless byte pool" — all reopened by 2026-08-12 ("no mechanism survives automatically").
  - `ops-doctrine.md` §Adopted core (unamended): lens conformance C1–C5, relayer mortality invariant, `expiresAt` in signed bytes — July envelope grammar (Q1/Q2 superseded in the inbox).
  - `os-pass-handoff.md` §What changed under you: "Reserved surface you can rely on: `lang`/`dir` minted; persona-link + `label` reserved; `act` reserved; `encryptionKey` minted … WHITEOUT ships as `/.well-known/whiteout` REF-PIN; per-chunk SHA-256 in EFSBytes" — none of these objects exists in the candidate.
- Current and correctly labelled: `README.md`, `system-constitution.md`, `core-architecture-candidate.md`, `owner-rulings.md`, `owner-decision-inbox.md`, `fable-efs2-core-engineering-kickoff.md`.

Does any current doc still rely on the July claims? Not directly — the constitution restates the surviving *outcomes* generically. The risk is operational: the vault's `#status/draft` tag is what the tri-sync/needs-integration scripts and a phone reader use, and the README's own "two active docs" line is false. The brief's fixed context says audit scripts run clean; they run clean because unbannered normative July prose is not a "retirement". See F12.

Also: `README.md` does not mention the Stage A corpus at all (`grep -i stage` returns nothing), while `Kanban.md` In Flight (line 42–43) says Stage A completed 2026-08-13 and "next: execute disposable Stage B"; Stage A's `corpus/proposed-spine-edits.md` B2 explicitly proposes "Add the Stage A doc set + corpus to the README doc table". The README's "Focused next-pass prompt: [[fable-efs2-core-engineering-kickoff]]" points at a pass that has run. See F13.

---

## 5. Lane question 3 — freeze-gates / transition / confidence vs the 2026-08-08 ruling and 2026-08-12 reset

- `freeze-gates.md`: every A-item and B-gate assumes the July kernel — A9 "on-chain-completeness bundle" (full-body spine, `definitionId` postings, B5 revoke-echo, best-mirror view), A10 "KEL/account foundation" (per-principal co-located KEL/admission home via sparse L1 locator, HomeRegistry), B "Dead-chain fire drill (kill devnet…) — never yet run", "HomeRegistry + migration/finality model", "EIP-170 skeleton compile" of a "~2,300–2,900 LoC" Etched artifact, C pledge scope binding "the envelope wire format + … KEL authority seam". Post-08-12: home chain not revived; KEL topology must re-earn inclusion; dead-chain machinery not to be reopened (`pm-stage-a-directive.md` line 25). The doc's own block says it "is not the current protocol authority" — correct — but its remedy ("re-cut the joined specifications") describes continuity with the July design, not a greenfield restart. Coherent only as evidence.
- `efs-v2-transition-plan.md`: assumes EAS carrier (Phase 1, Phase 3 "EAS multiAttest", "Contract inventory … EFSIndexer, EFSRouter"), v1 migration/disposition (Phase 5), a "home" Sepolia deployment being transitioned in place, and abort triggers falling back to "salt-field-only reservation + PathRegistry overlay + in-account 7702 routine". Directly contrary to the 08-08 ruling on four counts (compatibility, migration, coexistence, legacy read). **No banner.** Coherent with nothing current.
- `confidence-and-open-decisions.md`: "Native kernel, not EAS `[ruled]`" — under the reset EAS is neither carrier nor banned ("An EAS import/export adapter remains possible", `README.md` §Current technical candidate); "One chain-free EIP-712 signature over a Merkle root … `[verified-in-code]` — the highest-confidence thing in the design" — that verification was of the July envelope, and nothing in the current spine says it carries to `PublicationEnvelope`; the constitution's "One transaction" section states the *outcome* (bounded dependency graph, one call, all-or-nothing) without the mechanism. "Storage/uploads decisions — RULED (James, 2026-07-07)" items 1–5 (`contractReadable`, permissionless byte pool, frozen `EFSBytes`, L2/L3-first, reserve blob tier) are attributed rulings that the owner-rulings ledger does **not** contain (ledger starts 2026-07-10); the 08-12 reset says mechanism-level rulings are superseded but "the problems they were solving" survive. Whether "fully permissionless byte pool — no gating" and "everyone pays their own writes, no free tier" (`efs-substrate-decision.md` §5, RULED 2026-07-02) still bind EFS 2.0 is not stated by any current doc. I flag this in the undecided list, not as a headline finding, because these are outcome-level and the constitution's "Permanent-public-data tools warn and may refuse … at the client edge; Core remains neutral" is compatible.

So: none of the three assumes a home chain *as a current claim*, but all three assume EAS/July mechanisms and v1 continuity, and only two of the three say so.

---

## 6. Lane question 4 — Build order vs 2026-07-22 vs the write-capable File Browser direction

Four sequences exist:

1. `README.md` §Build order: (1) review constitution/candidate vs survivor ledger; (2) two disposable Core prototypes; (3) benchmark; (4) "Run the focused Fable 5 pass plus independent … reviews"; (5) integrate, close owner choices, freeze bundle; (6) "In parallel, build the narrow direct Web Client/File Browser + one-game Arcade slice behind an adapter".
2. `owner-rulings.md` 2026-07-22 §Research sequencing before an MVP pass: "Do another joined deep pass before contracting to an MVP … The contraction gate comes later. After the joined pass and comparable prototypes, reconcile the owner inbox, write the short constitution and explicit support matrix, and only then choose the MVP. The matrix should distinguish required, extension-ready, experimental, and explicitly unsupported behavior." The constitution exists; **no support matrix exists** (grep: only the ruling, July `ethereum-first-efs-and-os.md` line 344, and Stage A `proposed-spine-edits.md` C9 "Four-tier support matrix: fold into output 7 or retire — flag for PM").
3. `Designs/web-client-os/README.md` direction 2 (2026-08-14→23): "The first MVP must be an official write-capable File Browser, not a read product plus a substitute debug page … Guest reading remains independent of that path." §Current work sequence 3: "MVP critical path: freeze only the symbolic inputs in [[type-data-abi-boundary-pressure]], then—after explicit experiment authorization—convert its guest read and official wallet-owned File Browser write journeys into one disposable exact-Type fixture against the current Core/Files candidates." `mvp-and-acceptance.md` §Outcome: read path + "explicit promotion into a basic write-capable File Browser"; §Scope floor excludes "Arcade Play, native mounts, private folders, or a default Commons" (line 96).
4. Stage A → Stage B (`stage-a-report.md`: "Next: run disposable Stage B"; `Kanban.md` line 43). Stage A's `proposed-spine-edits.md` contradiction ledger #1: "Kickoff outputs 3-4 (specs AND complete cost table) vs README/constitution sequencing (Fable pass after prototyping/benchmarks) — SPINE finding 1, BLOCKING" → resolved by the PM as "Staged program: Stage A designs, B/C measure". The README still says step 4 comes after step 3.

Conflicts:
- README step 6 vs direction 2: README's parallel product slice is a *direct guest* reader plus Arcade; the owner says the first MVP is *write-capable* and the MVP doc excludes Arcade Play. `system-constitution.md` §Open questions still asks "Does the first Web Client ship only read-only Files plus one Arcade view, or also explicit writes?"; `owner-decision-inbox.md` V2-E6 still says "Then decide whether the first Web Client also needs writes". Owner answered; spine not updated. **DRIFT** (F1).
- 07-22 ruling vs direction 2: the 07-22 sequence says MVP after constitution + support matrix; the matrix was never written, yet the MVP was chosen. The 07-23 correction says James "may voluntarily answer an isolated choice; any such answer is recorded and its affected axes are reconciled" — so direction 2 legitimately wins, but no doc records that the 07-22 sequencing guidance is now satisfied or superseded. **UNDECIDED/owner** (F2).
- Write-capable MVP vs Core: `README.md` lines 86–89: "Complete listing and certified filesystem writes depend on the draft's generic `BindingScope` and executor/operation-bound consent experiments; neither is current B0." `mvp-and-acceptance.md` dependency table: complete directory listing = "**Proposed dependency**"; certified Files CAS = "**Proposed, not MVP-frozen**". So the owner-directed MVP depends on two Core/Files mechanisms that neither the candidate nor B0 defines. **MISSING/blocking** (F3).

Is there one agreed sequencing? **No.** The nearest to agreed is: Stage B bytes/prototypes (Core) in parallel with the web-client-os symbolic-input freeze + one disposable exact-Type fixture — but that is inferred (inference), not written in any one place, and the Kanban card that would carry it expired 2026-08-16.

---

## 7. Lane question 5 — does "chains don't die" (2026-07-10) still stand?

- Ledger: `owner-rulings.md` 2026-07-10 "ADOPTED (James): assume a blockchain persists indefinitely and stays queryable"; DROP dead-chain machinery; KEEP cross-chain replication (reach) and full-body spine (pruning insurance); "NET effect: … 'home-chain-authoritative + home chain always queryable'". Never superseded in the ledger; `Retirements.md` has no row for it.
- 2026-08-12 reset: "supersedes earlier mechanism-level rulings, not the problems they were solving"; "a fixed authority-home model … must re-earn inclusion"; "does not … revive a global home chain". The 07-10 ruling's stated payoff (definite reads at an always-live home chain) depended on the home chain that 08-12 removed. The ruling's *scope* (which chain is assumed immortal, when there is no home) is therefore unowned.
- Constitution: silent on chain persistence. Requires reconstruction "from the declared Realm state and byte carriers" (§Honest reads) and, for cross-Realm, "clients display source and destination authority/basis rather than inventing global current state" (§Architecture-level acceptance tests, Cross-Realm). Candidate: "`UNKNOWN` is reserved for … a missing required basis" (§Contract Resolution Plan). Neither says "assume persistence".
- Stage A (proposal-only): `pm-stage-a-directive.md` line 25 "Do not reopen broad dead-chain survival machinery. Define qualifying-Realm assumptions and honest behavior when a source basis is unavailable"; `chapters/b0-realm-admission.md` §4: "the 2026-07-10 chains-don't-die ruling … was scoped to a home-chain world; the intake RULINGS lane flagged its per-Realm scope as unowned … designed to work under either answer … The per-Realm scope of chains-don't-die is nevertheless a **candidate owner decision**, surfaced … only through proposed spine-edit A2"; `chapters/traceability.md` OR-1 "COVERED for the historical DROP/KEEP ruling; A2 remains unadopted".
- Evidence: `Reviews/2026-08-13-claude-evidence-round/README.md` §Realm, venue, and L1 evidence: "Polygon zkEVM: its sequencer stopped producing blocks when it was sunset on 2026-07-03. A read-only archive RPC still returned its frozen final block while its former explorer domain was gone"; "disappearing testnet/explorer/RPC paths"; "OP's unrecoverable legacy event-log loss"; §Cross-domain signal: "confirmed, then unreadable". §Held routing notes: "chain mortality assumptions … feed the existing EFS 2.0 Realm/Commons evidence gates V2-E5 and V2-E7." `CORRECTIONS.md`: "archived is not synonymous with dead"; "Ethereum history expiry … out-of-protocol archives".
- Where current docs carry either posture: **neither** — the current spine is silent. July docs carry both: dead-chain posture in `freeze-gates.md` B "Dead-chain fire drill", `efs-substrate-decision.md` §7d, `efs-v2-transition-plan.md` §2 note; home-chain posture in `ops-doctrine.md` amendments 3 ("MUST-pull-home-chain for safety-class gate reads") and 7 ("home-chain certain"), `joined-pass-synthesis.md` T1, `multichain-dependency-map.md` §1–2.

Verdict: the ruling stands *as a ledger entry* but is orphaned — its premise is gone, the evidence contradicts its literal form for L2/L3s and testnets, and the only workable reframing (per-Realm qualifying assumption + `UNAVAILABLE_SOURCE_BASIS`) is an unadopted Stage A proposal. **UNDECIDED/owner** (F5). This matters for the MVP because the direct-guest client must state what it assumes about the Realm it opens (V2-E5) and Sepolia is a testnet with documented read-path mortality (`CORRECTIONS.md` rows on dead testnets).

---

## 8. Lane question 6 — constitution requirements no current doc designs; MVP cuts

Requirements in `system-constitution.md` with no current design (current = README-listed docs; Stage A is proposal-only and unintegrated):

| Constitution requirement (§, line) | Nearest current design | Status |
|---|---|---|
| "qualifying EVM Realm" criteria (lines 38, 60, 303, 353) and Realm descriptor for a fresh L3 (§Open questions) | candidate `RealmId/RealmRevision` only; Stage A QR-1..8 proposal | **undefined** — MVP-relevant (F8) |
| Complete, hard-bounded enumeration incl. complete directory listing (§Honest reads lines 205–209; Files/mount traces lines 316–317) | `hierarchical-files` `BindingScope` experiment arm; "neither is current B0" | **no Core primitive** — MVP-blocking (F3) |
| Revocation-aware current counts; deterministic best-locator selection (§On-chain graph lines 180–184) | none in candidate; Stage A b0-indexes §6/§7 | **missing** (F6) |
| Revocation as a Realm-qualified state (lines 107, 156) | candidate Withdrawal only | **vocabulary/primitive gap** (F10) |
| Contract Lens outcomes FOUND/ABSENT/conflict/unsupported/UNKNOWN and 1/8/32/64-Principal plans (§Lenses; trace "Contract Lens") | candidate `ResolutionPlan` skeleton; V2-E2 | partial (F9); direction 8 sets 64 as target |
| Equivocation non-guarantee / challenge-window (ruled 2026-07-15 item F) | none | **missing from constitution** (F7) |
| Bounded, version-identified Realm validator whose basis is in the receipt (trace "Type and admission validation") | candidate: "Optional external validators are ordinary evidence or explicitly bounded, revisioned Realm modules" | thin; V2-E8 |
| Withdrawal/tombstone/no-resurrection state machine (§One transaction) | candidate open question | open |
| Page-result ABI (Realm, policy/code basis, high-water mark, cursor, coverage, status) | candidate open question | open |
| Encrypted-body profile with separated key roles, versioned AEAD, transplant resistance (§Privacy bullet 4) | none current; Stage A G-3 GAP; `privacy-pass-synthesis` is July evidence | **missing** — not MVP |
| EAP fixture "backed by a durable EAP brief before freeze" (trace "EAP proposed fixture") | no EAP brief exists in the vault | **missing artifact** — not MVP (F16) |
| Git multi-ref "one typed transaction Record or an explicit bounded profile rule" (§One transaction; trace "Git/Markdown") | `Reviews/2026-08-07-efs-git-deep-dive.md` (evidence); Kanban Git/forge card expired 2026-08-17 | no design — not MVP |
| Linux/macOS/Windows read-only adapters (§Filesystem; trace "Three-host mount") | `mountable-filesystem-semantics.md` (July, "adopted outcome"); `hierarchical-files` shared resolver | adapters undesigned — not MVP |
| Cross-Realm copy with "destination receipt or Recognition Record" (candidate §Occurrence) | "Recognition Record" named once, defined nowhere | undefined — not MVP |
| Sensitivity policy layer (ruled 2026-07-10) | `web-client-os/privacy-and-agents.md` (not opened in this lane) | out of lane |

What I would cut from Core for a first MVP (proposal, not adoption):
1. Managed Principals / KEL / recovery — already deferred; keep only the additive seam (`AccountPrincipal/1` + domain reservation). But see F11: the client direction may need more than `AccountPrincipal/1`; settle that first.
2. Type Variant B machinery (`QueryProfileId`, `ViewRevisionId`, `ViewQueryProfileId`) — ship the MVP on exact-Type Variant A with explicit versioning; keep Variant B as the bakeoff arm (direction 12 says the axis is open; `web-client-os` calls exact-Type-first "a reversible adapter recommendation").
3. Contract Lens beyond a small Plan (1–8 Principals) — measure 64 later (direction 8: "if measurement supports it").
4. Portable-Occurrence + `AdmissionIntent` cross-Realm profile and "Recognition Record" — single-Realm MVP; keep the bakeoff on paper.
5. On-chain deterministic best-locator selection — client-side selection over `Locator/1` + durability-evidence Records for the MVP.
6. Revocation-aware live counts — **cannot be cut silently**: 2026-07-15 item E says "PAY for it" and the constitution says a failed budget must be "returned to James". Needs an explicit owner ask.
7. Compound-key `IndexSpec`, `ObjectGenesis` for Topics/ownerless literals, EAS adapter, three-host mounts, encrypted-body profile (reserve domain separation only), Git/EAP/Nanda beyond fixtures, Commons venue matrix (V2-E7 — not needed for Core or the direct client per 08-12), the Arcade view in the first slice (F15).

---

## 9. Neighbour assumptions (what this set assumes, and whether the neighbour agrees)

| This set assumes | About | Where | Neighbour says | Agrees? |
|---|---|---|---|---|
| The client MVP is a direct guest reader + one Arcade view; writes undecided | web-client-os / arcade | `README.md` §Build order 6; `owner-decision-inbox.md` V2-E6; `system-constitution.md` §Open questions last bullet | `web-client-os/README.md` direction 2: write-capable File Browser is the first MVP; `mvp-and-acceptance.md` line 96 excludes Arcade Play; `Open-Decisions.md`: arcade HELD | **No** |
| Files can be built on `PositionKey`/`BindingKey`/`ResolutionPlan` | Files | `core-architecture-candidate.md` §Binding, §Contract Resolution Plan | `hierarchical-files-and-folders.md` lines 395–398, 622–623, 504–506 uses them; adds `BindingScope`, `FilesRouter` as experiment arms | Yes, plus extras Core lacks |
| `layered-type-system-and-data-abi` is "the current Type-system proposal" using `TypeSchema` | Type system | `README.md` §Current technical candidate + §Evidence map | That doc's identity is `TypeRevisionId` with `RecordId = H(DOM_RECORD, TypeRevisionId, H(canonicalBody))` (lines 200, 344, 380) | **No** (naming/formula) |
| Sepolia is the first development Commons, ratified by James | Realm/venue, owner | `README.md` §Current status, §The current shape | `web-client-os/README.md` direction 10 (2026-08-14→23) says the same; `owner-rulings.md` has no Sepolia entry; `system-constitution.md` never mentions Sepolia; evidence round: Sepolia permissioning "unresolved" | Yes in product README; **not in the ledger** |
| The uniform `PrincipalId` surface is open (V2-E1) | Core/Principal | `owner-decision-inbox.md` V2-E1; `system-constitution.md` §Authorship | web-client-os direction 7: "The client uses one uniform `PrincipalId` surface" with a mutable default controller account and multiple controller keys; `mvp-and-acceptance.md` table: "Owner direction; exact Core mechanism and default-account storage open" | Partially — client decided, Core mechanism open, mapping unwritten |
| 64-Principal Lens is one of four measurement sizes | Lens | `system-constitution.md` trace "Contract Lens" 1/8/32/64; V2-E2 | direction 8: 64 is *the* target "if measurement supports it"; keys don't consume entries | Compatible; not recorded in ledger/inbox |
| Three-host read-only mount outcome is adopted | mounts | `system-constitution.md` §Filesystem; `README.md` evidence map row | `owner-rulings.md` 2026-07-22 ADOPTED | Yes |
| An EAP brief will exist before freeze | EAP (no owning set) | `system-constitution.md` line 312 | no file anywhere; PM directive "provisional" | **No** |
| Git/Markdown trace needs no Git Core primitive | git-forge | `system-constitution.md` trace "Git/Markdown"; `owner-rulings.md` 2026-08-07 | `Reviews/2026-08-07-efs-git-deep-dive.md` (evidence only); Kanban prototype card expired 2026-08-17; no design folder | Unknown — untested |
| The 2026-08-13 venue evidence selects no venue and feeds V2-E5/E7 | Realm/venue | `README.md` evidence map row | evidence README §Held routing notes agrees | Yes |
| New repos will be `core`, `os`, `drive` | repos / owner | `hierarchical-files-and-folders.md` header "Proposed new repos: core, os, drive"; `docs/superpowers/plans/2026-08-14-efs2-core-files-foundation.md` "provisionally named `core/`" | direction 11: "rename legacy repos to `*-v1` and reclaim `contracts`, `sdk`, `webclient`, and `drive`" | **No** (F18) |
| SDK pending/outbox truth boundary exists somewhere | sdk | Stage A G-5 routes to "SDK result-model lane" | root `Designs/sdk-*.md` are v1 (brief: all sibling repos are v1) | Unknown — no v2 SDK design |

---

## 10. Decided / undecided / disagreeing docs

**Decided (ruling location → docs that still disagree):**
- Greenfield successor, no v1 compatibility/migration/coexistence/legacy read — `Decisions.md` 2026-08-08; `owner-rulings.md` 2026-08-12 → `efs-v2-transition-plan.md` Phase 5, header "Supersedes … v1 UID set" (no banner).
- Core / optional Commons / clients boundary; no Commons chain; contract Lenses required; mandatory automatic indexing per admitted Type/index profile; direct guest File Browser required — `owner-rulings.md` 2026-08-12 → none disagree.
- Three-host read-only mount required; research before MVP; contraction gate later with support matrix — `owner-rulings.md` 2026-07-22 → MVP chosen without a matrix (`web-client-os/README.md` direction 2; `mvp-and-acceptance.md`).
- Chains don't die; KEL design it; public-by-default + sensitivity layer; storage tiering — `owner-rulings.md` 2026-07-10 → constitution silent on chain persistence; KEL mechanism reopened 08-12 (consistent).
- 18-item on-chain list: A backlinks, B reverse membership, C best-mirror, E revocation-aware counts "PAY for it", F equivocation limitation "SIGN", 12 mandatory indexing, 13 content-hash lookup, 16 file bytes off-chain, 17 full-body spine, 18 no elision — `owner-rulings.md` 2026-07-15 → constitution carries A/B/13/17/18 generically (§On-chain graph, §Honest reads); **C, E, F have no current design** (F6, F7).
- Git/GitHub-class collaboration stays expressible — `owner-rulings.md` 2026-08-07 → constitution trace Git/Markdown; no design.
- First MVP = write-capable File Browser; uniform `PrincipalId` client surface; 64-entry Lens target; Sepolia dev Commons; repo reclaim names; Type axis open — `Designs/web-client-os/README.md` directions 2, 7, 8, 10, 11, 12 → `system-constitution.md` open question (writes), `owner-decision-inbox.md` V2-E6, `README.md` build order 6, `hierarchical-files` repo names.

**Undecided (suggested owner):**
- One agreed sequencing to MVP; disposition of the 07-22 support-matrix step — owner.
- "qualifying EVM Realm" criteria and Realm descriptor — efsv2 (V2-E5), owner only if a fork remains.
- Per-Realm scope of "chains don't die" — owner (Stage A A2 is the packet).
- Type identity variant and one vocabulary (`TypeSchema` vs `TypeRevisionId`; `RecordId` preimage) — efsv2 (V2-E4/E8), owner for the 50-year question.
- Whether directions 7/8 are satisfiable by `AccountPrincipal/1` (smart-account) or require managed Principals in the MVP — efsv2 (V2-E1) + owner.
- Core primitive for complete directory enumeration (`BindingScope` or smaller declared index) — efsv2 + web-client-os jointly.
- Revocation semantics on Occurrences; whether counts/best-locator stay in the MVP budget — efsv2 (V2-E4), owner if budget fails.
- Lens outcome enumeration (five outcomes vs UNKNOWN+cause) — efsv2 (V2-E2).
- Whether the 2026-07-02/07-07 outcome rulings (permanent archive, everyone pays, permissionless byte pool, no free tier) still bind EFS 2.0 — owner; they are in `efs-substrate-decision.md` §5 and `confidence-and-open-decisions.md` §Storage/uploads, not in the ledger.
- Commons venue criteria matrix — efsv2 (V2-E7); not needed for MVP.

---

## 11. Candidate findings (F1–F18)

**F1 — DRIFT (owner/vault-process; important; MVP-relevant).** Owner directions 1–28 (2026-08-14→23) exist only in `Designs/web-client-os/README.md` §Direct owner direction; `Designs/efsv2/owner-rulings.md` (the append-only ledger that the 2026-07-16 META ruling made "ONE canonical place") ends at 2026-08-12. Consequences in this lane: `system-constitution.md` §Open questions still asks whether the first Web Client ships writes; `owner-decision-inbox.md` V2-E6 says "Then decide whether the first Web Client also needs writes"; `README.md` §Build order 6 describes a read-oriented slice; V2-E1 does not record direction 7. `web-client-os/README.md` §Authority map "Upstream synchronization note (2026-08-14)" admits the spine "still present[s] the uniform Principal surface, MVP writes, and rich-name posture as open" and says "The EFS v2 PM has the exact reconciliation handoff" — I found no such handoff file (grep), and no spine commit after 2026-08-14.

**F2 — UNDECIDED (owner; important; MVP-relevant).** No single agreed sequencing. `README.md` §Build order (Fable pass after prototypes/benchmarks; product slice in parallel), `owner-rulings.md` 2026-07-22 (research → constitution + explicit four-tier support matrix → then choose MVP), `web-client-os/README.md` §Current work sequence 3 (freeze symbolic inputs → one disposable fixture), and Stage A → Stage B (`stage-a-report.md`; `Kanban.md` line 43) all differ. The support matrix required by 07-22 was never written (only Stage A `proposed-spine-edits.md` C9 "fold into output 7 or retire — flag for PM"), yet the MVP was chosen by direction 2. Stage A contradiction ledger #1 called the README/kickoff sequencing conflict BLOCKING; the PM resolved it as a staged program; the README is unchanged.

**F3 — MISSING (efsv2, jointly web-client-os; blocking; MVP-relevant).** The owner-directed write-capable File Browser MVP depends on Core/Files capabilities no current Core doc defines. `README.md` lines 86–89: "Complete listing and certified filesystem writes depend on the draft's generic `BindingScope` and executor/operation-bound consent experiments; neither is current B0." `mvp-and-acceptance.md` dependency table: complete directory listing "**Proposed dependency**"; certified CAS/preconditions "**Proposed, not MVP-frozen**". The constitution requires complete enumeration (lines 205–209, 316–317). `hierarchical-files-and-folders.md` §5 line 662–665 states that existing Type/backlink/Principal postings "cannot prove the complete" set. Nobody owns closing the gap between the candidate's `IndexSpec[]` and Files' `BindingScope`.

**F4 — WRONG/DEFECT (efsv2; important; MVP-relevant).** Three vocabularies and two `RecordId` preimages among "current" docs. `README.md`: "`TypeSchema` is the current plain-language name; older files call similar concepts `TypeRevision`." `core-architecture-candidate.md` §Record: `RecordId = H(domain, typeSchemaId, canonicalBody)`. `layered-type-system-and-data-abi.md` (README-designated "Current Type-system proposal", 2026-08-14) line 380: `RecordId = H(DOM_RECORD, TypeRevisionId, H(canonicalBody))` with `TypeRevisionId` derived from `SemanticSpecId + LogicalShapeId + RepresentationId` (lines 198–204, 344–348). Stage A B0 pins `TypeSchema`/Variant A. `Glossary.md` lines 240–242 defines `TypeSchemaId`. No document maps one to the other; the README's "older files" claim is false for the doc it promotes.

**F5 — UNDECIDED (owner; important; MVP-relevant via V2-E5).** The 2026-07-10 "chains don't die" ruling is orphaned. Its payoff ("home-chain-authoritative + home chain always queryable", `owner-rulings.md` 2026-07-10 NET effect) depended on the home chain removed by 2026-08-12 ("does not … revive a global home chain"). `system-constitution.md` and `core-architecture-candidate.md` never restate it; Stage A `chapters/b0-realm-admission.md` §4 reframes it as per-Realm QR-1..8 + `UNAVAILABLE_SOURCE_BASIS` and says the per-Realm scope "is nevertheless a candidate owner decision" (A2, unadopted); `Reviews/2026-08-13-claude-evidence-round/README.md` documents Polygon zkEVM sunset 2026-07-03 with frozen archive RPC and dead explorer, disappearing testnet read paths, and OP legacy log loss. July docs still carry both postures (`freeze-gates.md` B "Dead-chain fire drill"; `ops-doctrine.md` amendments 3, 7 "MUST-pull-home-chain"). `Retirements.md` has no row.

**F6 — MISSING (efsv2; important; MVP-relevant).** Constitution costed-gate promises with no current design: "revocation-aware current counts, and deterministic best-locator selection" (`system-constitution.md` §On-chain graph lines 180–184; rulings 2026-07-15 items C and E "PAY for it"). `core-architecture-candidate.md` has no count or ranking mechanism (grep: no "count", no "best"); its `IndexSpec[]` is scalar equality / reference equality / backlink / optional compound key. Only Stage A `chapters/b0-indexes.md` §6–§7 designs them, proposal-only and unintegrated. The constitution forbids silent deletion ("return the specific capability to James"), so an MVP cut requires an owner ask.

**F7 — DRIFT (efsv2; important; MVP-relevant).** The 2026-07-15 item F ruling ("RULED — SIGN THE LIMITATION … on-chain gates use closed, trusted author sets; EFS does not guarantee contracts can detect equivocation, and contracts needing certainty against untrusted authors must use a challenge-window", `owner-rulings.md`) is absent from `system-constitution.md` and `core-architecture-candidate.md`. `pm-stage-a-directive.md` line 24 says to preserve it; Stage A `proposed-spine-edits.md` A1 proposes restoring it; nothing applied. It is a non-guarantee statement, so the 08-12 "mechanism-level rulings superseded" clause does not remove it.

**F8 — UNDECIDED (efsv2; important; MVP-relevant).** "qualifying EVM Realm" is undefined. `system-constitution.md` uses it at lines 38, 60, 303 ("Fresh qualifying L3" trace), 353 (open question); `core-architecture-candidate.md` defines `RealmId/RealmRevision` but no qualification criteria. The direct-guest MVP must "open an explicit Realm/L3" (`owner-rulings.md` 2026-08-12); `mvp-and-acceptance.md` row "Explicit Realm and pinned read basis — Candidate; exact bootstrap/finality bytes open". Stage A QR-1..QR-8 is the only proposal.

**F9 — WRONG (efsv2; minor; MVP-relevant).** Lens outcome vocabulary differs: `system-constitution.md` §Lenses "explicit `FOUND`, proved `ABSENT`, conflict, unsupported, or `UNKNOWN`" vs `core-architecture-candidate.md` §Contract Resolution Plan "The result is typed and may be `UNKNOWN`" and "`UNKNOWN` is reserved for unsupported profiles, partial replicas or backfills…" (unsupported folded into UNKNOWN; conflict undefined). Stage A contradiction ledger #2 flagged it; A4 proposes one five-outcome enumeration; unapplied. `hierarchical-files` treats the Plan as a Record Type `ResolutionPlan/1`; the candidate never says whether a Plan is a Record.

**F10 — WRONG/UNDECIDED (efsv2; minor; MVP-relevant).** "Revocation" is a constitution term with no candidate primitive. `system-constitution.md` lines 107, 156, 181 treat revocation as Realm-qualified state and a counted quantity; `core-architecture-candidate.md` §Binding and withdrawal defines only Withdrawal ("its issuer no longer maintains it") and never maps revocation to Withdrawal, Binding tombstone, or anything else. The 2026-07-15 item E count semantics ("count drops when endorsements are revoked") cannot be specified without this mapping.

**F11 — UNDECIDED (owner/efsv2; important; MVP-relevant).** Owner directions 7 and 8 (`web-client-os/README.md`: one Principal, "mutable default/main controller account", "three controller keys", "Multiple controller keys do not consume multiple Lens positions; key authorization belongs inside Principal verification") describe a multi-controller Principal. `core-architecture-candidate.md` §Principal: MVP `AccountPrincipal/1 = {authorityKind, originIfRequired, accountOrKey}`; "Later managed Principals may add … multiple actors, delegation, rotation"; "contract-account authority is Realm-qualified". `system-constitution.md`: "A full custom KEL is not frozen into the MVP". Whether a smart-account `AccountPrincipal/1` satisfies 7/8 (ERC-1271 validating any controller key at admission) or managed Principals are needed is unwritten; `mvp-and-acceptance.md` row: "Owner direction; exact Core mechanism and default-account storage open"; Stage A OR-2 DEFERRED, G-2 GAP.

**F12 — DEFECT (efsv2/vault-process; important; not MVP-blocking).** Ten July docs in `Designs/efsv2/` are unclassified by `README.md` §Evidence map and carry `#status/draft`; six have no historical banner and read as normative: `efs-v2-transition-plan.md` (Phase 5 v1 re-attestation; "Supersedes … v1 UID set" — contra 2026-08-08), `joined-pass-synthesis.md` (line 17/73: inbox "now carries the … P-1…P-23 packet" — those are "Superseded questions" in the inbox), `multichain-dependency-map.md` (§Open questions: P-5/P-3 "are where … get decided"), `fable-next-pass-scope.md` ("Settled decisions every pass must respect: … first-attester-wins lenses, string-only properties"), `ops-doctrine.md` ("Adopted core (unamended)"), `os-pass-handoff.md` ("Reserved surface you can rely on: … WHITEOUT ships … per-chunk SHA-256 in EFSBytes"). Four more have banners that route readers to `assumptions-and-requirements`/`human-overview`, themselves bannered as evidence on 2026-08-12. `README.md` §Status "The two active docs are `#status/draft`" is false (seven current docs; 40+ drafts in the folder). `Retirements.md` cannot catch this (phrase-based; "v1 coexistence" is marked Cleared).

**F13 — DEFECT (efsv2/vault-process; important; MVP-relevant).** The spine has not absorbed Stage A. `README.md` never mentions the Stage A corpus (grep "stage": none) and still presents `fable-efs2-core-engineering-kickoff` as the "Focused next-pass prompt" although the pass ran 2026-08-13 (`stage-a-report.md`). Stage A produced 16 proposal-only spine edits (`corpus/proposed-spine-edits.md`), one contradiction marked BLOCKING (#1 sequencing) and six others; `system-constitution.md` and `core-architecture-candidate.md` have no commit after the 2026-08-13 import (`git log`). `Kanban.md` line 43: the Core hardening card "expires 2026-08-16". The vault audit scripts cannot see this because proposals are not retirements.

**F14 — DIRECTION/DEFECT (efsv2/owner; minor).** "Sepolia is the first development Commons" (`README.md` §Current status and §The current shape; added by commit `da5fcc3` 2026-08-14 from the web-client-os pass) is attributed to James's ratification but appears in no ruling ledger — only `web-client-os/README.md` direction 10. It also conflates a venue with the Commons layer: `system-constitution.md` §EFS Commons defines Commons as "an optional shared Realm and/or replaceable services" with "No home chain or operator … selected", and never mentions Sepolia. `CORRECTIONS.md` leaves Sepolia's validator permissioning "unresolved" and records testnet read-path/archive loss (Goerli, Ropsten, Rinkeby, Kovan omitted from the archive registry). Harmless for disposable dev work; misleading as a "ratified" status line.

**F15 — CUT/DRIFT (efsv2/arcade; minor; MVP-relevant).** The Arcade view in the first slice. `README.md` §Build order 6 "+ one-game Arcade slice" and `owner-decision-inbox.md` V2-E6 "plus one verified Arcade view" vs `mvp-and-acceptance.md` line 96 excluding "Arcade Play" from the MVP, `Open-Decisions.md` arcade HELD, and `Reviews/2026-08-13-claude-evidence-round/README.md` §Arcade falsification (zero benefits classified as uniquely EFS-specific for the tested catalog; `CORRECTIONS.md`: one assessment, feeds the held recut). Recommend dropping Arcade from the Core MVP slice and keeping it as a generic fixture (constitution trace "Arcade").

**F16 — MISSING/CUT (efsv2; minor; not MVP).** `system-constitution.md` line 312 requires the EAP fixture to be "backed by a durable EAP brief before freeze"; no EAP brief exists anywhere in the vault (`find -iname '*eap*'` empty; grep hits only the constitution); `pm-stage-a-directive.md` line 22 "Treat EAP as provisional until Codex supplies a durable brief." Likewise the constitution's encrypted-body profile (§Privacy, bullet 4) has no current design (Stage A G-3 GAP) and the Git multi-ref typed transaction Record (§One transaction) has none (Kanban Git card expired 2026-08-17). All three should be explicitly deferred out of the MVP rather than left as freeze obligations with no owner.

**F17 — DEFECT (efsv2; minor).** Dead/ambiguous wiki-links in lane docs: `[[For-James]]` in `efs-v2-holistic-redesign.md` §The honest justification (line 25) — no `For-James.md` exists (`Owner-Inbox.md` does; `Decisions.md` 2026-07-01 records the rename lineage); `[[identity]]` in `owner-rulings.md` 2026-07-16 (lines 89, 100) resolves to two files (`Designs/efsv2/identity.md` and `Reviews/2026-07-07-efsv2-corpus/identity.md`). All other wiki-links and relative paths I depend on resolve (checked list in §12).

**F18 — DRIFT (efsv2/owner; minor).** Repository naming: `hierarchical-files-and-folders.md` header "Proposed new repos: core, os, drive; contracts/client remain legacy evidence" and `docs/superpowers/plans/2026-08-14-efs2-core-files-foundation.md` ("provisionally named `core/`") vs `web-client-os/README.md` direction 11 (same day): "rename legacy repos to `*-v1` and reclaim `contracts`, `sdk`, `webclient`, and `drive`". Both say no creation is authorized; the names still disagree.

---

## 12. Defects and stale facts checklist

- `README.md` has no header block (no Status/Last touched line) unlike every other doc in the folder; its `#status/draft` appears only in prose.
- `README.md` §Status "The two active docs" — false (see F12).
- `README.md` calls `hierarchical-files-and-folders` "a draft experiment target"; that file is `#status/review` ("converged semantic proposal").
- `README.md` evidence-map relative links all resolve: `../efs15/`, `../arcade/README.md`, `../../Reviews/2026-08-07-efs-git-deep-dive.md`, `../../Reviews/2026-08-13-claude-evidence-round/README.md#realm-venue-and-l1-evidence` (heading exists at line 157 as "Realm, venue, and L1 evidence"), `../../Brainstorms/2026-07-29-pm-nanda-neutral-agent-infrastructure-pressure.md`.
- Wiki-links verified resolving: `Decisions`, `Open-Decisions`, `Retirements`, `kel`, `ethereum-first-efs-and-os`, `client-os-pressure-report`, `technology-foundation`, `type-data-abi-boundary-pressure`, `large-file-uploads`, `fs-pass-freeze-reservations`, `fs-pass-james-decisions`, `fs-pass-synthesis`, `read-lens-spec`, `codex-envelope/kinds/kernel`, `deterministic-ids`, `write-ux-options-ranked`, `solana`, `human-overview`, `assumptions-and-requirements`, `mountable-filesystem-semantics`, `onchain-completeness`, `onchain-graph-queries`, `lens-spec`, `lens-pass-synthesis`, `lens-read-gotchas`, `privacy-pass-synthesis`, `layered-type-system-and-data-abi`, `hierarchical-files-and-folders`, `Reviews/2026-07-19-base-native-aa-impact`, `2026-07-08-fable-fs-prep/verify-time-model`, `2026-07-01-v2-adversarial-review`, `2026-07-02-substrate-investigation`, all `Reviews/2026-07-25-joined-fs-pass-corpus/*` and `2026-07-07-efsv2-corpus/*` paths cited. Broken: `For-James` (F17). Ambiguous: `identity` (F17).
- The brief's path `Reviews/2026-08-13-efs2-stage-a-corpus/README.md` does not exist; the corpus index is `STATUS.md` (plus `stage-a-report.md`). Not a vault defect; note for the lead.
- `Open-Decisions.md` (generated 2026-08-21) matches the inbox: V2-E1…E8, F1, F2 waiting on evidence; Ask now 0. Consistent.
- `owner-rulings.md` 2026-07-16 says `kel-kickoff.md` was deleted — confirmed absent.
- `Kanban.md` In Flight: Git/forge card expired 2026-08-17; Grants 2026-07-30; Core hardening 2026-08-16 (matches brief).
- Confidence ledger `[verified-in-code]` claim and `efs-substrate-decision.md` §5/§6.3 rulings (2026-07-02) are attributed owner rulings not present in `owner-rulings.md` (ledger begins 2026-07-10). Not a contradiction, but the "one canonical place" promise is already broken backwards as well as forwards.

---

## 13. Solid now vs settle first vs cut

**Solid enough to build a disposable Stage B on:**
- The one-sentence model and layer boundary (`system-constitution.md` §The one-sentence model, §System layers; `owner-rulings.md` 2026-08-12).
- Record ≠ Occurrence ≠ Admission ≠ Binding ≠ Lens separation and falsifiers 1–14 (`core-architecture-candidate.md` §Candidate in one picture, §Falsifiers).
- `RecordId` independent of Envelope (falsifier 3); full-width `bytes32 PrincipalId` end to end; mandatory indexing per admitted Type/index profile; `UNKNOWN` ≠ absence; state-readable bodies/no elision; direct guest read path with no wallet/Commons/OS.
- Stage A B0 as a fixed comparison baseline (`STATUS.md`: "coherent enough to build and compare in a disposable Stage B").
- Files built on `PositionKey`/`BindingKey`/`ResolutionPlan` (`hierarchical-files-and-folders.md`), as an experiment.

**Settle first (before any MVP bytes):** F2 (one sequence), F3 (complete-enumeration primitive), F4 (one Type vocabulary + one `RecordId` preimage), F11 (Principal shape the client needs), F8 (what "qualifying Realm" means for the Realm the guest opens), F1/F13 (spine absorbs the owner directions and Stage A), F5 (dispose of "chains don't die" per Realm), F6/F7/F10 (revocation semantics, counts/best-locator budget or explicit owner cut, equivocation non-guarantee restored).

**Cut for the first MVP (proposal):** see §8 list — managed Principals/KEL, Type Variant B machinery, Lens >8 entries, cross-Realm Occurrence portability/`AdmissionIntent`/Recognition Record, on-chain best-locator ranking, compound indexes, `ObjectGenesis` for Topics/literals, EAS adapter, three-host mount adapters, encrypted-body profile, Git/EAP/Nanda beyond fixtures, Commons venue matrix, Arcade view.

**Unverifiable from here:** all "Last touched" dates before 2026-08-13; whether the "exact reconciliation handoff" the web-client-os README says the EFS v2 PM holds exists outside the vault; whether any Stage B prototype has started outside the vault (brief says no EFS 2.0 code exists in any repo); Sepolia's current validator permissioning; the truth of Stage A's own internal claims (151-row trace, 9-cell bakeoff) — I verified their existence and headings, not their content.
