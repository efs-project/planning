# EFS v2 — Deep Privacy Pass: critic addendum (repair round)

**Pass:** Deep Privacy Pass, 2026-07-11 — REPAIR ROUND consolidation.
**Role:** COMPLETENESS CRITIC, second sitting. This file is the BINDING addendum to [critic.md](critic.md); it integrates the three repair-round files (`os-private-tier.md`, `attack-layer1.md`, `attack-os-tier-2.md`). critic.md stands except where a ruling below explicitly amends it; the synthesis applies **critic.md + this addendum** mechanically. Nothing is renumbered.
**Status:** draft — binding. #status/draft #kind/review #topic/privacy #pass/deep-privacy

---

## 0. What the repair round changed, in one paragraph — and two self-corrections stated loudly

The two dead lanes are replaced and both replacements held up under attack. But the round's most important output is that **two of my round-1 rulings were wrong and are corrected here**: (1) **F-2 as I consolidated it is NOT freeze-safe** — attack-layer1 proved the child-derivation chain does not compose with F-1 (the salt-provenance rule is missing; a `K_node` holder cannot obtain `salt32_child`, so the frozen headline property "reveal K_node ⇒ unlock descendants" is false as specified). I marked F-2 CRITIC-VERIFIED in round 1; the verification missed the non-composition. F-2 is **PULLED from the ceremony** (ruling in §1.1, gated on JD-36). (2) **My GAP-3 repair sketch's arm (B)** ("deterministic FEK = f(oldFEK, membership-epoch)") **is cryptographically unsound standalone** — os-private-tier's anti-(B) derivation (a removed member holds all epoch-e state and the epoch counter is public, so any deterministic function of them is computable by the removed member) is correct, and I withdraw the arm as a freestanding option (kill-list #21). Everything else in the round is repair, tightening, or honest de-claiming: no new frozen bytes are demanded by any repair-round file, the launch (recoverable) tier is not blocked, and the shreddable tier is gated behind one genuinely new FATAL (the shred-ring concurrency hole, JD-31).

---

## 1. Dispositions — every FATAL / SERIOUS in the two new red teams

Vocabulary as in critic §1: **KILLS** · **REPAIRED-BY** · **OVERRULED**. Every entry re-derived, not carried.

### 1.1 From attack-layer1.md — and the FINAL F-batch rulings

First the findings, then the per-item ceremony verdict the charge demands.

| # | Finding | Severity | Re-derivation | Disposition |
|---|---|---|---|---|
| F-2 non-composition | F-1 treats `salt32` as a free input (revealed only in that node's own disclosure record); F-2's `K_child = HKDF(K_node, "efs/v2/salt/child" ‖ blindedName_child)` requires `blindedName_child`, which requires `salt32_child`; a `K_node` holder has no way to obtain a random-and-stored `salt32_child` ⇒ the frozen claim "reveal K_node ⇒ unlock exactly the descendant set" is **false** unless a third, unpinned salt-provenance rule exists. | NOT-FREEZE-SAFE (the round's one freeze defect) | **Reproduced in full; the trace holds.** I add one result the red team understated: the "now-or-never" pressure on F-2b is itself overstated. Because F-1 pins salt as a *free input* and D3 already permits deterministic HKDF salts, a post-freeze convention can define `salt32_child = HKDF(K_node, …)` for trees built under it — subtree bulk-unlock remains **addable later for new trees** with zero frozen change. Only retroactive bulk-unlock of pre-convention trees is forfeited, which is acceptable. So Option B loses nothing permanent. | **KILLS critic §3.1 F-2 as written. REPAIRED-BY pulling F-2 to Durable** (Option B): the frozen family text carries the *negative* sentence (§3 delta below) so it never promises the property; F-2b is recorded as the optional post-freeze convention shape; encrypted dirnodes (JD-6) carry subtree sharing via child caps, which also dodges Option A's rename-re-anchors-the-subtree cost. → **JD-36** (James may still choose Option A/F-2b pinned at the ceremony; recommend Option B). |
| Q1 X-Wing anonymity asymmetry | X-Wing IND-CCA is a robust combiner (secure if **either** half holds) but its anonymity/key-privacy is a weak combiner (needs **both** halves; eprint 2026/396) ⇒ recipient-unlinkability of every wrap is classical-only and retroactively falls at CRQC, while content stays secret; scan keys are deliberately classical, so the CRQC retro-clustering covers the ordinary keyWrap tier, not just stealth. | SERIOUS (honesty) | Combiner-direction logic reproduced and internally consistent (anonymity of a concatenated hybrid is broken by distinguishing either component's ciphertext contribution; confidentiality only needs one surviving KEM). The 2026/396 citation itself is **carried, not re-fetched** — but the repair is safe in both directions: if the citation later weakens, the caveat is merely conservative; the dishonest direction (implying PQ recipient-privacy) is what must not ship. | **REPAIRED-BY** (a) the honesty line as a positioning + row-doc MUST (§3.2 addition; §7 markup #4 on critic's positioning verdict); (b) the R5-zk classical qualifier (exact text §1.1a below); (c) broadening **JD-9**: the CRQC blast-radius pricing input now covers the ordinary recipient graph. Kill-list #19. |
| F-1 under-specification | Four missing pins: UTF-8-of-NFC byte encoding; whether the E4 path-segment reject-set + `MAX_NAME_BYTES` applies pre-blinding; `DOMAIN_NAME_BLIND_V1` fixed-width + provable distinctness across all keccak domains; ID-SHAPE-1 inheritance. | Freeze-relevant under-spec | All four reproduced. (ii) is the sharpest: without it a salted anchor can commit to a name that is illegal as a plaintext path and cannot round-trip on disclosure/promotion — a real fork/promotion bug, not pedantry. | **REPAIRED-BY** amending F-1 with the four pins (exact text §1.1a). Confusable/bidi display duty on verifying clients noted as convention. |
| F-4 over-freezing | The exact `HMAC(scanRoot,fileId)` construction has no cross-author interop need (only same-user-multi-device, which an SDK version gate covers) ⇒ freeze the PROPERTY, keep the formula Durable. Plus: `scanRoot` is a long-lived, not-freely-rotatable secret (rotation breaks re-derivation of all prior self-escrow keys, mailbox keys, view-tag matches). | Over-claim + design note | Correct on both. The G9 check from round 1 (occurrence key = slot coordinate, not wrap target; encryption-branch derivation) still holds; nothing about pulling the formula to Durable weakens it. | **REPAIRED-BY** amending the F-4 row text to property-only (exact text §1.1a); the formula moves to §3.2 convention. The `scanRoot` blast-radius/generation-list note is routed to the OS/recovery lane (rides JD-11's recovery ladder docs). |
| F-5 governance mis-class | "Never mint a classical-only KEM tag" is registry governance, not a freeze-enforced invariant (the registry is Durable by design); the operative fence is the conforming-client refuse rule. Malicious-app blob downgrade = session compromise, out of the frozen-surface question; no negotiated-suite downgrade exists (wraps are per-recipient independent). | Honest de-claim | Reproduced. My round-1 row text implied the frozen surface prevents a classical tag; it cannot. Substance unchanged — F-5 remains the linchpin, and Q1 strengthens it (the open registry is what admits a future PQ-anonymous KEM). | **REPAIRED-BY** splitting F-5: grammar open-endedness stays ROW-text; the no-classical-tag discipline + refuse rule move to §3.2 convention (exact text §1.1a). |
| Q4 hardware-wallet gap | G9 forbids deriving enc/scan roots from the signing key; HW wallets do no X25519/ML-KEM/HKDF-with-secrets ⇒ a HW-wallet-only user's private tier is protected only by their software keystore. The HW wallet secures authorship, not confidentiality. | SERIOUS (honesty) | Reproduced; it is architectural (independent of EIP-5630's status), forced by the coupling rule, and unfixable — the exact analogue of identity.md's smart-wallet authorship exclusion, deserving the same loud statement. | **REPAIRED-BY disclosure**: → **JD-38** (messaging decision). Kill-list #20 (never imply HW wallets protect the private tier). |
| NOTE cluster | §8.1 "∧" notation inverts the confidentiality guarantee (must read: confidentiality = OR of halves, anonymity = AND); Padmé dirnode bucket-crossing broadcasts "folder just grew past ~N entries" (mitigate: fixed-size padding for sensitive dirnodes); VAL 8 KiB bucket must account for AEAD/prologue overhead under `MAX_VALUE_BYTES`; H2 counter must roam in P9 state (or re-derive by self-scan) and large counter gaps are a widen-window perf cliff, not a strand; dekCommit = wrap↔content binding, EtM-HMAC = content equivocation — two mechanisms, don't conflate in docs. | NOTE | Spot-checked; all correct. | **REPAIRED-BY** doc/convention amendments (§3.2). None freeze-bound. |

Also adopted from attack-layer1 as **confirmations** (no action): the committing-AEAD recipe survives all three transplant/replay/equivocation games (bless unchanged — the strongest-specified part of Layer 1); view tags k=16 have no birthday/intersection leak (fresh per-wrap ephemerals ⇒ tags don't cluster; FP costs the scanner one failed decap, leaks nothing); no owner-escrow fingerprint (self-escrow wraps structurally indistinguishable from recipient wraps); G9-to-root-secrets sound; no downgrade-negotiation surface in per-recipient wraps.

#### 1.1a THE F-BATCH — final ceremony rulings (the charge's special-attention item)

| Item | FINAL RULING | Exact text / delta |
|---|---|---|
| **F-1** | **AMENDED → goes to ceremony** (RED-TEAMED) | Pin `blindedName = keccak256(DOMAIN_NAME_BLIND_V1 ‖ salt32 ‖ utf8(nfc(name)))` + constant + golden vectors, in the D3/D4 batch, **with four pins**: (i) the name bytes are the **UTF-8 encoding of the NFC form**, stated; (ii) `name` MUST validate under the **E4 path-segment reject-set + `MAX_NAME_BYTES = 255` before blinding** (the salted-family NFC-validation variant is E4's full set; boundary cases in the vectors) — a salted anchor may never commit to a name illegal as a plaintext path; (iii) `DOMAIN_NAME_BLIND_V1` is **fixed-width**, and the golden-vector suite asserts **pairwise distinctness across all keccak domain constants** (`DOMAIN_CLAIM_V1`, `DOMAIN_LIST_V1`, `DOMAIN_ANCHOR_SALTED`, the `efs.id.tagdef.v1` family, the propertyId domain); (iv) salted tagIds inherit **ID-SHAPE-1** (digest-shaped, never address-shaped), stated in the family text. `salt32` is an author-chosen 32-byte free input (random, or deterministically derived per D3's HKDF-salts permission); **salt provenance is never verified on-chain**. |
| **F-2** | **PULLED — stays Durable/unfrozen** (subject to JD-36) | Remove the `K_child` pin from the ceremony batch. The frozen family text instead carries: *"Per-segment and subtree key derivation is capability-holder convention (Durable). The family does not define — and holders MUST NOT assume — that possession of a node's key or salt yields any descendant's or sibling's key or salt; cross-client partial-disclosure interop is guaranteed only for single-node disclosures (D4 + F-1)."* Record **F-2b** in the conventions registry as the optional, post-freeze-addable subtree-unlock shape for trees that opt in: `salt32_child = HKDF-SHA-256(K_node, "efs/v2/salt/anchor" ‖ utf8(nfc(name_child)))[0:32]`, with the rename-re-anchors-the-subtree caveat and the names-you-know (not blind-enumeration) limit stated. If James chooses Option A instead (JD-36), F-2 + F-2b freeze together with vectors — never F-2 alone. |
| **F-3** | **FREEZE-SAFE — unchanged** (upgraded CRITIC-VERIFIED → RED-TEAMED) | As in critic §3.1. The red team confirmed the wording catches the A1 oracle (`H(recipientEncKeyId)` is linkable *by non-holders*) while admitting every structured-but-opaque key the tier needs. |
| **F-4** | **AMENDED → goes to ceremony as PROPERTY-only** | Frozen E5 text: *"The reserved self-occurrence-key is opaque, owner-derived from the owner's encryption/scan key branch (never from the identity signing key or its signatures), deterministically re-derivable after total device loss, and never a public constant. The concrete derivation is SDK convention (Durable), version-gated."* Drop the `PRF(scanRoot, fileId)` example from frozen text (or mark explicitly non-normative). The concrete construction `occ_self = HMAC-SHA-256(scanRoot, "efs/v2/self-escrow" ‖ fileId)` moves to §3.2 convention. |
| **F-5** | **AMENDED → goes to ceremony, minus the governance clause** | Frozen C3 text: *"The VAL blob is a typed multi-key structure — entries {algoTag, role, keyBytes, …} with roles ⊇ {kem, scan}; the entry grammar and the KEM algoTag registry are open-ended (Durable, post-freeze-extensible)."* The rest is convention (§3.2): launch registry `0x01 = xwing-v1`; `0x02/0x03` reserved names; **no classical-only KEM tag is ever registered (governance discipline), and conforming clients MUST refuse to create private-tier wraps under any classical-only tag** (the operative fence). The ceremony sheet must not imply the frozen surface prevents a future classical tag. |
| **R5-zk** | **AMENDED → goes to ceremony with the qualifier** | *"Registry KEM/AEAD suites for private-tier wraps MUST be key-private (anonymity-preserving; no key-id hints in wrap blobs). Classical key-privacy is the launch floor; post-quantum-anonymous suites are a Durable registry upgrade — no launch-registry suite provides post-quantum recipient unlinkability."* |

**Net ceremony surface after this addendum:** F-1 (four-pinned) · F-3 · F-4 (property) · F-5 (grammar) · R5-zk (qualified) · optionally the stealth genesis announce line with epoch field (JD-8, still open) · optionally F-2+F-2b together (only if JD-36 = Option A). **F-2 as quoted in critic §3.1 must not be cut.**

### 1.2 From attack-os-tier-2.md

| # | Finding | Severity | Re-derivation | Disposition |
|---|---|---|---|---|
| §6 shred-ring concurrency | The shred keyring is a cardinality-1 rotation with fresh, deliberately non-re-derivable epoch keys (`K_ring_{e+1}` CSPRNG; a persistent-root derivation would let any device re-derive the destroyed key and defeat shred). Concurrent shreds from two devices fork the ring: LWW keeps one version; the loser device erased `K_ring_e`, holds only its own losing `K_ring_{e+1}`, and has **no path** to the winner's key (shred keys never touch chain) ⇒ device lockout; and the winning table still contains the loser's shred target ⇒ **silently ineffective shred**. R-GAP3.2's deterministic-convergence escape hatch is structurally unavailable (convergence needs pre-shared entropy; a destroyable key must not be re-derivable). | **FATAL (shreddable tier)** | Reproduced end-to-end, including the irreconcilability argument — this is the anti-(B) logic correctly biting the design's own new machinery. Scope check confirmed: the recoverable tier never touches the ring; G-2 already gates the shreddable tier separately from launch. | **KILLS "shreddable tier ships as specified." REPAIRED-BY JD-31**: the ring gets an explicit single-writer serialization discipline (designated shred-committer device, or checkpoint-serialized shred with read-your-latest-ring-epoch refusal of stale rotations), stated as loudly as R-GAP3.1, PLUS the design must state *why* no deterministic-convergence discipline can exist for it, PLUS the concurrent-shred fixture (one live epoch; every enrolled device derives it; every intended shred effective across ALL decryptable ring versions; no lockout). Launch (recoverable tier) is NOT blocked. Interim option: shreddable ships single-device-only. |
| §4 inbound-share recovery | Inbound wraps are sealed to per-device KEM keys ("per-device random, never derived from rootSecret" — layer1 §4.2); the share-ack/self-index store the wrap *recordId*, not a phrase-openable DEK; after total device loss the phrase locates the wraps but cannot open them ⇒ WA-2's "accepted shares re-open via the recorded wrap refs" is **false**, and the §0.2 recoverable-tier promise overstates. | SERIOUS | Reproduced against layer1 §4.2 + W1.3 + §2.5. `recoveryKemSeed` exists but W1.3 does not target it for inbound shares and no text publishes its pubkey as a blob entry. | **REPAIRED-BY JD-32**: (a) publish `recoveryKemSeed`'s pubkey as a standing `kem` entry in the blob (every inbound wrap then also hits the phrase-derivable key; +1 wrap/share; the standing entry is indistinguishable from another device key, so no fingerprint) — recommended; or (b) SDK re-wraps the accepted DEK to the recovery key inside the share-ack. **The walkaway gate (JD-12) MUST NOT assert WA-2 inbound-share recovery until one lands.** Kill-list #23. |
| §3 Schedules A/B (R-GAP3 residue) | (A) Concurrent *different* removals: `teamKeypair_{e+1} = HKDF(teamSeed_T, LE64(e+1))` is roster-independent ⇒ two devices derive the SAME e+1 keypair but wrap it to different remnant rosters; the union of e+1 recipients includes **both** removal targets ⇒ the epoch excluded no one. (B) One curator self-racing two removals: both target pointer version e+1; roster merge is undefined (W2.3's OR-set covers child entries only); add-wins-union re-admits, naive LWW silently drops a removal. Severity of (A) conditional on the custody model (W4.1 vs R-GAP3.2 inconsistency, §5 C-L below). | SERIOUS | Reproduced; the crux (roster-independent epoch derivation — the very thing that makes same-removal convergence work — guarantees the worst outcome under different-removals) is correct and elegant-nasty. | **REPAIRED-BY JD-33**: rotation (team epoch AND shred ring) is **read-your-latest-epoch serialized** — a rotation prepared from a stale epoch is refused and rebased (the second removal composes as e+2, never a colliding e+1); roster state is **LWW-by-rotation-order with monotone removes** (a remove at epoch e+k is never undone by a concurrent stale roster; NEVER OR-set-union for rosters); the §5.4 fixture adds the different-removal and self-race cases + the roster assertion. R-GAP3 itself stands (its same-removal convergence and anti-(B) derivation are confirmed). Kill-list #24 (roster OR-set-union). |
| §7 live-session eviction | Removal doesn't evict a member from an in-progress collab session: Eve holds `K_sess`; §5.6 is silent on mid-session re-key; a checkpoint written mid-rotation by a saver who hasn't seen the rotation wraps to epoch e, which Eve reads — brand-new content leaking post-removal. | SERIOUS | Reproduced from §5.6's session model. This is lazy re-key's collab analogue and currently undocumented. | **REPAIRED-BY JD-34**: on epoch rotation, live sessions MUST re-announce `K_sess` under e+1 (best-effort eviction — the relay is untrusted); checkpoints MUST wrap to the latest epoch the saver can confirm; the removal ceremony states the residual ("a removed member retains live-session access until the current session ends") — never let "removed" imply an eviction the design can't guarantee. |
| §5 multi-writer dirnode invisibility | W5.3 reads roster members' pointer *slots* (winners only); a member's own device-race leaves the losing pointer version visible only in that member's spine; other readers never see its entries until that member next syncs — invisibility window, permanent strand if they never do. Also "recent slot history" is wrong as a bound: completeness requires ALL causally-concurrent losing versions, not a recency window. | SERIOUS | Reproduced; it is a genuine W2.3-vs-W5.3 internal contradiction (adjudicated as C-M, §5). | **REPAIRED-BY** the C-M ruling: causally-complete read is normative for team folders — resolution reads each roster member's pointer slot AND that member's causally-concurrent superseded pointer versions (via RP-2 spine filtering; cost honestly O(N members × pointer history since the last stable frontier), maintained incrementally by cursor); "recent history" is replaced by a **causal-frontier bound** (a version may be dropped from the merge only when it is in the ancestry of every live head). Refines **JD-26** (changed row, §4). The `priorClaimId chain` nomenclature is restated against the real read ABI (spine filter by (definitionId, subject)) — no such frozen structure exists. |
| §8 class-(a) secret sync | The §5.6 relay is stateless and needs both devices co-online ⇒ class-(a) secrets have no path between never-co-online devices; single-device users have zero backup; "re-issuable at the provider" understates full re-auth friction. | SERIOUS | Confirmed from §5.6's own statelessness spec. | **REPAIRED-BY JD-35(i)**: the platform-keychain vendor-escrow path (named per item, "vendor-escrowed") becomes the *default recommendation* for re-auth-expensive tokens; the co-online requirement for mesh sync is stated. |
| §6 corollary: shared ≠ shreddable | Team epoch keys derive from the curator's persistent `archiveRoot` ⇒ always re-derivable ⇒ shared/team folders can never be in the shreddable tier; the §0.2/§3.2 tables imply a per-file choice without this constraint. | SERIOUS (honesty) | Confirmed — this is JD-1's tier split meeting §5.1's curator-rooted derivation; the constraint is structural. | **REPAIRED-BY JD-35(ii)**: tier tables state shared/team content is recoverable-only and a removed member's access to cold shared files is forever forward-only-lazy, never shreddable. **Amends JD-1** (changed row, §4). Kill-list #22. |
| NOTE cluster | Sponsored-flush relocates funding-provenance linkage to the sponsor (per-persona sponsors or a named non-logging trust assumption — refines JD-20); enrollment SAS must commit to the FULL offer incl. kem pk (G-6); publish partial-failure warns before the FIRST envelope; Shamir honesty (any k friends can silently reconstruct off-chain — the threshold IS the trust assumption; shares are permanent on-chain reconstruction targets, HNDL-bounded-by-hybrid); relay sees the collaborating clique at the network layer (state as loudly as the on-chain clique leak); forged intra-session deltas mean the saver signs smuggled content (spell out); curator phrase = skeleton key to all team epochs forever (state at team creation); add-wins masks a concurrent remove (state it). | NOTE | Spot-checked; all correct. | **REPAIRED-BY** doc/convention amendments (§3.2) + the JD-20 refinement (§4). |

Also adopted as **confirmations**: the round-1 repair audit (6 of 7 clean landings; encrypted-dirnode launch construction verified record-by-record against mintable rows; canon + kill-list check clean); the codex am.8 flag on the `efs.os/dirnode` PIN shape (COULD-NOT-VERIFY, fallback exists, routed to the kinds owner — the one possible frozen-surface touch, and it is a *reading* of existing surface, not new surface); the "zero ceremony bytes" claim survives the entire OS-tier design and both red teams.

---

## 2. Gap-closure ledger (critic §6, GAP-1..6)

| Gap | Status | By what / what remains |
|---|---|---|
| **GAP-1** crypto red team owed on the F-batch | **CLOSED** by attack-layer1.md — and it earned its keep: it found the one freeze defect (F-2) single-reviewer verification missed. The two load-bearing derivations it asked a third party to re-run, I re-ran this session: the F-2 salt-provenance trace **holds** (pure logic from the two pinned formulas); the 2026/396 anonymity-dependency direction is internally consistent but the citation is **carried, not re-fetched** — acceptable because the repair (an honesty caveat) is safe in both directions. The F-batch now freezes only as amended in §1.1a. |
| **GAP-2** no OS-private-tier design of record | **CLOSED** by os-private-tier.md (numbered W1–W6, R-GAP3, config classes, §5.6, recovery ladder) + the mandated re-run (attack-os-tier-2.md attacked the actual steps). The closure condition is met. The FATAL/SERIOUS repairs it owes (JD-31..35) are forward work on a design that now exists, not a reopened gap. |
| **GAP-3** concurrent FEK-rotation hazard | **CLOSED for team epochs** by R-GAP3 (ratified; its anti-(B) derivation supersedes my arm-(B) sketch — see §0) **as completed by JD-33** (read-your-latest-epoch serialization + monotone-remove roster rule + extended fixture, closing Schedules A/B). **The same hazard class re-emerged on new surface** — the shred ring (§1.2 FATAL) — tracked as **JD-31 / G-2**, owed by the OS lane **before the shreddable tier ships** (launch unaffected). |
| **GAP-4** collab-transport spec | **CLOSED** by os-private-tier §5.6 (session/room/delta/checkpoint/offline spec, relay trust statement, launch-honest tiering) + attack-os-tier-2 §7 audit. One spec amendment owed: **JD-34** (mid-session eviction on rotation). Relay operation remains a resourcing decision (JD-30), honestly darked at launch. |
| **GAP-5** PSI ruling | **CLOSED — text supplied here for mechanical adoption by the synthesis:** *"PSI adds nothing over the existing recipient-privacy design: random/opaque occurrence keys plus published high-entropy `encryptionKey` blobs leave no low-entropy identifier for an intersection protocol to operate on — the oracle PSI would guard against is already closed by construction (A1/C6/F-3). The OPRF-hardened variant earns one narrow home: the OS-level 'share to an email address/human name' flow, where a low-entropy identifier genuinely exists (frontier-zk §3(a)). No reservation, no convention beyond that flow."* |
| **GAP-6** stealth epoch-field ⊂ JD-8 | **STILL OPEN — unchanged.** Neither new file touches it. It remains folded into JD-8 (reserve the genesis feed WITH the epoch field, or skip the feed), a James ceremony decision. Nothing new owed by any lane. |

---

## 3. Deltas to critic §3 (the consolidated reservation table) — added/changed rows only

### 3.1 ROW-class changes

- **F-1 — CHANGED:** as in §1.1a (four pins added; salt declared a free author-chosen input with provenance never verified on-chain). Sufficiency column now reads RED-TEAMED (attack-layer1 §5).
- **F-2 — REMOVED from §3.1** (pulled to Durable per §1.1a; kill-list #18 guards the quoted form). If JD-36 = Option A, it re-enters ONLY as F-2+F-2b with vectors, never alone.
- **F-3 — unchanged;** confidence label upgraded CRITIC-VERIFIED → RED-TEAMED.
- **F-4 — CHANGED:** row text freezes the property only (§1.1a); the formula moves to §3.2. RED-TEAMED (incl. the no-owner-escrow-fingerprint check).
- **F-5 — CHANGED:** row text freezes grammar open-endedness only (§1.1a); the no-classical-tag discipline + refuse rule move to §3.2. RED-TEAMED.
- **R5-zk — CHANGED:** classical-floor qualifier appended (§1.1a). RED-TEAMED.
- **Stealth genesis announce TAGDEF — unchanged** (JD-8/GAP-6 open).

### 3.2 CONVENTION-class — additions (all BLESS)

- **Concrete self-escrow derivation** `occ_self = HMAC-SHA-256(scanRoot, "efs/v2/self-escrow" ‖ fileId)` — SDK convention, version-gated (moved out of F-4). Plus the **scanRoot custody note**: long-lived, not freely rotatable (rotation breaks re-derivation of all prior self-escrow keys, mailbox secrets, view-tag matches); recovery/rotation keeps a scanRoot-generation list; compromise blast radius = the recipient graph + self-escrow index. Rides JD-11 docs.
- **KEM-registry governance** (moved out of F-5): launch `0x01 = xwing-v1`; `0x02/0x03` reserved names; no classical-only tag ever registered; **conforming clients MUST refuse private-tier wraps under any classical-only tag** — the operative fence, CI-tested.
- **F-2b optional subtree-salt convention** (post-freeze-addable, per-tree opt-in): shape as §1.1a, with the rename-re-anchors and names-you-know caveats.
- **Recipient-privacy quantum-expiry honesty line (MUST, positioning + row docs):** *"EFS wraps give post-quantum content secrecy and classical-only recipient unlinkability. Who-shared-with-whom is protected against today's adversaries and is retroactively exposed to a future quantum adversary. Anonymity was never promised; even confidential-tier recipient privacy has a quantum expiry."* Doc correction alongside: layer1 §8.1's combiner statement carries BOTH directions — confidentiality = OR of halves; anonymity = AND — and never the bare "∧".
- **Scan-convention notes (H2):** the per-counterparty counter roams in P9 state or re-derives by self-scanning own past wraps (chain-recoverable, not fatal); counter gaps are a widen-window perf cliff with H1 fallback, never a strand.
- **Shred-keyring single-writer discipline** (JD-31; gates the shreddable tier, G-2): designated shred-committer or checkpoint-serialized shred with read-your-latest-ring-epoch refusal; the spec states why no deterministic convergence can exist for a destroyable key; concurrent-shred fixture as §1.2.
- **Rotation serialization + roster rule** (JD-33): read-your-latest-epoch for team AND ring rotations; roster = LWW-by-rotation-order, removes monotone, never OR-set-union; §5.4 fixture extended (different-removal, self-race, roster assertions).
- **Inbound-share recovery repair** (JD-32): recovery-KEM standing `kem` entry (or ack-embedded re-wrap); a WA-2 fixture asserting phrase-only re-open of an accepted share.
- **Live-collab eviction** (JD-34): rotation ⇒ re-announce `K_sess` under e+1 + checkpoint-to-latest-confirmable-epoch + the stated best-effort residual.
- **Team-dirnode causally-complete read** (C-M ruling): per-member slot + causally-concurrent superseded versions; causal-frontier bound replaces "recent history"; cost stated honestly.
- **Publish partial-failure copy:** warn before the FIRST envelope ("once any part lands, that part is public permanently"), not only at completion.
- **Shamir honesty lines:** any k friends can silently reconstruct off-chain (the threshold IS the trust assumption); shares are permanent on-chain reconstruction targets (HNDL, bounded by the hybrid). Rides JD-11.
- **Relay/collab honesty:** the relay sees the collaborating clique at the network layer (Playbook-1 off-chain twin — state as loudly as the on-chain leak); intra-session forged deltas mean the saver signs smuggled content under their own authorship (spell out); curator phrase = skeleton key to all team epochs past and future (state at team creation, next to "curator device theft is a re-key event").
- **Padding refinements:** sensitive dirnodes may pad to a fixed large size (bucket-crossing broadcasts folder growth); the 8 KiB VAL bucket definition accounts for AEAD/prologue overhead under `MAX_VALUE_BYTES = 8192`; add-wins-masks-remove stated in the dirnode UX.
- **Class-(a) sync default** (JD-35(i)): platform-keychain vendor-escrow named per item as the default for re-auth-expensive tokens; mesh sync requires co-online devices, stated.
- **dekCommit doc nit:** dekCommit = wrap↔content binding; EtM-HMAC (CMT-4) = content-equivocation prevention — two mechanisms, documented as two.

### 3.3 REJECT-class — no changes. §3.4 — one change

- **D3/D4 row (§3.4) — CHANGED:** now reads "CONFIRM + amend (**F-1 as four-pinned; F-2 pulled to Durable — the frozen family text must carry the §1.1a negative sentence and must not promise subtree bulk-unlock**)."

---

## 4. Deltas to critic §4 (the James decision list) — added/changed rows only

**Changed rows:**

- **JD-1 — CHANGED:** add the third structural leg: **the shreddable choice exists only for unshared content** — shared/team files are recoverable-only (curator-root epochs are always re-derivable; JD-35(ii)); tier tables and first-run copy state it.
- **JD-2 / §7 positioning — CHANGED:** add **markup #4**: the recipient-privacy quantum-expiry line (§3.2) joins the launch-availability caveat, tier honesty, and the metadata §6 sentence.
- **JD-9 — CHANGED:** the CRQC blast-radius input is broader than stealth view keys — **the ordinary keyWrap recipient graph is retro-linkable at CRQC** (Q1); price default-on stealth against that baseline.
- **JD-12 — CHANGED:** the walkaway gate may not assert WA-2 inbound-share recovery until JD-32 lands (fixture required).
- **JD-20 — CHANGED:** sponsored flush names its sponsor trust: per-persona sponsors or an explicitly named non-logging assumption, recorded in the fleet map (a logging/shared sponsor re-links personas; a shared relayer is already barred).
- **JD-26 — CHANGED (and CONFIRMED):** ratify per-member pointers + CRDT merge, with the C-M amendment: team-folder resolution is causally complete (slot + concurrent losing versions; causal-frontier bound), or the invisibility window is documented as an accepted limit — recommend the former.

**Confirmed new rows from os-private-tier.md (adjudicated, all adopted):** **JD-27** shred keyring over mesh-only (adopt keyring, now gated by JD-31) · **JD-28** full-vs-scoped enrollment default (adopt: full for personally-held devices, scoped as travel mode, W4.5 honesty box at enrollment) · **JD-29** ratify R-GAP3 (adopt; it legitimately amends my round-1 sketch — arm (B) withdrawn, see §0 and kill-list #21) · **JD-30** collab relay resourcing (adopt (a): async-collab at launch; reference relay under the JD-16 posture when resourced).

**Added rows from attack-os-tier-2.md (adopted as numbered):** **JD-31** shred-ring concurrency discipline before the shreddable tier ships (recommend (a); (b) single-device interim acceptable) · **JD-32** inbound-share recovery repair (recommend (a) standing recovery-KEM entry; (b) acceptable; (c) rejected) · **JD-33** rotation serialization + monotone-remove roster rule + fixture extension (recommend ratify) · **JD-34** live-session eviction posture (recommend force re-announce + state the best-effort residual) · **JD-35** class-(a) keychain default + shared-not-shreddable honesty (recommend both).

**Added rows from attack-layer1.md:**

- **JD-36 — F-2: pin the salt-provenance rule (Option A/F-2b) or drop subtree bulk-unlock (Option B).** Plain: the reserved "reveal one folder key ⇒ unlock its subtree" math was internally broken — the fix is either a heavier pinned rule where renaming a folder re-keys its whole subtree, or dropping the on-chain-derived subtree unlock (encrypted dirnodes already give subtree sharing by handing out child caps, without the rename cost — and the option stays addable later for new trees). **Recommend Option B (drop).** Defer-cost of B: none permanent (post-freeze-addable for future trees); defer-cost of ignoring: freezing incoherent math forever.
- **JD-37 — Adopt the recipient-privacy quantum-expiry honesty line** (§3.2) into positioning and row docs (it is §7 markup #4). Plain: we can honestly promise your file *contents* stay secret even against future quantum computers; we cannot promise *who you shared with* stays hidden from them — say so once, plainly, everywhere the wrap tier is described. **Recommend adopt verbatim.** Defer-cost: an overclaim a competent cryptographer will publicly falsify.
- **JD-38 — Disclose the hardware-wallet private-tier gap.** Plain: a Ledger/Trezor protects your *authorship* key but cannot hold or derive your *encryption* root (and the coupling rule rightly forbids deriving it from the signing key) — so a hardware-wallet user's private archive is only as safe as their computer's software keystore. Not fixable; must be said, like the smart-wallet authorship exclusion. **Recommend a dedicated FAQ/docs statement + enrollment-time note for HW-wallet users.** Defer-cost: HW users assume a protection they do not have — the worst kind of silent overclaim.

---

## 5. Kill-list deltas (new entries only; critic §5 continues at #18)

18. **F-2 child-derivation chain as quoted in critic §3.1** ("pin `K_child = HKDF(K_node, …)`" with the K_node-unlocks-descendants property, salt as free input). KILLED as frozen math by attack-layer1 §5 (does not compose with F-1; the property is false without an unpinned salt-provenance rule). The frozen family text must not promise subtree bulk-unlock (JD-36). Do not reinstate the bare pin.
19. **"X-Wing wraps are key-private" unqualified.** DOWNGRADED: key-privacy is **classical-only** (anonymity is a weak combiner needing both halves; carried eprint 2026/396); content confidentiality remains post-quantum (robust combiner). Never write or imply post-quantum recipient unlinkability.
20. **"A hardware wallet protects your EFS private tier."** KILLED preemptively (attack-layer1 Q4): the coupling rule forces the enc/scan root into software; the HW wallet secures authorship only. Banned implication class, same as "anonymous."
21. **Deterministic FEK = f(oldFEK, membership-epoch) as a standalone removal re-key discipline** (critic §1's arm (B)). KILLED by R-GAP3's anti-(B) derivation: the removed member holds all epoch-e state; exclusion requires entropy they never held. Survives only as R-GAP3.2's form — determinism from the *curator's own secret root*, i.e. single-writer wearing math.
22. **"The per-file/subtree shreddable choice extends to shared/team files."** KILLED (attack-os-tier-2 §6 corollary): curator-root-derived epochs are always re-derivable; shared content is recoverable-only, forever.
23. **"WA-2: accepted inbound shares recover with the phrase alone" — as currently specified.** KILLED until JD-32 lands (wraps target per-device, non-phrase-derivable KEM keys; the ack stores a pointer, not a key). The walkaway gate must not assert it meanwhile.
24. **OR-set/add-wins-union as a roster-merge rule.** KILLED for roster state (union re-admits concurrently-removed members — attack-os-tier-2 Schedule A/B). Add-wins stays correct for dirnode *child entries*; rosters are LWW-by-rotation-order with monotone removes. Do not let the dirnode CRDT discipline leak into membership state.

---

## 6. Conflict adjudications (new files vs round-1 rulings, and vs each other)

**C-J. attack-layer1 F-2 vs critic §3.1 F-2. WINNER: attack-layer1.** My round-1 "ADOPT (pin it)" froze math whose headline property was false as specified. The red team's trace is reproduced in §1.1; F-2 is pulled (JD-36). This is the round's cleanest demonstration of why GAP-1 existed.

**C-K. os-private-tier R-GAP3 vs critic §1 GAP-3 sketch. WINNER: os-private-tier.** My sketch offered arm (A) OR arm (B); the anti-(B) derivation shows (B) standalone is unsound and only survives as determinism-from-a-single-writer's-secret. R-GAP3 ratified (JD-29); arm (B) killed (#21).

**C-L. os-private-tier internal: W4.1 ("the phone never holds or reconstructs the primary key") vs R-GAP3.2 (curator's two devices racing primary-signed rotations). RULING: W4.1 is the binding custody model.** Rotations are primary-signed checkpoint ops, and exactly one device custodies the primary key at rung 0/2 — so Schedule A's two-signer race requires an unusual setup (two desktops / a hardware wallet driven from two hosts). R-GAP3.2's convergence story is retained as defense-in-depth for exactly those setups, and JD-33's read-your-latest-epoch rule closes Schedule A regardless of custody model — the repair is custody-independent, which is why it, not a custody assertion, is the normative fix. os-private-tier owes one clarifying sentence reconciling the two passages.

**C-M. os-private-tier internal: W2.3 ("pointer slot plus its recent slot history") vs W5.3 ("resolve all roster members' pointer slots"). RULING: W2.3's semantics, made causally precise, is normative for team folders.** Resolution reads each roster member's slot AND that member's causally-concurrent superseded pointer versions (RP-2 spine filtering; incremental frontier cursor); "recent" is replaced by the causal-frontier bound (drop a version from the merge only when it is in every live head's ancestry — an old-but-concurrent loser dropped from a recency window IS lost data). Cost stated honestly: O(N members × pointer history since the last stable frontier), not O(N) slot reads. If an implementation chooses the cheaper W5.3-literal read, the invisibility window MUST be documented as an accepted limit — but the recommended default is completeness (refines JD-26).

**C-N. attack-layer1 vs critic on F-5's "never mint a classical-only tag" class. WINNER: attack-layer1.** The registry is Durable by design, so the clause is governance + a conforming-client refuse rule, not frozen surface. My round-1 row text over-claimed the enforcement class; substance unchanged. Reclassified (§1.1a, §3.2).

**C-O. attack-layer1 vs critic on F-4's formula-in-frozen-text. WINNER: attack-layer1.** Self-escrow has no cross-author interop; the property freezes, the construction stays Durable behind an SDK version gate. My "e.g." example is dropped from frozen text.

**C-P. attack-layer1 Q1 vs layer1-crypto §8.1/R5-zk. WINNER: attack-layer1.** The lane's "∧" notation inverts the confidentiality guarantee and its key-privacy blessing lacked the against-whom analysis. Doc correction + R5-zk qualifier adopted (§1.1a). Note the two files do not actually disagree on confidentiality (layer1's §8.3 "dead unless both fall" states the OR-combiner correctly); the conflict is notation plus the missing anonymity split.

**C-Q. attack-os-tier-2 §6 vs os-private-tier W1.7. WINNER: attack-os-tier-2.** The keyring spec mandates fresh non-re-derivable epoch keys (correctly — shred requires it) and simultaneously permits multi-device rotation with no serialization discipline; the two are irreconcilable without single-writer. Not a disagreement to split — a hole in the newer design, repaired by JD-31. The red team's own scope note is confirmed: launch is unaffected.

No other conflicts found: os-private-tier consumes critic rulings faithfully (repair audit 6/7 clean), attack-os-tier-2 reinstates nothing killed, and both new red teams honored the over-reservation guard (zero new rows requested anywhere in the round).

---

## 7. FINAL VERDICT

**The pass corpus is now complete and consistent enough to synthesize from**, with critic.md + this addendum applied together. The two structural defects of round 1 (§0.1/§0.2 of critic.md) are cured: the OS tier has a design of record that was re-attacked against its actual numbered steps, and the F-batch has been adversarially cryptanalyzed — and amended where it failed. No mission end is breached; no killed claim was reinstated; the "zero new ceremony bytes from the OS tier" claim survived two red teams; the ceremony surface is now: **F-1 (four-pinned) · F-3 · F-4 (property) · F-5 (grammar) · R5-zk (qualified) · JD-8's optional stealth line · JD-36's optional F-2+F-2b.**

**Remaining owed work (all assigned, none blocking synthesis):**

1. **James:** round-1 JD-1..25 as amended (§4 changed rows), JD-26..30 (confirmed), JD-31..38 (new). Ceremony-relevant: JD-8 (epoch field — the only still-open now-or-never), JD-36 (F-2 option).
2. **OS/collab lane:** JD-31 shred-ring discipline + fixture (gates the shreddable tier only); JD-32 recovery-KEM repair + WA-2 fixture (gates the walkaway *claim*, not launch); JD-33 serialization/roster spec + fixture extension; JD-34 eviction spec; the C-L and C-M clarifying amendments to os-private-tier.md; JD-35 doc fixes.
3. **Kinds owner:** rule codex am.8 on the `efs.os/dirnode` PIN-under-KIND_DATA shape (launch-blocking for private folders; the +1-record fallback exists, so not now-or-never).
4. **Synthesis:** apply §3/§4/§5 deltas mechanically; include the GAP-5 PSI paragraph (§2, supplied verbatim); positioning ships with four markups (§7 of critic.md + this addendum's markup #4).
5. **Ceremony sheet:** cut F-1/F-3/F-4/F-5/R5-zk per §1.1a exact text; the golden-vector suite gains the domain-distinctness assertion and the E4-pre-blinding boundary cases.
6. **No further red team is owed.** GAP-1 and GAP-2 are closed; the two derivations attack-layer1 flagged for third-party re-run were re-run here (F-2 trace: holds; 2026/396: carried with the safe-direction argument). The standing re-open triggers are listed in §8.

---

## 8. Confidence

**VERIFIED (read in full this session; reasoning reproduced, not recalled):** critic.md (my own round-1 record, re-read end-to-end), os-private-tier.md (all walkthroughs, R-GAP3, §5.6, recovery), attack-layer1.md, attack-os-tier-2.md, fs-pass-freeze-reservations.md, fs-pass-synthesis.md; layer1-crypto.md re-read in the attacked sections (§2, §3, §4, §5, §6, §7, §8, §9, freeze table, decisions). Independently re-derived this session: the F-2↔F-1 non-composition (and the post-freeze-addability of F-2b under D3's deterministic-salt permission — my addition); the shred-ring irreconcilability (fresh-key-mandatory ⊥ deterministic-convergence) and the fork/lockout trace; Schedule A's roster-independent-derivation crux; the inbound-share key-absence trace against layer1 §4.2 + W1.3; the anti-(B) derivation; the F-4 property-vs-formula interop argument; the F-5 governance reclassification.

**CARRIED, not independently re-verified:** eprint 2026/396 (X-Wing anonymity weak-combiner) — attack-layer1's fetch; adopted because the repair is safe in both directions and the combiner logic is internally consistent. X-Wing draft-10 statuses, ERC-5564 parameters, all gas/dollar/q-s figures, Yjs/enclave properties — as the lanes verified/flagged them. The codex am.8 reading remains COULD-NOT-VERIFY (kinds owner owes the ruling).

**LOAD-BEARING RESULTS OF THIS ADDENDUM (re-open if any is wrong):** (1) F-2 pulled — if someone shows F-1's salt CAN be derived by a K_node holder under the texts as written, F-2 reverts to the ceremony; (2) F-2b's post-freeze-addability (rests on salt being a free input to F-1 and D3's deterministic-salt permission — if either is narrowed at the ceremony, JD-36 becomes now-or-never and must be decided before freeze); (3) the shred-ring FATAL and its single-writer-only conclusion (if `K_ring` were persistent-root-derived the lockout vanishes — but shred would be defeated, which is worse); (4) the quantum-expiry honesty line (if 2026/396 is overturned, the line becomes merely conservative — do not remove it without a cryptographer's sign-off that X-Wing anonymity is post-quantum, which no current source claims); (5) the C-M causal-frontier read rule (if RP-2 spine filtering cannot cheaply enumerate superseded pointer versions per author, the accepted-window fallback becomes the default and must be documented).
