# Codex prompt — affordable, programmable and durable EFS foundation

**Status:** superseded launch prompt; never sent or executed

#status/superseded #kind/prompt #repo/planning #topic/efsv2

> Do not run unchanged. The replacement split and retained requirements are in
> [[Reviews/2026-09-10-foundation-reply-after-economics]]. Original text follows
> for comparison, not as an active assignment.

Act as the EFS v2 PM and engineering coordinator. Implement and validate the
next useful reversible foundation experiments, refine the designs from evidence,
and keep going through independent reviewed checkpoints. Use expert subagents
for genuinely independent work. Prioritize answers that change what we build;
do not spend the run adding another broad requirements catalogue.

## Baselines and ownership

Read AGENTS, your v2-pm profile, current owner inbox/rulings and this round's
README. Verify these baselines or explicitly reconcile successors:

- PM design: `codex/mvp-c0-coherence`, intake baseline
  `2dce19a8b101910e410a54de4f29968f3ab7c20a`; this prompt's containing commit is
  a later documentation-only checkpoint.
- Fable Files: `fable/2026-09-09-files-browser`,
  `0132e3561570fff710625216a4e99303dce0faae`.
- Acceptance: `codex/programmable-acceptance`,
  `e358ad66bb6471e1d89b1327d03c5ba7a286b116`.

Fable owns shared Files reader/browser/Core integration and its current-index
comparison for this round. You own current-spine reconciliation and isolated
cost, acceptance/evolution and continuity labs. Use owned worktrees and disjoint
artifacts. Pin any imported Solidity/browser fixture to a commit; no moving
cross-worktree imports, second authoritative JS filesystem or bulk branch merge.
Produce minimal integration patches/tests for Fable; only claim joined evidence
after a coordinated checkpoint actually runs together.

## Start with coherence, not new vocabulary

Resolve remaining mappings against the existing four C0 point outcomes and
operation-specific SDK families. Reconcile old S6/S7/A4 findings with today's
spine; name the law-owner and causes without resurrecting a universal wire enum
or a sixth SDK seam. Preserve separate coverage, support, integrity, authority,
currentness and effects. Fable implements aggregate consumer shapes.

Trace the multi-controller user direction and Stage A graduation G1 against
current owner rulings and the greenfield reset. Reconcile documented requirements
versus prototype shortcuts; don't silently promote old KEL mechanics or invent
a new owner ruling. Make day-one limitations visible in first-writer guidance.

## Parallel priority A: replace economic guesses with a decision-changing comparison

Recover Fable's exact inputs/transactions where available. Otherwise reconstruct
a source-pinned equivalent, label it non-identical and retain the missing
provenance. Keep traces, encoded calldata, slot changes, compiler/client/fork
pins and pricing inputs. His 98.5% figure is a
residual, not opcode attribution; 391 slots and 50x are not verified outcomes.
Separate new metadata/body, context, index and Binding writes from payload staging,
setup, authorization, sponsorship, fees, reads and recovery. Apply exact calldata
zero/nonzero floors and chain-specific transaction caps; don't use a blanket
chunk multiplier, assert all Anvil versions behave alike, or infer batching
impossibility from adding standalone transaction gas.

First resolve the 104,520-gas chunk discrepancy: the U3 carrier writes the whole
chunk to Solidity bytes storage, whereas identical restaging returns early.
Measure fresh nonzero upload, resumed upload, identical restage and a real
two-file batch separately. Retain opcode and unique-slot attribution instead
of treating all unexplained gas as SSTORE.
Reconcile receipt gas, opcode gas, refunds, calldata-floor adjustments and
storage pre/post values. Price exact chain IDs and block numbers/hashes, all
fee components and input compressibility under both low-fee and congested
scenarios; a single cheap snapshot is not an affordability verdict.

Compare, in order: compact/packed storage; deduplicated immutable shared context;
immutable code-backed canonical bodies (SSTORE2-style, not automatic dependency
adoption); and only then commitments plus caller-supplied witnesses/external
custody. Separate storage-layout changes preserving exact IDs/bytes from semantic
encoding changes. Map who reads each field today AND test a future independent
Solidity consumer reading an unfamiliar old field without today's router.

Code-backed data remains chain state. Price deployments, size limits, reads,
pointer/code authentication, upgrade migration and clean reconstruction. For
witness/external arms, state what ordinary contract reads and independent retrieval
are lost, who retains bytes and proofs, and how missing data fails. Never label
calldata/logs/blobs permanent ordinary-state storage or assume an archive node
retains everything. Keep full verification; only optimize it if actual evidence
identifies a problem. Report net gains including index costs supplied by Fable.

## Parallel priority B: join programmable acceptance and useful Type evolution

Reuse the acceptance lab, but connect its rules/generated consumers to a pinned
actual upgradeable Core/Files implementation through the narrowest adapter.
Produce a patch plus tests for the integration owner rather than editing Fable's
checkout. Inventory direct, relayed, imported, bootstrap, upgraded and privileged
paths so none can claim equivalent acceptance after bypassing its required rule.

Demonstrate developer-defined Note and RPG Outfit rules, an independent Solidity
reader, and generated TS/static inspection. Pin Type/rule/activation evidence;
mutable rule state and execution dependencies must have explicit qualifications.
Attack changed activation, dependency/proxy drift, stale intents, reentrancy,
oversized returndata, resource exhaustion and racing unique/paid claims. Preserve
unrelated-Type progress and distinguish ordinary reads from new stateful actions.
Test whole-plan invariants where per-item checks are insufficient. Limit rollback
claims to participating execution effects, excluding outer fees/nonces/delegation
processing and earlier staging. Retry evidence names the original execution.

Go beyond old-editor refusal: Note 1.1 adds a field, an old reader reads its
supported fields, an old editor changes a known field while preserving the new
one through SDK/cache/export, and a new reader sees both changes. Add explicit
incompatible-version refusal/projection and unknown authority-restriction refusal.
Compare the mandatory Type/profile-pair arm if it can change the recommendation;
do not redesign the entire layered Type language merely to finish the flat lab.

## Parallel priority C: continuity and privacy with concrete users

Reuse KEL/account research to compare intrinsic key-bound succession, account-
native recovery and managed stable-Principal control. Two fresh users deliberately
select each other's data; one Principal has multiple authorized controllers without
adding Lens positions. Exercise old-key revocation, scoped delegation, recovery,
replay and historical signature interpretation. State pre-authorized recovery
assumptions; a reserved authorityRef/authEpoch is a seam, not implemented recovery.

Build a small real encrypted-manifest/content fixture with established crypto,
not a fake private Boolean. Test locked/unsupported/missing/corrupt/verified-empty
outcomes, tampering, scope/key epochs, public-index leakage and AEAD context.
Restore signing authority, ciphertext and decryption keys separately. Rotation
doesn't retract copied keys/plaintext; encrypted shape doesn't prove plaintext
application validity. Give Fable fixtures and an adapter for the actual UI.

## Continue only where it buys a clear answer

Use existing MUD/Protobuf/AT/Tahoe/CAR dossiers before new research. A useful
remaining spike is two offline edits racing a revision: compare explicit CAS
conflict with an application-level merge; CRDT convergence does not resolve
permissions, uniqueness or revocation. Keep collaboration outside the MVP unless
the experiment exposes a Core requirement. Broader EAS interoperability and a
small Arcade consumer follow the foundational checkpoints, not replace them.

For each checkpoint: reproduce a falsifier, implement, independently review,
validate, refine current designs, and commit/push owned artifacts. Preserve dated
reports. Deliver what is proven separately versus together, simpler developer
examples, measurable costs, remaining unknowns and only evidence-ready owner
choices. Coordinate a final single Files → SDK → Core demonstration with Fable.
No permanent repos, public transactions, real funds, main merge, protocol promotion
or freeze. No claim that one run fully de-risks a fifty-year foundation.
