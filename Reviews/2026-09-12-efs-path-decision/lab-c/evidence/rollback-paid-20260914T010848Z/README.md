# C matched mined rollback controls

**Standing:** independently reviewed RPC-observed evidence from one disposable
local run, September14 01:08:48–01:08:51 UTC. Not authenticated chain proof,
full Files/portability conformance, permanent adoption or new normal price rows.

Runner/checkout: `84e1081e46163547e2928c12a4551233e8f07afc`.
Unchanged compiled Solidity: `2ca7349e5d683c3ff10651c0fc106c10da946145`.
Solidity0.8.30, Cancun, viaIR, optimizer200; fresh chain31337,30M blocks,
public deterministic test accounts, bounded history256, owned cache/watchdog.
All owned process groups were stopped; no compiler/cache/chain database is
included here. Original Claude workspaces were untouched.

| Complete signed A1 arm | Receipt status | Gas | Result |
| --- | ---: | ---: | --- |
| Mandatory Quote scale7 refusal | 0 | 366,020 | Full100-byte rule error; prescribed S0 unchanged |
| Final market-tag index refusal | 0 | 2,246,568 | Full36-byte Index error; prescribed S0 unchanged |
| Zero-poison calibration | 1 | 2,400,648 | Exact prepared S1, including mandatory index effects |

Each graph starts with genuine Producer Types/Items/Pair admissions1–6 and
then attempts Subject/Quote/HEAD/FOLDER/TAG7–11. Static and mined attempts use
identical sender/destination/data/5M gas. Failed S1 equals S0 for all126 logical
and261 physical observations, including hidden backing words. Calibration
commits the expected state and high-water11. Error attribution joins exact
static bytes to reviewed source and mined identical input; receipts themselves
do not contain revert bytes.

`audit.json` checks2,518 raw envelopes,2,322 fixed-block state observations,
27 signed transactions,18 full deployed runtimes and28 linked headers.
`supplemental.json` reconciles all27 gas summaries, chain/source/raw/gate fields,
72 sealed files and6 compiler artifacts/49 transitive source hashes. Source
Node tests93/93, independent preparation18/18 and actual-packet supplement
tests61/61 passed. Separate packet review rejects corrupted backing words and
full-error bytes. See the exact reviews and logs; no gate flag substitutes for
raw evidence.

`SHA256.json` inventories29 unchanged evidence/source files; this README and
the inventory itself are not self-hashed. `pins.json` retains original run-time
paths and commitments. Re-deriving inputs or repeating the pin supplement
requires those retained source/artifact inputs; the packet is not a compiler
bundle. The frozen `audit.mjs` accepts a packet directory plus the included
`expectations.json`, using the recorded Node26/ethers6.15 dependency.

Known invocation limitation: the sealed supplementary CLI compares an alias
path with Node's canonical module path. On macOS invoke its canonical
`/private/tmp` path or exported `checkPacket`, as done for this run. An empty
CLI output is not a passing result. Sealed code was not rewritten after the
run to conceal that issue. This matters for repeating the checker, not for
the already reviewed raw state and receipt results.

Both B and C now have the specified logical refusal/calibration controls.
Their other query obligations are still unequal and must be priced. Normal
product rows remain the September13 19:10 snapshot; the next discriminator is
the complete required incoming-Quote query, not another rollback rerun.
