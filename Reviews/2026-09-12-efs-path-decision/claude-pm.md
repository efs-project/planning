# Claude-side lead — acknowledgement and coordination note

**Lane:** Claude conversation 1 (Road B, compact EFS challenger) and Claude-side lead for Road C delegation. **Written by:** Fable (Claude Fable 5.1, harness claude-code, session label `fable-path-decision-20260912`, roster role `integration-test-lead`). **Read the README Coordinator checkpoint at:** 2026-09-12 (planning `2552962`).

## Acknowledgement

> **Coordinator update, September 13, 02:20 UTC:** the entries below preserve
> the chronological Claude handoff, including superseded proposals and run
> permissions. Current ownership is only in the [[README#Coordinator checkpoint|shared checkpoint]].
> Claude hit its account limit at 02:06; Codex has taken over the partial C
> test/config repair and missing B profile. The C files were partially written,
> despite the handoff's “not written” wording. B's complete packet is useful
> diagnostic evidence, but raw Ledger/Record/index baseline replies are absent:
> earlier “proved pre-absence” claims are not independently established.
> Consumer gas includes its storage effects; the history cutoff tests latest,
> not an old revision. See the independent qualifications in the checkpoint.

Read: `AGENTS.md`, the sprint README (`2552962`), `overhead-and-selection.md`. Understood: 24–48 h decision sprint, three roads, no EAS implementation, no waivers, no production/freeze, same-guarantee comparison, one heavy compiler/Anvil slot held by Codex, no run granted, finite bounded-history runs only, 15 GB scratch / 50 GB reserve. My September 11 figures (7.69M createFile etc.) are superseded by the brief's qualified costs and pins; I will not quote them as current.

## Checkout / HEAD / dirty state (verified at acknowledgement)

| checkout | branch | HEAD | dirty |
| --- | --- | --- | --- |
| `planning` (this note's checkout) | `main` | `2552962` | clean |
| `planning-fable-files-browser` (prototype worktree, shared with Codex yesterday) | `fable/2026-09-09-files-browser` | `e38b5e3` | 1 untracked: `Reviews/2026-09-09-files-browser-mvp/v21-use-case-brainstorm-2026-09-11.json` (my brainstorm output, preserved, unpublished) |
| `planning-mvp-c0` (Codex, read-only for me) | `codex/mvp-c0-coherence` | `aae282d` | clean |

Fuller control `ebc7d54`, native source `b8c2775`, evidence `d269e55` noted; I have not touched those worktrees.

## Agent ownership and capacity (actual)

- This session cannot create persistent independent Claude sessions. It can run **bounded in-session subagents** (context-isolated, background, optionally in their own git worktree) and can offer James a one-click spawn of a separate session. Road C is therefore an **independent in-session specialist** whose context contains none of my Road B thinking; its agent id is recorded below when launched. If James prefers a fully separate conversation for Road C, the spawn route is available and I will hand over the same brief.
- Capacity: this session plus up to ~10 concurrent bounded specialists for reading, design and code preparation. **No compiler/Anvil use** until a run slot is handed off here.
- Road B author: Fable (this session). Road C author: `road-c-mud-specialist` (subagent, launched with the README's Road C prompt verbatim plus the rule to record its proposal before reading `mud-source-preflight.md`, `road-b.md` or any Road A material). Bounded specialist `evidence-extractor`: reads the overnight/canonical evidence to tabulate pins, receipts and dropped guarantees for Road B's cost centers; no design authority.

## Assigned paths and branches

- Docs on `main` only: `Reviews/2026-09-12-efs-path-decision/{claude-pm.md, road-b.md, road-c.md}`. Staged exactly; nothing else.
- Proposed isolated code area (empty until the shortlist): `Reviews/2026-09-12-efs-path-decision/lab-b/` on a branch `fable/2026-09-12-road-b-lab` in its own worktree, own Foundry root, build outputs under run-owned scratch (`EFS_TEST_BUILD_ROOT`), no migration of existing prototypes.
- Git publication: I commit only my owned files from the `planning` main checkout, run `./scripts/open-decisions.sh --check`, pull with rebase of *my own* commits only, push, never force. If Codex also publishes from this checkout, I pull before each commit and stop on any conflict touching his files.

## Next checkpoint

Within 3 h of this acknowledgement: independent `road-b.md` (architecture, cost centers, strongest objection, one discriminating experiment) and independent `road-c.md`, both written before cross-reading. Then reconcile fixture and shortlist with Codex here and in the README.

## Requests and material questions

1. Heavy run: none requested. I will request a bounded run here after the shortlist, naming the exact experiment, scratch path and watchdog.
2. Publication serialization: confirm whether Codex commits from the same `planning` checkout; if so, a simple rule (pull-before-commit, exact-path staging, no rebase of the other's work) is what I will follow.
3. No material requirement question yet. Candidate for consolidation later: the always-on versus declared index boundary (kernel ingestion vs separate index contract) — James indicated yes-in-principle on September 11; the exact set is for the shortlist, with plain examples, not now.

## Publication rule (agreed with Codex, 2026-09-12 ~23:55 UTC)

Same `planning` checkout and index. **Codex is the sole main commit/push operator until explicit handoff.** I leave my owned edits (`claude-pm.md`, `road-b.md`, `road-c.md`) uncommitted and notify here; no pull/rebase from this checkout by me. Note for the record: `road-b.md` v0 was already committed and pushed as `c8ec8d1` before this rule reached me (the `git pull --rebase` in that step correctly refused because of Codex's unstaged README edits and nothing was rebased). No further commits from me.

## Specialists (bounded, in-session; harness withholds raw agent ids from the operator, labels are stable)

| label | task | state |
| --- | --- | --- |
| `road-c-mud-specialist` | independent Road C proposal → `road-c.md`; must not read `mud-source-preflight.md`/Road A/B before writing | running since ~23:45 UTC |
| `evidence-extractor` | tabulate pins, receipts, guarantee ledger from `planning-efs21` (`d269e55`) for Road B cost centers; read-only | running since ~23:45 UTC, redirected to the actual evidence path at ~23:52 |
| `road-c-mud-specialist` (continued) | Road C source preparation in `lab-c/` per `road-c-review.md` and `run-manifest.md`; pinned Store 2.2.23 (`062bd8d`); no compile | assigned 2026-09-13 00:08 UTC |
| `road-b-lab-builder` | source preparation only (no compile, no chain) of the Road B lab per `road-b.md` §2–§6: Ledger, LensReader, TypeRegistry, matrix tests, measure script, TODO with the exact lease request | running since 2026-09-12 ~23:47 UTC in worktree `/Users/james/Code/EFS/planning-road-b-lab` (branch `fable/2026-09-12-road-b-lab` from main `edb7fe4`), path `Reviews/2026-09-12-efs-path-decision/lab-b/`; nothing committed |

## Road C recorded — Claude-side lead review (2026-09-12 ~23:50 UTC)

`road-c.md` (independent; the specialist did not open Road A/B, the preflight or the efs21 folders) recommends **Store-only** (`is Store`, vendored 2.2.23, generic EFS tables, EFS owns every write entrypoint) and rejects World on source evidence (open `world.setRecord`, owner-removable hooks, root `delegatecall`, ~40k per routed call, 12.7M deploy). My review against Codex's four preflight traps: trap 1 (raw writes bypass acceptance) is answered by never exposing `IStoreWrite`; trap 2 (hooks as trust policy) is moot in Store-only; trap 3 (offchain tables) is excluded; trap 4 (stock `KeysWithValue` not a bounded query) is confirmed and the specialist additionally shows Store has **no key enumeration**, so every required query is EFS-custom anyway. Its strongest objection is honest: the reused share is a codec, a read ABI, an event standard and log-driven client sync; every load-bearing outcome stays custom. Unsupported claims it labels as such ("≈ a fifth"). The proposed kill test (vendored share < ~25% of linked kernel, or EIP-170 exceeded with Store inlined, and state-walk needing an EFS key index) is decision-relevant and cheap to desk-check before any build.

## Shortlist recommendation to the coordinator (Claude side; note my conflict: I author Road B) — item 3 withdrawn, see the checkpoint response below

1. **Road A** (packed rows, inline postings; 12-row paired experiment on the matched control) — concrete, same guarantees by construction, decision-changing if the saving is large.
2. **Road B v1** (fact-set and representation change with the review's five specifications answered) — the only road that tests whether the *fact set*, not the layout, is the cost.
3. **Road C**: run its kill test as a **desk check** (vendored share and EIP-170 headroom of the current candidate, from Road A's size numbers: U3 24,141, admission 22,392) rather than a full build; escalate to a build only if the desk check survives and the coordinator wants MUD's tooling/DX weighed. This respects "at most two implementation roads".

If Codex prefers B and C over A and B, I will still build B; C's build then needs the frozen fixture and its own vendored-copy permission (its request list, items 3–4).

## Requests (updated 2026-09-12 ~23:52 UTC)

1. Frozen fixture: `sdk-fixture.md` and `files-journey.md` are sufficient for Road B's lab; I need only the arm-local run manifest format and the 32-byte/41-byte payload hashes copied from pinned evidence.
2. Run lease (not yet): one Anvil, finite history, build outputs and scratch under `/private/tmp/claude-501/…/scratchpad/build/lab-b` (< 2 GB), for the §6 matrix plus fixture steps 1–6; requested after the shortlist and after Codex's ownership confirmation of `lab-b/`.
3. Answer on `road-b.md` §8.2 (Evidence cell as the closure carrier) and §8.3 (all mandatory maintenance in the IndexModule) before I let the lab builder finalize those two contracts.

## Evidence ledger (2026-09-12 ~23:58 UTC)

`evidence-extractor` finished: 360-line ledger with every retained receipt for fuller `ebc7d54` (44 named operations), native `b8c2775` (54 rows), the fuller lineage 7.69M → 5.06M (six same-fixture kernels), the native source description and a §1 guarantee ledger; copied to my lab worktree at `lab-b/evidence-extract-2026-09-12.md` (branch `fable/2026-09-12-road-b-lab`, uncommitted). Two facts for the coordinator: (a) **`ebc7d54` is checked out in no worktree** (the `efs21-direct` directory is the stopped `f873890` probe), so Road A's paired experiment needs a fresh checkout before any run; (b) the "5.5M v2.1 estimate" from yesterday's chat has no retained provenance and should not appear in any ledger; Road B no longer cites it.

## Response to Codex's checkpoint (2026-09-12 23:56 UTC)

- Acknowledged: B source preparation continues in `lab-b/`; the separate mandatory IndexModule stands.
- **Withdrawn:** my suggestion to run Road C's kill test as a desk check. Agreed that linked-code share and "Store size added to the monolith" are not disqualifiers (reuse replaces code; ordinary modularization is allowed; testing needs no permanent dependency acceptance). I support the leaning: a small **B versus Store-only C matched probe with A as the richer control**; I will not argue C down on size.
- **Fixed in `road-b.md` v1.1:** (1) the signed preimage now binds the complete action vector and execution context (`PublicationIntent(realm, coreCodeCommitment, author, nonce, deadline, acceptanceProfile, indexObligations, actionsHash)`), with the same-signature mutation tests you listed; (2) the subject id is a creator-signed logical claim `keccak(creator, creatorSalt)`, salt committed in the action, so `F` keeps its identity across export/import — with that test added; (3) the listing law no longer implies a full scan for page one (bounded budget, `rawTotal`/`selectedSoFar`/`status`, selected total only at scan end). The Evidence cell is labelled "eligible to prototype, not a verified closure"; its independent-reconstruction test (re-encode actions from Admission rows, recompute the digest, verify the signature) is what would upgrade it. Receipt estimates stay marked unmeasured.
- Clock: corrected; earlier "2026-09-13" entries in this note were estimates, now replaced with actual times.
- Waiting for: the run-manifest format and the normal-limit run handoff after your source review; no lease assumed.

## Handoff of 2026-09-13 00:08 UTC acknowledged; ownership returned

- Read at `b2cd79a`: README shortlist (B + one corrected Store-only C; A retained as control/fallback), `run-manifest.md` (payload hashes `quote3000/3100`, `file41a/b`; required sections; the 198,745 receipt is a **reused-Record** update, so fresh-body, existing-body/new-occurrence and exact-retry rows will be separate, with pre-absence proved), `road-c-review.md` (five findings), `oracle-boundary.md`.
- Your answers applied: (1) B and C use the same semantic authorship closure and, where MUD's native encoding does not preserve identical evidence, the same Evidence-cell layout; layout differences reported, neither certified. (2) By-Type and by-author are in BOTH mandatory IndexModules for the first probe, marginal write costs reported separately, not an owner-ratified minimum. (3) `ebc7d54` left to you; no A build from my side.
- **Road C source preparation assigned:** specialist `road-c-mud-specialist` (the independent Road C author, separate from the B builder), owned worktree `/Users/james/Code/EFS/planning-road-c-lab` (branch `fable/2026-09-13-road-c-lab` from main `b2cd79a`), path `Reviews/2026-09-12-efs-path-decision/lab-c/`; Store pinned to release 2.2.23 = `062bd8d` (vendored `packages/store/src` bytes with MUD's MIT LICENSE, hashes recorded), `StoreRead` + `StoreCore` composition or explicitly denied/gated writers per review finding 1, mandatory IndexModule as a separate contract (finding 3), restricted World noted as out of this probe, no compile. Nothing committed on either lab branch.
- **Road B:** lab builder still preparing source in `lab-b/` with all eight deltas; on return I review, then hand you the exact commit/diff hash, manifest, bounded commands, scratch path and watchdog for review and lease.

## Pre-seal checks (2026-09-13 00:17 UTC)

`oracle-boundary.md` published at `32ed292` noted. The B reviewer's four checks — origin-qualified contract creator identity (principal = kind, realmOrigin, address; subject id derived from it; origin preserved on import), evidence reconstruction retaining realm/code domain and every signed action field with byte-identical digest recovery after cache loss/upgrade, import requiring separate destination authorization/acceptance/CAS/index effects (source signature is evidence only), and a page basis that binds index generation / rules epoch / code commitment and dedupes names across author scopes — are relayed verbatim as source-plus-test deltas to BOTH lab builders (the closure, subject id, import path and cursor law are shared). Unresolved items will be listed explicitly in each `MANIFEST.draft.json`; first compile proceeds on the lease, not on passing tests.

## Road B source-ready handoff (2026-09-13 00:38 UTC) — request for source review and a bounded compiler/Anvil lease

- **Source:** branch `fable/2026-09-12-road-b-lab`, commit `727291a` (pushed; the commit touches only `lab-b/`: 17 files changed, 3841 insertions(+); patch sha256 prefix `f7c5b79f092c7a66`; base `edb7fe4`), path `Reviews/2026-09-12-efs-path-decision/lab-b/` — 3,190 lines: `src/{Ledger,IndexModule,LensReader,TypeRegistry,Keys,Interfaces,LabHarness}.sol`, three test files (24 tests), `script/measure.mjs`, `MANIFEST.draft.json`, `README.md`, `TODO.md`. Nothing compiled or run.
- **Manifest:** `lab-b/MANIFEST.draft.json` (all eight sections; the four payload controls with their Keccak-256 values; matrix + freshness rows `contract-fresh-body` with pre-absence, `contract-existing-body`, exact retry; sdk-fixture 1–6 and files-journey J1–J3 as separate logical actions; unknowns explicit).
- **Known deviations, ruled and recorded** (`TODO.md` §B and §E): listing across a mutation reports a `mutated` flag rather than a basis-consistent page; withdraw does not tombstone heads set by the withdrawn admission (the control's `withdraw-current-Binding` row is unmatched); contract-author import retains the source principal as a claimed witness (grade 0); acceptance profile binds the registry epoch; uniform Evidence cell for both ingress kinds; EIP-170 library split pre-authorized if the runtime overflows.
- **Independent review:** a reviewer who did not author the code is checking bypass paths, ordering, signature coverage, subject identity, reconstruction, index rollback, listing law and compile risks now; findings land as a second commit before compile, listed here.
- **Bounded lease request:** commands in order from the lab directory — `forge build --use 0.8.30 --offline`, `forge test --use 0.8.30 --offline -vv` (24 tests), `forge inspect Ledger storage-layout` / bytecode size, then `node script/measure.mjs --anvil --out <scratch>/measure.json` (the script spawns `anvil --hardfork cancun --prune-history 256 --accounts 4 --gas-limit 30000000` on a free loopback port, no `--steps-tracing`, ~80 transactions, and kills it). `FOUNDRY_OUT` and all scratch under `/private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b` (< 2 GB). Expected 15–25 min including one repair cycle; script watchdog 20 min, operator kill at 30 min; stop if free disk < 50 GB; cleanup deletes only that scratch path, keeping `measure.json`. Operator: Codex by default, or me if you hand the slot over here.

## Compiler lease result and early release (2026-09-13 00:50 UTC)

- **Lease use:** build #1 at 00:46 UTC found one error (NatSpec tag) plus a shadowing warning; the independent review returned at 00:47 with **no blocker, READY-WITH-FIXES**; one repair cycle applied its fixes; **build #2 at 00:49 compiled clean** (`forge build --use <cached solc 0.8.30, sha256 738dcdc6…> --offline --threads 2`, out/cache under `/private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b`), **24/24 tests pass** (three suites, 66 ms), size check: Ledger runtime **16,699** bytes (initcode 17,075), IndexModule 4,127, LensReader 9,445, TypeRegistry 1,504 — no EIP-170 pressure. Watchdog: `perl alarm 600` per command (macOS has no `timeout`); no background process left (precise check by executable name: anvil 0, forge 0, solc 0; an earlier substring pattern self-matched its own command line and printed 32 — wrong); scratch 1.7M under the run root; logs `/private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/logs/build1.log, build2.log, test1.log`. **Slot released at 2026-09-13 00:50 UTC**, before 01:15.
- **Source pins:** branch `fable/2026-09-12-road-b-lab`, commit `dcc7b94` (pushed; patch sha256 prefix `8789f2d3e8dc328e`; concatenated `src/*.sol test/*.sol` sha256 prefix `5f43604b42f2601f`).
- **Review fixes applied:** NatSpec; ordinal guard in `IndexModule._append`; `MAX_BUDGET` cap and loop shadowing in `LensReader.list`; registry `epoch` folded into `acceptanceProfileOf`; nonce-mutation test expects `E_SIGNATURE`; `Reconstructor` labelled a candidate self-check (it calls the candidate digest; the independent oracle must not); `setIndexModule(0)` labelled a non-equivalent ablation path.
- **Deferred (ruling-level per the reviewer, not probe-blocking):** (a) grade-0 (contract-author) import currently accepts `sourcePrincipal` as claimed and mints subjects under it — a squatter could namespace under another principal; fix = retain `sourceChainId` and derive the principal; (b) replay domain: the EIP-712 domain is `(name, version)` with realmId and coreCodeCommitment inside the intent, so a signed publication replays only on a deployment with identical realm and code — pinned for the probe as "same (realm, code) = same authority domain"; the alternative (chain-bound domain, portability via `importPublication` only) is yours to rule. Also noted: 7702-delegated EOAs classify by `code.length` (dormant on Cancun); contract-author import retry has no `AlreadyAdmitted` identity.
- **Next:** the B builder is applying your three measurement-script fixes (run-owned outputs and Anvil cache path; per-cell `evm_snapshot`/`evm_revert` from one sealed initial state with recorded pre/post state incl. proved pre-absence/presence; honest "ingress × multiplicity" naming with the capability ablation kept as a later gate; the `mutated`-page and unmatched withdraw rows marked diagnostic unknowns). When landed I commit, give you the hash, and request the bounded Anvil run.

## Road C source-ready handoff (2026-09-13 00:51 UTC) — request for source review and a compiler/unit-test lease (after B's, which is released)

- **Source:** branch `fable/2026-09-13-road-c-lab`, commit `f2e4051` (pushed; 61 files changed, 14183 insertions(+); patch sha256 prefix `7e7c736559a7fde1`; base `b2cd79a`), path `Reviews/2026-09-12-efs-path-decision/lab-c/`. MUD Store vendored **byte-pinned at release 2.2.23 = `062bd8d`** (`vendor/PIN.md`, 43 sha256 rows, MIT `LICENSE`; e.g. `StoreCore.sol` `682cd5f2…`, `StoreRead.sol` `35b979b2…`, `Store.sol` `a9133966…`). Boundary per your review finding 1: `StoreRead` + `StoreCore` composition with `initialize` and internal table registration, no `IStoreWrite` exposed; README carries the full external-selector inventory and an authority manifest; `test/RawWriteDenial.t.sol` denies all 13 raw-write selectors with post-state checks. Mandatory `IndexModule` is a separate contract (finding 3) with by-Type and by-author included and `coverage()`. Shared Evidence-cell closure and origin-qualified principals as in lab-b; MUD's per-row differences reported in the README/TODO (one `Store_SetRecord` log per row, a `StoreHooks` SLOAD per write, an `EncodedLengths` slot per dynamic row; the Evidence cell is 11 slots because it retains realm/code/import fields verbatim — a layout difference to hold against B). Nothing compiled or run; all gas ESTIMATED/QUOTED/UNKNOWN.
- **Known risks (its TODO):** Ledger EIP-170 unknown until `--sizes` (decomposition plan: split `importPublication` into an authorized `Importer`); first-compile breakage across ~10k vendored lines called from reading; the coordinator's `realmOrigin = keccak(chainId, coreCodeCommitment)` makes two same-code Realms on one chain share contract principals (tests use `vm.chainId`; flagged in the manifest's `preSealChecks.1` — this applies to lab-b too); native-author portability is graded `UNVERIFIED_NATIVE`, not proven; `publishNative` treats any `msg.sender` as an origin-qualified account. The script's ethers path defaults to `/Users/james/Code/EFS/client/node_modules/ethers` (6.13.5) because the coordinator's path does not exist here; overridable by `ETHERS_PATH`.
- **Lease request (C compile + unit tests only, no Anvil):** same shape as B's — `forge build --use <cached solc 0.8.30> --offline --threads 2` with out/cache under `…/scratchpad/build/lab-c` (< 2 GB), `forge build --sizes`, `forge test -vv`; expected 10–20 min including one repair cycle; `perl alarm 600` watchdog per command; operator: me, at a slot you record.

## Road B bounded Anvil-run request (2026-09-13 00:52 UTC)

- **Source:** `fable/2026-09-12-road-b-lab` at `369b770` (pushed; patch sha256 prefix `86f06d8ef9d7086f`): `dcc7b94` (green build/tests) plus the three measurement-control fixes you required — run-owned `FOUNDRY_OUT`/scratch/`--cache-path`; `evm_snapshot`/`evm_revert` to one sealed initial state before every cell with recorded pre/post probes (the four control Records absent, nonces, counters, scope/by-Type/by-author heads) and explicit pre-absence / pre-presence / post-presence rows around `contract-fresh-body`, `contract-existing-body` and the exact retry; cells named `native-one`, `signed-one`, `native-two`, `signed-two` ("ingress × multiplicity, NOT the capability ablation"), no interaction term, the ablation an explicit `{unknown, consequence}`; the `mutated`-page and withdraw-current rows are `diagnostic: true` with "a mixed-basis page must not be treated as COMPLETE". Disclosure: the builder ran `node --check` (parse-only) on the script; no execution, no chain. Verified from `anvil --help` (1.7.1): `--hardfork`, `--prune-history` and `--cache-path` are all accepted, so the run needs no flag fallback.
- **Exact run:** from the lab directory, `FOUNDRY_OUT=<R>/out EFS_LAB_SCRATCH=<R> EFS_ETHERS_PATH=<ethers> node script/measure.mjs --anvil --out <R>/measure.json` with `<R> = /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b` (already holds the green `out/`) and `<ethers>` = `/Users/james/Code/EFS/planning-fable-files-browser/Reviews/2026-09-04-mvp-rehearsal/node_modules/ethers` (6.15.0; the `planning` main checkout has no node modules, `client/` has 6.13.5). The script spawns `anvil --hardfork cancun --prune-history 256 --accounts 4 --gas-limit 30000000 --cache-path <R>/anvil-cache` on a free loopback port (no steps tracing), deploys, runs the matrix + freshness controls + failure rows + a without-index diagnostic pass (~80 transactions, each cell from the sealed snapshot), records Anvil `pid/port/argv/startedAt/stoppedAt`, and kills it; expected 2–4 min. Watchdog: script kills Anvil at 20 min; I wrap the command in `perl alarm 1500`. Scratch stays under `<R>` (< 2 GB), cleanup deletes only `<R>/anvil-cache` and `<R>/cache`, keeping `measure.json` and `out/`. Operator: me (this session) at the slot you record, or you.
- **Deliverable back:** `measure.json` (receipts per row with pre/post probes), the size table already reported, and the manifest updated with actual hashes/receipts; unresolved items stay listed.

## Serial lease progress (2026-09-13 01:00 UTC)

- **Implementation handoff (00:55) read and adopted as experiment choices** for both labs' post-measurement repair: replay bound to source chain + Ledger deployment as well as realm/code/rule context (so B's realm-bound `(name, version)` domain is superseded; B keeps its pinned source for the diagnostic run and takes this in the next repair); native identity qualified by original chain/Realm deployment + account, not codehash alone (this also resolves the same-code same-chain collision both labs flagged); grade-zero native import retained as attributed evidence only, never minting or writing as that principal, reported as unsupported until a witness is verified; exact-Type descriptor/activation gap recorded as a finalist gap, not a saving.
- **C build #1** (`f2e4051`, lab-c scratch, watchdog 600 s): two config parse failures first (`gas_reports` declared as a table; now `["*"]`), then the compiler ran (26 s) and failed: via-IR **stack too deep in `Ledger.importPublication`** with "No memoryguard was present" — the vendored Store has 273 of 286 `assembly { }` blocks without the memory-safe annotation, which disables the stack-to-memory mover for every contract inlining them. Repair in progress in OUR code only (vendor stays byte-pinned): restructure the import path and other local-heavy functions; the same repair cycle applies your denial-test corrections (table-correct inputs, permissive positive control, metadata-unchanged checks, zero/no-code attach rejection, one-shot/foreign writers, reciprocal address/codehash seal; the denial tests are no longer described as proof of unreachable writes) and replaces the TODO's Anvil plan (fixed port 8545, no bounded history) with lab-b's controls.
- **B diagnostic run:** the B builder is applying your measurement fixes (persist tx/calldata/receipt/block hash/raw getter bytes before `evm_revert`, partial output on failure, fresh provider per cell, Consumer pre-state and read-back, PRESENT wrong-Type target with expected error and unchanged-state checks, honest header naming the joined Pair/Quote gate and the diagnostic Reconstructor). The B run starts only after C's compile/tests/size complete, never simultaneously.
- Logs: `…/scratchpad/build/lab-c/logs/build1.log, build1b.log, build1c.log`.

## B run pin updated (2026-09-13 01:04 UTC)

- The B diagnostic run will execute `fable/2026-09-12-road-b-lab` at **`6104a6f`** (pushed; patch sha256 prefix `bdbc0024b3afa07d`): `369b770` plus your measurement review (per-cell persisted tx/calldata/receipt/block hash/logs and raw getter bytes before each `evm_revert`, incremental `measure.json` with partial output on failure, fresh provider per cell with explicit block tags and the post-revert block recorded, Consumer pre-state and field-by-field read-back, present wrong-Type target plus expected/observed selector and unchanged-state checks on every failure row, honest header with the two verbatim remaining-gate lines). `src/` and `test/` are unchanged since the green build; the compiled `out/` is reused.
- Queue: C compile/tests/size remain first. If the C repair is not back by ~01:12 UTC, I will run the B diagnostic (≈4 min, no C activity, never simultaneous) so the slot is not wasted, and record the reordering here; otherwise C first as recorded.

## Road C independent review (2026-09-13 01:05 UTC) — READY-WITH-FIXES, one blocker

- **BLOCKER (forwarded to the C repair):** `importPublication` accepts a native-source packet with no source check and applies its actions under the claimed source author, so a self-authorised importer could rebind another principal's heads; fix per your handoff — native-source imports become attributed evidence only, never authorised writes (UNSUPPORTED in this probe), plus a spoof-via-import test.
- **Majors:** (1) `coreCodeCommitment = address(this).codehash` embeds immutables, so identity was already deployment-bound by accident; per your handoff it becomes deployment-bound **by design** — `realmOrigin = keccak(chainId, Ledger address)`, codehash retained only as execution evidence; docs and the two-Realm test rationale rewritten. The same holds for lab-b (its B reviewer noted the codehash embeds the admin/registry immutables) and will be applied there in the post-measurement repair; B's pinned source is unchanged for the diagnostic run. (2) The C reconstruction test uses the candidate encoder → labelled self-check, oracle re-derivation owed. (3) low-`s`/`v` guard before every ecrecover. (4) EIP-170 risk for the Store-inlined Ledger — `forge build --sizes` runs first.
- **Passes with evidence:** constructor-only Store initialisation and table registration, no `IStoreWrite`/registration selector reachable, no fallback/receive, all 13 denied selectors recomputed independently; validations before the first write; the only `try/catch` cannot swallow; acceptance via bounded STATICCALL; closure parity with lab-b (typehash, ten-field action tuple, nonce/deadline/retry); IndexModule separate with MUD's 40-bit length guard on every push; listing law; all StoreCore call signatures match vendored 2.2.23; no offchain table; four control hashes match.
- Reviewer's verdict: compile may start; the measured run waits on the blocker and the identity/self-check/low-s fixes. C stays uncompiled until the specialist hands back; if that is not by ~01:12 the B diagnostic runs first (see above).

## Serial lease result and release (2026-09-13 01:18 UTC)

- **Road C:** repair cycle applied (stack discipline in our code, vendor untouched; the review's blocker and majors; denial tests with a permissive `OpenStore` positive control; bounded Anvil controls in TODO). Build attempts under the lease: #1 config parse (`gas_reports`), #1c via-IR stack too deep, #2a reserved identifier `sealed`, #2c file-level doc block + `EncodedLengths` wraps, **#2d compiles** — but `--sizes` fails: **Ledger runtime 25,032 bytes (456 over EIP-170)**, IndexModule 10,303, LensReader 10,585, `OpenStore` (test control) 15,757, QuoteConsumer 1,386. Tests were attempted **diagnostically with `--code-size-limit 30000` in the test EVM only** and gave **no semantic signal**: both test contracts fail in `setUp` with "create initcode size limit" (EIP-3860, 49,152 bytes) — the test contracts themselves (`LabBase` helpers plus inlined tables) are too large, independent of the Ledger's runtime size; 0 tests executed Source committed and pushed: `fable/2026-09-13-road-c-lab` **`bc6beee`**. Next for C: ordinary decomposition of `Ledger` (the TODO's trade a/b/c; the vendored `StoreRead` externals and inlined `StoreCore` write path are the bulk) before any Anvil.
- **Road B diagnostic run:** started 01:05 UTC at `6104a6f`, reordered ahead of C while C was being repaired; setup persisted (12 setup transactions), then the script **hung at block 19 after two persisted cells** for 10 minutes with Anvil responsive and no console output; killed at 01:16:41 (node 25963, anvil 25989). Partial report retained as `…/build/lab-b/measure1-partial.json` (setup receipts, two cells with their persisted transactions and raw bytes). Diagnosis in progress (suspect: wallet nonce/pending-state cache across `evm_revert` with the per-cell provider); no receipt figures are quoted from this run.
- **Lease released 2026-09-13 01:18 UTC**; no anvil/forge/solc processes remain; scratch: lab-b 1.8 MB, lab-c under the same parent, both far under budget; logs under each lab's `logs/`.

## B hang diagnosed; new bounded lease requested (2026-09-13 01:26 UTC)

- **Cause (from `measure1-partial.json`):** setup and the first two cells completed correctly in 5 s (reverts to the sealed block, chained snapshot ids used, explicit nonces re-read after revert, 77 raw reads harvested per cell); the third cell's fourth transaction (the consumer read with a two-address lens) never yielded a receipt: ethers v6 `waitForTransaction` checks once, then waits for a *new* block event; Anvil had already mined the block, and with the script as the only writer no later block came. Timing-dependent (15 transactions won the race, the 16th lost). Nonces and snapshot ids are refuted as causes. A second bug: consumer read-backs were flagged as mismatches by a string-versus-number comparison while the stored targets/values/revisions were correct.
- **Fix (script only, `src/`/`test/` untouched):** explicit nonces per transaction (fresh tracker per cell, re-read from `latest`), 100 ms receipt polling with a 30 s cap that throws with the pending hash, nonces and `txpool_content`, unbuffered progress lines per row/transaction, `anvil_getAutomine` asserted after every revert with an empty-pool check, in-progress cells persisted before their body and after every mined transaction, normalized read-back comparison. Commit **`a16d7d4`** (pushed; patch sha256 prefix `e8bbdcc1ca835d21`).
- **Lease request (B diagnostic, ready now):** same command and scratch as recorded, at `a16d7d4`; expected under 3 minutes (the persisted part ran in 5 s), `perl alarm 900` outer watchdog, script's own 30 s per-receipt cap, no traces; log via `tee` to `…/build/lab-b/logs/measure2.log`. Operator: me, at the slot you record. C build #3 (after its decomposition returns) can follow in the same serial slot or a later one.

## C decomposition ready; combined lease request (2026-09-13 01:29 UTC)

- **Road C at `731200d`** (pushed; patch sha256 prefix `b5d76db22ae07a6d`): Ledger decomposed by ordinary modularization with no privileged path — the import path is an **external `ImportLib`** (delegatecall, no storage, address linked immutably into Ledger bytecode and recorded as deployment identity in `MANIFEST build.linkedLibraries`; one delegatecall per import charged as a cost row), the action pipeline an inlined `ActionLib`, errors/events shared; blocker fix, reciprocal seals and low-s guard intact. Test helpers now live in deployed `Fixture` and `DenialProbe` contracts and the fixture tests are split into `Publish`, `Selection`, `Import` suites so no test contract exceeds the initcode limit. ESTIMATED sizes: Ledger 18–20 KB, ImportLib 15–18 KB (unmeasured). First-compile risk noted: `calldata` struct params on the external library function.
- **Lease request, one serial slot:** (1) **C build #3**: `forge build --sizes`, then `forge test -vv` with normal limits, lab-c scratch, `perl alarm 480/300`, ≤2 threads, one repair cycle allowed; (2) **B diagnostic run** at `a16d7d4` as requested above (expected < 3 min). Never simultaneous; I operate both; PIDs, start/stop and logs recorded here.

## Lease result (2026-09-13 02:00 UTC): C build #3 and the first B diagnostic; lease released

**Road C (`731200d` + my two compile slips fixed: `sealed` → `sealedCodehash` in `LedgerErrors.sol`, probe builders `view`):** `forge build --sizes` — **Ledger 23,145 B runtime (1,431 under EIP-170), ImportLib 19,861, IndexModule 10,303, LensReader 10,585, DenialProbe 7,560, OpenStore 15,757, QuoteConsumer 1,386**; the `--sizes` gate still fails only on the test helper `Fixture` (88,623 B runtime / 166,188 B initcode), so no C test has executed; the specialist is splitting that helper into deployable pieces and applying your attach-context fix (source only). Logs `…/build/lab-c/logs/build3*.log`.

**Road B diagnostic (`a16d7d4`, 01:59:09–01:59:21 UTC, Anvil pid 47156 port 50752, run-owned cache, no traces; outer watchdog `perl alarm 900`, exit 0, then verified anvil/forge/node 0):** evidence committed on the lab branch as **`322b320`** (`lab-b/evidence/measure2-a16d7d4.json`, the partial first run, the log, `SHA256SUMS`). Field-level checks, not exit code: 11 cells, `failure: none`, 6 failure rows with **0 selector mismatches and 0 state changes**, **29 consumer read-backs, 0 mismatches**, freshness rows with proved pre-absence/pre-presence, no-index pass labelled non-equivalent.

MEASURED receipts (diagnostic; ingress × multiplicity; NOT same-guarantee rows against the fuller/native controls — labels are hashes only, joined Pair/Quote consumption absent, Reconstructor is a self-check):

| row (32-byte quote unless noted) | native | signed |
| --- | ---: | ---: |
| create (subject + record + head + placement, mandatory index incl. by-Type/by-author) | 1,216,793 | 1,261,965 |
| edit (new record + head rebind) | 511,898 | 558,598 |
| second author's competing revision | 837,107 | 882,989 |
| create, 41-byte binary | 1,231,495 | 1,276,673 |
| paid consumer read: resolve / list / as-of history | 137,883 / 97,678 / 71,905 | 137,626 / 97,156 / 71,636 |
| resolve under second-first lens (two authors) | 53,712 | 53,431 |
| contract-fresh-body (proved absent) / existing-body (new occurrence) / exact retry (reverts) | 496,049 / 314,527 / 53,756 (status 0) | — |
| create with the index module detached (**non-equivalent diagnostic**) | 801,359 | — |

Reading: the mandatory index share of a create is ≈415k of 1.22M; signed ingress costs ≈+45k over native; the two-author competing create is ≈0.84M. Nothing here is compared to the 5.06M / 627,672 controls as equivalent; the label-retention gap you named (hash-only names, empty placement bodies) is now a listed gate and a design task (`lab-b/LABELS.md`, in progress), and the SDK public-profile supplement (`lab-b/PROFILE.md` + `vectors/profile-b.json`) is being written from the pinned source.

**Lease released 2026-09-13 02:00 UTC**; no heavy process remains; scratch lab-b 1.8 MB / lab-c under the same parent.

## SESSION LIMIT HIT (2026-09-13 02:06 UTC) — state handover

- The account's session rate limit was hit (HTTP 429 "session limit · resets 11:20pm America/Chicago" = **04:20 UTC**); the Road C specialist was terminated mid-task and further Claude calls from this task may fail until the reset. **No lease is held; no anvil/forge/solc process is running.** Everything below is on disk and pushed except the uncommitted working-tree files listed.
- **Road C** (`fable/2026-09-13-road-c-lab` at `731200d`, pushed): deployables fit (Ledger 23,145 B). Uncommitted local fixes in the worktree: `sealed` → `sealedCodehash` (LedgerErrors.sol), probe builders `view`, `EncodedLengths.wrap` in the permissive controls. **Remaining before any C test can run:** the test helper `Fixture` (88,623 B) — root cause diagnosed by the specialist before termination: any helper that does `new Ledger` / `new IndexModule` / `new LensReader` embeds their creation code; the fix is a test-only `Deploy` helper that fetches artifacts with `vm.getCode`, links the `ImportLib` placeholder from the artifact's `linkReferences`, and creates via a small memory-safe assembly block, then five small helpers (`FixtureRealm`, `FixtureBuilders`, `FixtureSeeder`, `FixtureExport`, `FixtureActors`) with every attach in the denial tests performed through `FixtureRealm` (the module's deployer); nothing of that was written yet. Also owed: `lab-c/LABELS.md` (cold-name gap). Working tree now: M Reviews/2026-09-12-efs-path-decision/lab-c/foundry.toml
 M Reviews/2026-09-12-efs-path-decision/lab-c/src/LedgerErrors.sol
 M Reviews/2026-09-12-efs-path-decision/lab-c/test/DenialProbe.sol
 D Reviews/2026-09-12-efs-path-decision/lab-c/test/Fixture.sol
 M Reviews/2026-09-12-efs-path-decision/lab-c/test/Import.t.sol
 M Reviews/2026-09-12-efs-path-decision/lab-c/test/LabBase.sol
 M Reviews/2026-09-12-efs-path-decision/lab-c/test/Publish.t.sol
 M Reviews/2026-09-12-efs-path-decision/lab-c/test/RawWriteDenial.t.sol
 M Reviews/2026-09-12-efs-path-decision/lab-c/test/Selection.t.sol
 M Reviews/2026-09-12-efs-path-decision/lab-c/test/Vm.sol
?? Reviews/2026-09-12-efs-path-decision/lab-c/test/Deploy.sol
?? Reviews/2026-09-12-efs-path-decision/lab-c/test/FixtureActors.sol
?? Reviews/2026-09-12-efs-path-decision/lab-c/test/FixtureBuilders.sol
?? Reviews/2026-09-12-efs-path-decision/lab-c/test/FixtureExport.sol
?? Reviews/2026-09-12-efs-path-decision/lab-c/test/FixtureRealm.sol
?? Reviews/2026-09-12-efs-path-decision/lab-c/test/FixtureSeeder.sol.
- **Road B** (`fable/2026-09-12-road-b-lab` at `322b320`, pushed, evidence retained): the B builder was writing `lab-b/PROFILE.md` + `lab-b/vectors/profile-b.json` (public profile supplement for the checker, from the pinned `a16d7d4` source) and `lab-b/LABELS.md` (label-retention experiment design); whether it completed before the limit is visible in the worktree listing: ?? Reviews/2026-09-12-efs-path-decision/lab-b/LABELS.md.
- **If this task is unavailable until 04:20 UTC:** Codex may proceed with A/C reviews and the checker; the two lab branches are self-describing (README/TODO/MANIFEST in each); any Claude session can resume from the pushed commits plus the uncommitted files above.

## Resumed (2026-09-13 04:23 UTC) after the quota pause — ownership respected

- Read the Coordinator checkpoint at main `1cce62c` and both worktrees first. Acknowledged: Codex completed the two handover scopes during my pause (C harness split `774dfcd`, 37/37 tests under normal limits; B public profile `885d9f9`, verified against `dcc7b94`; branch heads `7876477`/`89d3d66` and `2824297`), owns C's measurement runner in the C worktree, and holds the **compiler-only lease 03:53–04:35 UTC**. I start no build and touch no `lab-c/` file. SDK's signature-binding extension `0e6e682` noted; not duplicated.
- **My next bounded scope (Road B, source only, new specialist, no runs):** (1) close the measurement gap you named — retain the sealed-baseline raw getter bytes per cell (Record rows for the four controls, Ledger evidence/admission/head/nonce/counter replies, IndexModule heads/words) BEFORE each cell's first transaction, with exact request/response correlation, so pre-absence/pre-presence are independently established rather than asserted by the script; (2) add the typed joined Pair/Quote journey (sdk-fixture steps 1–6: ITEM_ETH, ITEM_USDC, PAIR with two checked references, QUOTE_A1/A2 signed, QUOTE_B1 from the real producer, three lenses) with a stateless test-only measurement consumer exposing paid point and list methods with explicit result commitments that check the Pair reference and author/evidence context; kept separate from the supplemental 32-byte diagnostic; (3) the label probe exactly as your review specifies — Ledger unchanged, one fixed lab Label Type registered in common setup, hash-only create versus the same batch plus a Label PUBLISH for exact ASCII `entry`, fresh / existing republished / existing omitted, paid consumer retrieval with hash check, labelled a client-convention baseline and not mandatory Files semantics. `LABELS.md` stays an unadopted proposal with your five corrections applied.
- Your four qualifications of the first B run are all in that scope: (i) raw Record/Ledger/Index pre-probes retained as bytes with request/response correlation; (ii) each freshness row becomes its own sealed cell from the same post-setup snapshot (no sequential state, no subtraction into a "premium"); (iii) paid reads measured with a stateless consumer that writes no storage, and the storing Consumer's storage component reported separately; (iv) an as-of history read at a strictly older basis (after QUOTE_A1 while QUOTE_A2 exists) in addition to `latest`. The manifest will state that every B row remains a hash-placement diagnostic, not a matched substitute for the fuller Files control, until labels and the joined journey are in.
- A B chain lease will be requested here only after that scope is source-ready and reviewed, and never while the C lease is active.

## B next scope source-ready (2026-09-13 05:08 UTC) — review before any lease

- Commit **`691e341`** on `fable/2026-09-12-road-b-lab` (pushed; 9 files changed, 2091 insertions(+), 443 deletions(-)); Core contracts unchanged at `dcc7b94` (verified by diff). New: `src/JoinedConsumer.sol` (stateless `JoinedConsumer` + a stateless twin of the storing `Consumer`), `src/LabAcceptors.sol` (`QuoteAcceptor`, `LabelAcceptor`), `test/JoinedConsumer.t.sol` (5), `test/LabelType.t.sol` (3); `script/measure.mjs` rewritten (1,106 lines): no ethers provider — locally signed raw transactions with the literal JSON-RPC envelopes of every call retained (run-unique `rpcId`, method, params, result, block header), sealed-baseline raw replies before each cell's first transaction, three freshness rows as separate sealed cells with one action shape, stateless paid reads labelled STATELESS/STORING, as-of reads at a strictly older basis and at latest, the joined Pair/Quote journey (steps 1–6 with the fixture's exact values, `QuoteAcceptor` as the mandatory rule, three lenses, folder and tag pages, step-6 move/replace/remove/restore), and the label probe cells exactly per your review; `LABELS.md` carries the five corrections and stays unadopted; manifest lists the exact cell names and the "no matched substitute" statement.
- An independent read-only review of this commit is running now (bypass/ordering/oracle-boundary/format checks as before). I will request the serial compile + chain lease only after its findings are applied, and not while any C lease is active.

## Review of `691e341` (2026-09-13 05:19 UTC) — READY-WITH-FIXES, no blocker

- PASS with evidence: Core unchanged (empty diff against `dcc7b94` for the six pinned files); baseline raw replies are literal JSON-RPC envelopes with block hashes from retained headers, taken before each cell's first transaction, covering the four controls and every touched key, and their shape matches the checker's `rpc-observed.mjs` (`cells[...]`, `raw[]`, `transactions[]`, `afterRevert`, `sealedInitialState`); freshness rows are three sealed cells from one snapshot with one action shape and pre-presence retained as bytes; the joined consumer is storage-free and commits over (pairId, mantissa, scale, authorKind, lensId, basis) plus evidence via public getters; older-basis as-of rows assert the older revision; the label probe matches your review's spec; fixture values and payload hashes exact; the compile risks I listed are, on desk inspection, legal at 0.8.30.
- **MAJOR (being fixed):** the joined consumer imports the candidate's `Keys` library to derive the label Record id; fix = pass the id in and rely on the self-certifying checks. **MINORs (being fixed):** header/receipt block-hash assertion in `send()`, no `?? 0` fallback for relative ordinals, drop the emitted `report.unrun`, watchdog in `main()`, readiness-probe id, two LABELS.md phrasings, TODO §D superseded by §G.
- After the fixes land I will commit, post the hash here and request the serial **compile + chain lease for B** (compile new consumer/acceptor sources and the eight new tests, then the measurement run: ~170 transactions, 25-minute script watchdog, outer `perl alarm 1800`, run-owned scratch, no traces), not before the C chain lease (through 05:25 UTC) has ended.

## B lease request (2026-09-13 05:24 UTC) — compile + chain, serial, after the C chain lease

- **Source pin:** `fable/2026-09-12-road-b-lab` at **`e77f36d`** (pushed; patch sha256 prefix `89ac25eeab85d573`): `691e341` plus the review fixes (consumer derives nothing — label Record id is an argument, self-certifying checks kept, new negative test; header/receipt block-hash assertion; no defaulted ordinals; `report.unrun` removed; watchdog in `main()`; frozen checker slots read at the receipt block, the rest one mined block later; docs aligned). Core contracts unchanged at `dcc7b94`.
- **Compile stage:** from the lab directory, `forge build --use <cached solc 0.8.30, sha256 738dcdc6…> --offline --threads 2 --sizes --out /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/out --cache-path /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/cache` (new: `JoinedConsumer.sol`, `LabAcceptors.sol`; normal runtime/initcode limits), then `forge test … -vv` (32 tests: the 24 green ones plus `JoinedConsumer.t.sol` 5 and `LabelType.t.sol` 3); `perl alarm 480/300`; one repair cycle if the new tests need it. Expected ≤ 10 min.
- **Chain stage (same serial slot, only after compile/tests):** `FOUNDRY_OUT=/private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/out EFS_LAB_SCRATCH=/private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b node script/measure.mjs --anvil --out /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/measure3.json 2>&1 | tee /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/logs/measure3.log` — the script spawns one Anvil on a free loopback port with `--hardfork cancun --prune-history 256 --accounts 4 --gas-limit 30000000 --cache-path /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/anvil-cache`, no traces, ~170 transactions across 18 sealed cells (matrix, three freshness cells, failure rows, `joined/steps-1-6`, four label cells, one non-equivalent no-index diagnostic), every observation retained as a literal JSON-RPC envelope; script watchdog 25 min, outer `perl alarm 1800`, Anvil PID/port/start/stop recorded, killed by the script and verified by me. Scratch under `/private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b` (< 2 GB), cleanup deletes only `anvil-cache` and `cache`, keeping `measure3.json` and `out/`.
- Deliverable back: `measure3.json` + log committed under `lab-b/evidence/` with `SHA256SUMS`, the manifest updated with actual hashes, and the results reported as receipt diagnostics with the joined and label rows named separately; no claim of matched parity.
- Operator: me, at the slot you record; never simultaneous with any C process.

## Acknowledgement of the 06:50 checkpoint (2026-09-13 06:55 UTC)

- Read main `9eb3e0d` and the new checkpoint. The B run I requested at `e77f36d` was completed by root on the isolated branch `codex/efs-warroom-b-run` (`5960336`, source `df23bbbb`, 18 cells / 173 transactions, 32 Forge + 11 runner tests, packet independently reconciled: 2,675 raw calls, 94 consumer checks, 14 deployments). **I will not rerun it or the completed C run (`324e7c4`).** Results read from that branch's `lab-b/measurement-results-20260913.md` (diagnostic, not parity): native/signed hash-keyed quote create 1,216,793 / 1,261,965; paid stateless point/list/older-history 51,147 / 89,121 / 66,112; joined signed A1 (subject + typed quote + head + placement + tag) 1,608,502, A2 edit 654,289, genuine contract-authored B1 791,814, joined paid point 123,047 / 123,278, folder / folder-and-tag list 101,953 / 112,471, move / remove / restore 572,901 / 279,157 / 336,765; create with fresh printable label 1,422,724, with a retained label republished 1,292,309, not republished 1,182,569 (label admission by another author 497,039 charged separately); all 14 deployments 13,246,072; Ledger 16,699 B. The results file's qualifications stand (label retrieval is a candidate-derived convention; stateless readers still carry call/event overhead; RPC-observed evidence, no state proof).
- **Selective intake:** the four reviewed runner repairs (`f58fc72` consumer readback on the receipt basis, `d600d37` fail on consumer mismatches, `155df3a` harden failure evidence, `df23bbb` label test string) are cherry-picked onto `fable/2026-09-12-road-b-lab` as `e5c5717`, `ef83063`, `78a9e03`, `fdd837e` (script, its tests, manifest, one test string only; intake head **`fdd837e`**, pushed); no Core change. Given the clock (specialist started 06:53), source readiness plus review will not land by 07:05; I will request a new finite slot when the repair is reviewed rather than start late inside this one.
- **New bounded scope accepted** (native-import/source-origin authority; exact Type identity versus separately changing Realm acceptance policy): a bounded specialist is writing the falsifying tests first (grade-0 native-source import minting/controlling a subject under a claimed principal; Type re-registration changing shape/ref/rule under one Type ID; no separation between identity and Realm policy), then the minimal repair (fail-closed unsupported native import as a temporary limit, not a waiver; Type identity committed to its exact descriptor with re-registration refused; a separate append-only Realm acceptance policy with per-admission recorded basis; signatures bound to the active policy epoch), tests, and a `REPAIR.md` stating changed profile/source pins and effects on historical interpretation, signatures/import and costs. An independent review follows before any slot use.
- **Slot 07:05–07:40 UTC:** conditional on source readiness plus that review. If not ready by 07:05 I continue light work and request a new finite slot here; I will not start any heavy process outside a recorded lease, and never alongside another.

## Falsify-then-repair delivered (2026-09-13 07:21 UTC); slot 07:05–07:40 not used

- **Falsification record `71e689b`** (against `e77f36d`, Core `dcc7b94`, desk-checked, unrun): **F1 UNSAFE** — a grade-zero (`src.v == 0`) import packet is checked nowhere (`Ledger.sol:266-272`), `p.creator` is the claimed source principal (`:276`), `Keys.subject` mints under it (`:598`): an unrelated importer with its own destination signature (or an unrelated contract claiming the producer's origin-qualified principal) mints and controls a subject under another principal, the lens resolves it FOUND, and the real principal's later create is squatted (`E_SUBJECT_EXISTS`). **F2 SAFE** — the signed import path rejects other-principal claims, relabelled realms, missing destination authorization and source-intent replay via `executeSigned`. **F3 UNSAFE** — `TypeRegistry.register` overwrites shape/refs/rule in place (`TypeRegistry.sol:42-55`) and the Ledger reads the current rule on every admission, so an admitted record is reinterpreted under the new rule. **F4 UNSAFE** — no policy table, no history, no per-admission basis; a rule activation is only expressible as an overwrite.
- **Repair `aaecfed`** (14 files, +841/−76, uncompiled): `importPublication` reverts `E_SOURCE_UNSUPPORTED` for native-source packets before any write (temporary prototype limit; signed path byte-identical; no portability waiver, no invented witness); `typeId = keccak(DOM_TYPE, shape, keccak(refTypes), ruleId)` with re-registration under an existing id refused (`E_TYPE_EXISTS`) and identical descriptors giving identical ids across registries; a separate append-only Realm acceptance policy (`activate`) with the admitting policy row packed into `AdmissionRow.meta` (no new slot) and exposed as `acceptanceBasis(ordinal)`; a policy activation stales unsent signatures; import re-runs the destination's current policy and retains the source basis. Tests adapted plus seven new (39 expected). `REPAIR.md` carries changed-source pins, effects (historical interpretation per admission; derived Type ids change `actionsHash`/profile/digest for the same fixture; the `5960336` packet stays interpreted under `dcc7b94` — no retrofit) and ESTIMATED cost deltas (register +2 fresh slots; activate 2 fresh + 2 rewrites; admission unchanged; grade-zero import strictly cheaper). Pinned `PROFILE.md` untouched except a marked "changed after dcc7b94" section; `vectors/profile-b.json` remains the `dcc7b94` vector.
- **Running:** an independent read-only review of `aaecfed` (falsification validity, no remaining path for an unverified claim to mint/control a subject, no in-place identity mutation, packing collisions, honesty of effects, compile risks) and the runner adaptation to derived Type ids plus the three new receipt rows (policy activation with an old-epoch signature rejected; refused re-registration; unsupported native import).
- **Slot:** 07:05–07:40 UTC lapses unused (source was not review-ready by 07:05, as predicted). I will request a new finite slot here once review fixes are in: compile + 39 tests, then one targeted receipt run of the three new cells plus the matrix rows needed to show the derived-id profile's cost, same limits/watchdogs/scratch as before.

## Independent review of `aaecfed` (2026-09-13 07:31 UTC) — READY-FOR-COMPILE, no blocker

- **Falsifications confirmed valid** against `e77f36d` (every cited line re-verified with `git show`): F1a/F1b unsafe, F3 unsafe, F4 unsafe, F2 safe with the replay-domain gap correctly scoped out.
- **Repair checks all PASS with evidence:** native-source import reverts before any write and retains nothing (`Ledger.sol:281`, only a memory hash computed); signed branch byte-identical; no other path lets an unverified claim mint or control a subject (native entrypoints use the realm-qualified `msg.sender` principal; signed paths require a signature; a same-address contract on another Realm has a different origin-qualified principal; signed import enforces `sourcePrincipal == principal(author)`). Type id commits to shape, ordered reference Types and rule id; re-registration refused; the only writer of identity is `register` (`setBindingRefType` is placement policy, not descriptor); policy rows append-only and 1-based; the admitting row is packed at bits 152–167 with no collision (existing fields end at 148); `acceptanceBasis` reads the retained row, never today's policy; a stale-epoch signature fails `E_INTENT(3)`; import re-runs the destination policy and retains the source basis. Effects and ESTIMATED costs honest; pinned profile untouched; retrofit of `322b320`/`5960336` forbidden in the docs; scope discipline kept; compile risks only warnings plus the via-IR stack in one large test.
- **Minors** (tidy-ups landing with the compile pass, assigned): shadowing renames, a dead `uint16` guard, `StrictQuoteAcceptor` moved into `src/LabAcceptors.sol`, registration checks that reference Types exist, stale "uncommitted" wording replaced by the SHAs, an "and immutables" caveat, and a TODO note on 7702 code-bearing EOAs (not a squat).
- **Runner** is the gate for a receipt run (not for compile) and is being adapted now (derived Type ids from `register`'s return / `typeIdOf`, consumer deployed after registration, new rows: policy activation with an old-epoch signature rejected and `acceptanceBasis` raw replies, refused re-registration, unsupported native import).
- **Runner patch committed `3c6947d`** (2026-09-13 07:31 UTC, pushed; script/docs only, Core at `aaecfed`): Type ids resolved from each registration's `TypeRegistered` log and asserted equal to the registry's `typeIdOf` reply and to a local derivation over the deployed acceptor's retained runtime codehash, in dependency order (PAIR ← ITEM, QUOTE_J ← PAIR); a guarded proxy refuses any cell reading an unresolved id; the joined consumer deploys after registration; `StrictQuoteAcceptor` (rule v2) deployed at setup with its code identity retained; three new sealed cells `policy/activate`, `failure/refused-re-registration`, `failure/unsupported-native-import` (two paths; `subjectCreatedAt` and `sourceEvidence` probes show nothing minted). The tidy-ups land next; then the finite slot request (compile + 39 tests + one targeted receipt run of the three new cells plus the matrix rows needed to price the derived-id profile).

## Acknowledgement of the 07:50 instructions (2026-09-13 07:49 UTC)

- **New slot 08:00–08:30 UTC accepted:** compile/tests only, on a recorded committed pin that has passed review; two workers, the pinned offline solc 0.8.30, normal limits, run-owned scratch under `…/scratchpad/build/lab-b`, `perl alarm` watchdogs, prior disk budget; no Anvil, no receipt run; expired reservations not reused. Root is reviewing `3c6947d` and its runner; I will not start the chain stage.
- **F5 (assessed, confirmed by construction):** in `aaecfed` the registration-time acceptor is pinned into the Type id (`ruleId`) but `_applyPublish` runs only the *active* policy acceptor; `activate(typeId, address(0))` was deliberately allowed as "policy weakening visible, not hidden" — which means it replaces the only validator, so a body the declared rule rejects can later be admitted under the same Type ID. Retaining the old acceptance basis records the change but does not prevent it. **Repair in progress (same specialist, after the tidy-ups):** the mandatory predicate (registration-time acceptor, address + codehash re-verified per call) is always run and its rejection is final; `activate` installs an *additional* policy acceptor that can only add constraints (both must accept); activation to zero means "no additional policy", never "no validation"; signatures fold mandatory rule id + active policy codehash + epoch; `acceptanceBasis` records both. Limit stated, not solved: a codehash does not pin an acceptor's mutable dependencies — the lab acceptors are pure; a production rule must be pure or have its dependency closure pinned. Falsifying tests first (rejected body admitted after activation to zero and after a permissive activation), then the repair tests (still rejected; policy can add but not remove constraints).
- **Tidy-ups:** status requested from the specialist; if they are not committed and reviewed by 08:00, the slot compiles the reviewed pin `3c6947d` (Core `aaecfed`, READY-FOR-COMPILE) and the tidy-ups/F5 compile in a later finite slot.

## F5 addendum and clock (2026-09-13 07:52:39 UTC)

- **MockAcceptor regression (from the independent review) folded into the F5 repair:** `LabHarness.MockAcceptor` carries mutable `mode`/`minBody` under an unchanged codehash, so it is not a pure intrinsic validator; mandatory fixture rules become stateless or immutable-configured acceptors, the mutable mock is only installable as explicit additional Realm policy via `activate`, a regression test covers it, and the docs will say stateful developer rules remain allowed when their declared dependency/basis semantics are explicit — no claim that production requires pure callbacks.
- **Clock:** actual `date -u` is used everywhere in this note; the slot job is gated on the real clock (it sleeps until 08:00:00 UTC before invoking forge) and records the actual launch and end times, not the slot label. Compile-only, no Anvil.

## Runner review NO-GO acknowledged (2026-09-13 07:56:14 UTC)

- No receipt run will be requested for `3c6947d`. The runner fixes are queued in my lane behind the F5 repair (the runner must follow the final API): actual transaction sender passed and retained as `from` in every static probe (the `register` probe currently observes `E_ADMIN` instead of `E_TYPE_EXISTS`; the native caller-author probe uses the wrong caller) with regression tests; error-argument assertions (`E_INTENT(3)`, `E_TYPE_EXISTS(typeId)`); Type ↔ active policy row ↔ acceptor codehash ↔ epoch joins retained as raw replies; exact selected-cell list printed and zero/unknown filters rejected before chain startup; manifest text corrected (mantissas vs the `quote3000` payload control; 21 cells, not 18); runner tests extended. No broad reimplementation and no full replay.
- Compile-only lease 08:00–08:30 unchanged; the job for `3c6947d` launches at the actual 08:00 UTC and reports real times. SDK's candidate-neutral outputs and pre-run pin procedure noted; not read or adapted to.

## Compile slot result (2026-09-13 08:00:38 UTC) — released

- Pin `3c6947d` (Core `aaecfed`), clean detached worktree, launched **08:00:09 UTC**, ended **08:00:11 UTC** (actual times): `forge build --sizes` **failed** — via-IR "too deep in the stack by 1 slot" at `src/Ledger.sol:478` (`registry.refTypes(x.typeId)` in the publish path) with the memory guard present; the repair's added locals (basis packing, policy row, descriptor reads) overflowed the stack evader. No tests ran; no Anvil; heavy processes at start and end: 0; scratch `…/build/lab-b-pin-3c6947d` (logs `build.log`). **Slot released at 2026-09-13 08:00:38 UTC.**
- The fix (split the reference and acceptance checks into small helpers, keep `_applyPublish` under ~12 live locals) is folded into the F5 repair now in progress on the same function; the next compile will be requested as a new finite slot on the F5 commit after its review, not as a reuse of this one.

## F5 repair + stack fix + runner fixes committed: `3a30fe2` (2026-09-13 08:18:37 UTC)

- Lab branch `fable/2026-09-12-road-b-lab` at **`3a30fe2`** (pushed). F5 falsified on `aaecfed` (FALSIFY.md F5, phase-1 text retained) then repaired: `TypeInfo` pins the mandatory acceptor (packed into the existing header slot; its codehash is the `ruleId` already folded into the Type id); policy row 1 = "no additional policy"; `activate` only appends an additional acceptor that must ALSO accept; the Ledger runs the mandatory rule first (`E_REJECTED`, final) then the policy (`E_POLICY_REJECTED`); profile folds `(typeId, ruleId, policyCodehash, epoch)`; `acceptanceBasis` reports both bases. `MockAcceptor` is a documented mutable double installed only as policy row 2; fixture Types use immutable `MinBodyAcceptor`/`StrictQuoteAcceptor`. The mutable-mandatory limit (same id and codehash, changed behaviour; codehash pins code, not dependencies) is demonstrated by test F5d and STATED, not waived; no claim that production requires pure rules. Cost: one more bounded STATICCALL per admission only when a policy row is active.
- The 08:00 stack-too-deep is addressed in the same commit: `_run` → `_beginPublication` / `_applyOne` loop / `_endPublication`; `_applyPublish` → `_typeOf`/`_bodyOf`/`_checkRefs`/`_acceptAll`/`_admitRecord` (≈7 live locals; `expected` lives only in `_checkRefs`). No ABI/state-layout change beyond the widened views.
- Runner NO-GO items 1–5 repaired in the same commit (sender passed and retained in static probes with regressions; `E_INTENT(3)`, `E_TYPE_EXISTS(typeId)`, `E_POLICY_REJECTED`, `E_REJECTED`, `E_SOURCE_UNSUPPORTED` arguments asserted with raw data retained; `resolveType` and `policy/activate` join typeInfo ↔ rows ↔ acceptanceBasis by Type/codehash/epoch and compare the full immutable descriptor; `--cells` exact list with abort on unknown/zero before Anvil and planned-vs-executed recorded; manifest states payload controls vs the deliberate above-cap body and the 21-cell inventory). Node tests 11 → 16, syntax-checked only. Forge suite expected 43, NOT run (no compiler outside a slot).
- Two independent read-only reviews of `3a30fe2` are running now (Core/F5/stack; runner vs the five NO-GO findings). **I will request a NEW finite compile-only slot (build + forge test, no Anvil, ≈10 min) as soon as the Core review returns READY-FOR-COMPILE**; the receipt run stays unrequested until the runner review flips to GO and a compiled pin exists. Launcher is prepared (clean detached worktree at the pin, real-clock gate, deadline-bounded watchdogs, actual times and heavy counts logged).

## Reviews of `3a30fe2` returned; hardening committed as `ca1a228`; NEW compile slot requested (2026-09-13 08:29:16 UTC)

- **Core review (independent, read-only): READY-FOR-COMPILE, no blockers.** F5 closed in code: every publish/reuse (direct, signed, import, reuse, zero-ref) runs the mandatory rule first, `activate` can neither replace, disable nor reorder it, `mandatory == 0` is a distinct "no-rule" Type id rather than a bypass, codehash re-verified on both rules at call time; Type identity and `E_TYPE_EXISTS` hold; basis bits and profile consistent; MockAcceptor policy-only with the limit stated and no purity claim; storage layout unchanged; 43 tests counted, none vacuous. Two compile RISKS (inliner folding the single-use helpers back into the loop; `acceptanceBasis` decoding seven values beside eight returns) — **applied its prescribed reductions now** (publication id and first ordinal moved into the `Pub` struct, `_checkRefs` on the view pointer, `_basisKey` helper), plus the shadowing rename and a NatSpec fix. Two minors deferred to lab TODO §K (full-bytes error assertions in Solidity tests; an `E_ACCEPTOR_CODE` etch test).
- **Runner review (independent): GO-FOR-RECEIPTS-AFTER-COMPILE, no blockers.** All five NO-GO findings verified repaired with line cites, every ABI shape checked against the source (typeInfo 7, descriptor 6, activation 4, acceptanceBasis 8, all error signatures), mock only reachable as policy row 2/3, cap 2.5e9 vs controls 3000/3100 vs the deliberate 3e9 body, sealed-cell discipline intact, no swallowed exceptions. One MAJOR (3 of 16 new Node tests red: harness artifacts — probe pair exhausted on a second call, cross-realm arrays) and four minors — **all fixed**; `node --test` now 16/16.
- **Commit `ca1a228`** (pushed, lab branch) = `3a30fe2` + the above. No Anvil, no forge, no solc used: Forge suite (expected 43) still unrun.
- **REQUEST: a new finite compile-only slot for pin `ca1a228`** — `forge build --sizes` then `forge test -vv`, no Anvil, ≈10 min, clean detached worktree at the pin, offline pinned solc 0.8.30 (cached), scratch `…/build/lab-b-pin-ca1a228` (<1 GB), alarm watchdogs bounded by the slot deadline, actual UTC launch/end times and heavy-process counts logged. Proposed window: the next 20-minute window Codex can grant (earliest 08:50–09:10 UTC); I will not launch until the lease is recorded, and I will not reuse the expired 08:00–08:30 lease. The receipt run is a separate later request (runner is GO once a compiled pin exists).

## Compile slot 08:50–09:10 UTC — RESULT for `ca1a228` (slot released 2026-09-13 08:51:18 UTC)

- Lease (README 08:48 handoff) accepted as written: compile/tests only, no Anvil, two workers, clean detached pin, run-owned scratch.
- **Attempt 1, launched 08:50:01 UTC (launcher pid 71273):** `forge build` exited 2 at 08:50:01 — my launcher passed `--root` before the subcommand ("unexpected argument '--root'"); NO compiler ran (solc never started; heavy 0). Fixed the argument order.
- **Attempt 2, launched 08:50:24 UTC (launcher pid 71589), worktree `/Users/james/Code/EFS/planning-road-b-pin` detached at `ca1a2283ae3f19352d90c0ada734b1bd98e03659`, clean:**
  - `forge build --sizes --root <pin>/lab-b --out <scratch>/out --cache-path <scratch>/cache --threads 2` (FOUNDRY_OFFLINE=true, cached solc 0.8.30, `perl alarm 300`): **exit 0**, 08:50:24 → 08:50:39 UTC.
  - `forge test -vv --root … --out … --cache-path … --threads 2` (`perl alarm 600`): **exit 0**, 08:50:39 → 08:50:39 UTC. **43 passed, 0 failed, 0 skipped** across 6 suites (7 + 11 + 5 + 5 + 3 + 12), including F1–F5 (F5a–F5d), the import matrix and the label/joined-consumer suites.
  - Heavy processes before/after: 0/0. New scratch: 3.2 MB at `…/scratchpad/build/lab-b-pin-ca1a228/{out,cache,logs}` (logs: `summary.txt`, `build.log`, `test.log`). Ended 08:50:39, well inside the 09:10 deadline; nothing left running.
  - Runtime sizes (`--sizes`): IndexModule           4,127             B;  JoinedConsumer        7,985             B;  Ledger                17,280            B;  LensReader            9,445             B;  MinBodyAcceptor       354               B;  TypeRegistry          3,270             B;
- The reviewed stack-depth reductions held: no via-IR stack error at `ca1a228`. The F5 Core is now a COMPILED, TESTED pin (still no chain evidence: no Anvil, no receipts).
- Next in my lane (light): the public `vectors/profile-b.json` for this pin from the build artifacts; PROFILE.md sizes updated; then a separate receipt-run request for the 21-cell (or bounded subset) runner at `ca1a228`, with its own reviewed pin/handoff as the lease terms require.

## REQUEST: bounded receipt run at `ca1a228` (own handoff, 2026-09-13 08:53:00 UTC)

- **What:** `script/measure.mjs --anvil --cells failure-rows,policy/activate,failure/refused-re-registration,failure/unsupported-native-import` at pin `ca1a228` (the compiled, tested Core; runner as reviewed GO at `3a30fe2` plus exactly the reviewer's five fixes, listed in the `ca1a228` commit message and lab TODO §K — root's cross-review of that delta is welcome first). Artifacts read from the already-built `…/build/lab-b-pin-ca1a228/out` (no rebuild). Not a full replay: the 21-cell inventory and the retained `5960336` packets stay as they are under their original profile.
- **Why these four:** they are the receipt-level evidence the authority/Type-identity scope asks for — mock refusal as `E_POLICY_REJECTED(0, QUOTE)` with the mandatory rule intact, activation of an additional policy with old-epoch signature refused `E_INTENT(3)` and both `acceptanceBasis` rows joined to the registry rows by Type/codehash/epoch (the F5 cost row: the extra bounded STATICCALL), refused re-registration `E_TYPE_EXISTS(typeId)` simulated from the actual sender, and the fail-closed `v == 0` import with state unchanged. The declared `vectors/profile-b.json` for this profile is produced from this run's retained packet (as the `dcc7b94` vector was), so it is owed after this run rather than from the build alone.
- **Envelope:** one Anvil (script-owned: cancun, chain 31337, 4 accounts, no tracing, `--prune-history 256`, PID/port recorded in `measure.json`, killed by the script), clean detached pin worktree, explicit nonces + polled receipts, per-cell `evm_snapshot`/`evm_revert`, planned-vs-executed cells asserted; expected ≈5 min for four cells, in-script watchdog 25 min, my alarm bounded by the slot deadline; new scratch <100 MB; heavy count 0 before/after; actual UTC launch/end, commands and PIDs reported. **Proposed: any 20-minute window Codex grants** (I will not launch before the lease is recorded).
- Lab docs commit `afa8392` (pushed) records the compiled pin (README/PROFILE/REPAIR say compiled-and-tested, runner unrun; manifest commands corrected: 43 tests, pinned artifact dir, the four-cell subset).

## Receipt-diagnostic lease 09:00–09:20 UTC accepted (2026-09-13 08:56:30 UTC)

- Accepted as written (README 08:58): pin `ca1a228` clean detached source, already-built artifacts (no rebuild), exact six-cell allowlist `native-one/quote,signed-one/quote,failure-rows,policy/activate,failure/refused-re-registration,failure/unsupported-native-import`, one loopback Anvil (script-owned, bounded history, run-owned cache), alarm bounded by 09:20, <100 MB new scratch. Launcher gated on the real clock at 09:00:00 UTC; tool versions are observed at launch (anvil/node/ethers printed by the preflight, forge/solc pins in the build log), not copied. Actual UTC launch/end, PIDs, exact selection (planned vs executed), raw caller-qualified probes, error arguments, joins and cleanup will be reported here with the retained `measure.json`, failures included.
- Two qualifications noted for the docs after the run: `acceptanceBasis.epoch` is the activation-row epoch while the signed profile folds the global registry epoch of the admission context; the reconstructor reuses the retained profile hash rather than regenerating it. Both are reconstruction gaps, not bypasses; no vector from this packet will be called a pre-sealed expectation.
- After the diagnostic: source-only B mapping/consumer changes for the common A1/A2/B1 point/list slice (`sdk-fixture.md`, `matched-cost-scope-review.md`), no Core change to force equal encodings, no second placement, no checker rebuild, no further heavy run without a new lease.

## Receipt diagnostic 09:00–09:20 UTC — RESULT for `ca1a228` (slot released 2026-09-13 09:02:27 UTC)

- **Actual run:** launcher pid 73336 gated on the real clock; `node script/measure.mjs --anvil --cells native-one/quote,signed-one/quote,failure-rows,policy/activate,failure/refused-re-registration,failure/unsupported-native-import` from the clean detached pin `ca1a2283ae3f19352d90c0ada734b1bd98e03659`, artifacts from `…/build/lab-b-pin-ca1a228/out` (no rebuild), `EFS_LAB_SCRATCH` = that build dir. **Started 09:00:00.771 UTC, finished 09:00:05.173 UTC, exit 0.** Anvil pid **79548**, port 58056 (started 09:00:00.778, stopped 09:00:05.173 by the script; cancun, chain 31337 confirmed by the retained `eth_chainId` reply). Heavy processes 0 before / 0 after; no `measure.mjs` or `anvil` process remains (verified by process list). New scratch 4.3 MB (total build dir 7.5 MB). Versions observed at launch: node v24.11.0, anvil 1.7.1, ethers 6.13.7; forge 1.7.1 / solc 0.8.30 from the build log; compiler per artifact metadata retained in `deployment[*].artifact.compiler`; sha256 of every src/test/script file in `build.sourceHashes`.
- **Selection:** `plannedCells == executedCells` = the six allowlisted cells in plan order; the 15 other cells are listed in `skippedCells`. **Mismatches: 0 in every cell; consumerMismatches 0; failure: null.** Registry epoch after setup: 8. Type ids derived and cross-checked (log == `typeIdOf` == local derivation) — e.g. QUOTE `0xd0d5…4142`, mandatory `MinBodyAcceptor(32)` at `0x0DCd…CD82`, ruleId `0xa05d…26a3`, no policy at resolution.
- **Evidence retained and pushed:** lab commit **`5891cc5`** — `evidence/measure3-ca1a228.json` sha256 `6cbdf98b5b4d22fe9ed6f65b3732a7adddd0129c1aa8b0dea1bd552e91a3c614` (4,444,381 B), `measure3.log`, `measure3-launcher.txt`, `SHA256SUMS`. Receipt diagnostics under the repaired profile; earlier packets keep their original profiles; nothing here is a pre-sealed expectation or an architecture verdict.
- **Refusal/policy cells (all with raw caller-qualified `eth_call` probes simulated from the actual sender, expected selector AND arguments asserted, state probe unchanged, mined receipt status 0):** `E_POLICY_REJECTED(0, QUOTE)` (mock as additional policy) 209,510 gas; `E_INTENT(3)` stale signature after activation 48,585; `E_POLICY_REJECTED(0, QUOTE)` above-cap body under the added StrictQuote policy 224,498; `E_REJECTED(0, STRICT)` three times — before any activation, after `activate(0)`, and under a permissive policy — 220,307 each (the mandatory rule cannot be removed); `E_TYPE_EXISTS(QUOTE)` refused re-registration 28,730 with the registry replies equal to baseline; `E_SOURCE_UNSUPPORTED()` on both destination paths 42,605 / 39,633 with the claimed subject absent before and after and no SourceEvidence row minted. Pre-existing rows `E_REF_TYPE`, `E_REF_MISSING`, `E_CAS`, `E_INDEX` remain selector-only (no arguments declared).
- **Joins retained (rows `registry-after`, `acceptance-basis`, `strict/acceptance-basis`):** `typeInfo.policyCodehash == activation(3).codehash == deployed runtime codehash`; immutable descriptor deep-equal to the baseline after activation; the old QUOTE admission names policy row **2/mock, activation epoch 7**, while its signed profile uses global epoch **8**. The post-activation admission names row **3/StrictQuote, activation/global epoch 9**. Both join by Type, mandatory rule, codehash and epoch. Codex corrected the original row-1 and new-global-8 narration against raw evidence; bytes are unchanged. `acceptanceBasis.epoch` is the activation-row epoch, not necessarily the profile's global epoch.
- **Costs under the repaired profile (receipts; NOT comparable 1:1 with `5960336`, whose profile had the mutable mock as the only rule):** native create 1,226,435 (was 1,216,793: **+9,642**), signed create 1,271,630 (was 1,261,965: +9,665), native edit 520,724 (+8,826), signed edit 567,395 (+8,822 over the exact retained 558,573); paid reads differ by tens of gas (storing resolve 137,909 vs 137,883; list 97,725 vs 97,678); current stateless-twin rows resolve 51,173 / list 89,168 / history 66,094–64,287. These are whole repaired-profile deltas, not a paired isolation of F5 or a universal per-admission premium. Mandatory/additional-policy calls, descriptor reads and other source changes differ. `activate` costs 82,530; `activate(0)` 59,661; in-cell Type registration 131,287; publish under mandatory + policy row 431,775–470,374.
- **Next (per the 08:56 handoff, light only):** source-only B mapping/consumer changes for the common A1/A2/B1 point/list slice in `sdk-fixture.md` / `matched-cost-scope-review.md`; no Core change to force equal encodings, no second placement, no checker rebuild, no heavy run without a new lease.

## B counterpart for the sealed paid point/list slice — source-only, started (2026-09-13 09:04:10 UTC)

- Scope taken from `matched-cost-scope-review.md` (C follow-through mirrored) and the `sdk-fixture.md` appendix: B's `B1` already writes no FOLDER placement, so B's work is in the measurement consumer and the joined runner cell, not the Core (`ca1a228` unchanged; no encoding forced equal, no second placement, no checker rebuild, no heavy run). Specialist is preparing, uncommitted for my review: `JoinedConsumer` separates placement provenance (the one A placement, A1 / AUTHOR_A / EOA_SIGNED_PUBLICATION_EFFECT) from selected-content authorship, shares the Quote → Pair → two Items closure between point and list, checks revision/admission/Evidence bounds and proof kind, list checks exactly one placement and a COMPLETE window, point does no directory lookup, both at one observation basis; runner adds the B-first paid list row, seals the post-B1 block/snapshot and runs each paid row first-from-revert with an unrelated pinned caller, retains the appendix's common comparison fields as `abstractResult` (grade RPC_OBSERVED, observations not expectations), proves "no B placement" from raw replies, and adds an optional non-default paired control `joined/a1-without-placement` so the single placement's marginal cost can be measured rather than estimated if Root pins it; negative consumer tests (wrong/missing Pair/Items, wrong author/proof category, extra placement, mixed bases, incomplete coverage, point/list disagreement). Docs carry the pre-run pin checklist and state that expectation/arm-input/basis seals are the run controller's, never this packet's.
- Will report file-level changes and compile risks here for Root's source review before any measurement is requested.

## Receipt review accepted by Root (2026-09-13 09:06:32 UTC); narration corrected

- Root's independent offline review of `measure3-ca1a228.json`: 75 signed transactions joined (26 setup + 49 cell), 13 expected failed receipts, nine explicit error-argument cases, 237 unchanged pre/post getter pairs, seven admission-basis rows, all 18 source hashes matching the pin; GO for retained RPC consistency/signature evidence, not chain-state authentication or full semantic-oracle passage. Acknowledged as such; no stronger claim is made anywhere in my lane.
- Correction applied to my own text (this note and `road-b.md` §9), with Codex's final raw-epoch correction: the old QUOTE admission's policy row is 2/mock (activation epoch 7, profile global epoch 8), not row 1/no-policy; the new admission is row 3 (global epoch 9). Evidence bytes unchanged.
- Noted: Root owns the C mandatory-rule commitment repair/map (C's structural TypeID excludes the mandatory rule; repaired B's includes it). No action in the B lane beyond keeping B's TypeID derivation documented as it is; the B consumer/fixture slice continues (specialist in progress, source-only).

## B paid point/list slice committed: `5c61d22` (source-only, UNRUN; 2026-09-13 09:29:54 UTC) — for Root's source review after my two reviews

- Lab branch `fable/2026-09-12-road-b-lab` at **`5c61d22`** (pushed): `src/JoinedConsumer.sol` +235 (existing functions/events/errors untouched), `test/JoinedConsumer.t.sol` +161 (existing tests untouched), `script/measure.mjs` +378/−9, `script/measure.test.mjs` +106, `MANIFEST.draft.json`, `README.md`, `TODO.md` §L. Core files, `evidence/`, `vectors/` untouched; Core stays `ca1a228`.
- Consumer: `paidPoint(address[] lens, Expect)` / `paidList(address[] lens, Expect, PlacementExpect)`; one `PaidResult(kind, commitment, Selection, Placement)` log per paid row with concrete observations (19 selection fields, 14 placement fields), not only a digest. Placement provenance (the one A placement: A1 / AUTHOR_A / EOA-signed effect) is observed separately from selected-content authorship — no author equality; point and list share the Quote → Pair → two Items closure at one pinned admission frontier (`BasisMismatch` on drift); selection checks revision, a live BIND admission inside its publication's range and the proof category (native publications carry v=r=s=0, signed nonzero); list requires COMPLETE + ended + exactly one row over exactly one raw candidate (`rawTotal == 1` proves no B placement or duplicate); point does no directory lookup. Negatives (3 new Solidity tests, desk-checked): wrong Pair/Items/mantissa expectation, wrong author or proof category, non-joined head, extra placement, mixed bases, incomplete coverage, point/list disagreement.
- Runner: new default cell `joined/paid-slice` — rows `paid/setup/{step1,A1,A2,B1}`, `paid/seal` (post-B1 block number/hash + snapshot retained before any paid read), `paid/seal/a-placement-provenance`, `paid/seal/no-b-placement` (B1 confirmed to bind no FOLDER; absence established by `head`/`postingHead`/`resolve([B])` raw replies at the seal), `paid/{point,list}-{a,b}-first` each first-from-revert to the seal with the unrelated pinned caller (mnemonic index 3, asserted unrelated to deployer/authors/every contract), `paid/ordering`, `paid/agreement`, `paid/cost-disclosure`; each row retains the appendix's common comparison fields as `abstractResult` with `inputEvidenceGrade: RPC_OBSERVED` (observations; the `Expect` values the runner passes are the candidate-side fixture mirror, to be replaced by the run controller's independent vectors). Optional non-default cell `joined/a1-without-placement` (exact `--cells` only): the paired control for the once-only placement cost; without it the decomposition stays an ESTIMATE. `node --check` OK; `node --test` 23/23 (16 + 7).
- Compile risks the author listed (TODO §L, unverified — no forge outside a lease): `lens.index().generation()` type scope; wide tuple destructurings of `admission()`/`evidence()`; uint64+uint16 arithmetic; struct-typed event/return params; a uint8/uint256 comparison; test-side positional struct constructors, `string.concat`, memory→calldata struct passing, via-IR stack depth in the negatives test, hard-coded ordinals; runner-side ethers `Result` named access on nested tuples, inner `evm_revert` vs the outer per-cell snapshot, wallet 3 funding on an external `--rpc`.
- Two independent read-only reviews (consumer/tests vs the appendix + compile risks; runner seal/revert/caller/abstractResult discipline) are running now; I will fix findings, re-commit, and then hand the pin to Root's source review. **Coordinator inputs needed before any measurement (no heavy slot requested):** which cells to pin (proposed bounded run `--cells joined/paid-slice,failure-rows`, plus `joined/a1-without-placement` only if the paired placement-cost control is wanted); the independent expectation manifest (7 rows), B arm-input manifest and post-B1 basis seal per the appendix checklist (`MANIFEST.draft.json → operations.paidSlice.operatorChecklist`); and whether the `PaidResult` log with ~33 data words (ESTIMATED several k gas, disclosed as consumer overhead) is acceptable or a digest-only log with observations taken from the replay is preferred.

## Reviews of `5c61d22` returned (2026-09-13 09:39:03 UTC); findings being applied before the handoff pin

- **Consumer/tests review: READY-FOR-COMPILE, no blockers.** Every author-flagged compile risk verified clean by desk-check (`LensReader.index` is typed `IndexModule` so `.generation()` needs no import — same precedent as `ledger.registry().epoch()`; `admission()` 8 / `evidence()` 13 arities match; struct events/returns fine under ABI v2; test ordinals derived from `runSteps1to4` are correct: placement adm 7/pub 2, A head 10/3, B head 12/4, frontier 12). Semantics verified: proof category separates CONTRACT_ORIGINATED (v=r=s=0 in the evidence cell for native publications) from EOA_SIGNED; `rawTotal == 1` catches a live or tombstoned B duplicate that the reducer would mask; no state shared between placement provenance and selected authorship; an empty page can never pass. Tightenings being applied: pin `expectedHead` as an input in `Expect` (10 fields) with a negative test; split the positive test's two halves to pre-empt via-IR stack depth; NatSpec on the `rawTotal` semantics; correct the header's log-overhead comment (`PaidResult` ≈ 34 data words, ESTIMATED ≈9–10k gas, disclosed as paid-row consumer overhead).
- **Runner review: GO-FOR-RUN-AFTER-COMPILE.** Verified: seal/revert trace (outer per-cell snapshot intact; every paid row reverts to the current seal, re-snapshots, drops cached headers/nonces, re-reads and asserts block number/hash and an empty pool; ordering asserted seal+1 and retained-before-revert); unrelated caller asserted before setup against deployer, authors, wallet 2 and every contract; all 27 `abstractResult` fields required and none defaulted, labels separate from physical ids, `lookedUpByThisRow:false` enforced for point, expectations from the plan not from decoders; no-B proofs are direct state reads at the seal block (`head` state 0, B's `/swaps` posting count 0, `resolve([B])` ABSENT); optional cell exact-`--cells` only with the paired-control / ESTIMATE labels; ABI and expected ordinals match; existing cells' code paths untouched; the 7 new tests are not tautological. One MAJOR being fixed: the post-row replay `eth_call` carried no `from` while three texts said "from the same caller" (result identical since the consumer never reads `msg.sender`; the label was false) → replay now simulated from the retained transaction sender and the texts made true. Minors being applied: `selfCheck` retained under `rawEvidence` and UNKNOWN outcomes when the self-check mismatches; balance pre-check for the paid caller at a pinned block (matters on external `--rpc`); `candidateCoverage` carries status + end condition; a `resolve([B,A])` seal reply for the B-first rows; seal rows pushed to `rowLog` immediately; exact optional-cell key in the manifest.
- Next: re-commit with the fixes, then the pin goes to Root's source review (no heavy slot requested).

## HANDOFF PIN for Root's source review: `980a017` (B paid point/list slice, source-only, UNRUN; 2026-09-13 09:44:30 UTC)

- `980a017` = `5c61d22` + every review finding applied (+104/−63 across `script/measure.mjs`, `src/JoinedConsumer.sol`, `test/JoinedConsumer.t.sol`, `MANIFEST.draft.json`, `TODO.md`): replay `eth_call` simulated from the retained transaction sender (`ctx.txs[i].from`) and the three evidence texts made true; mismatching self-check → `UNKNOWN` outcomes with `rawEvidence.selfCheck`; paid-caller balance asserted at a pinned block (row `paid/caller-funding`); `candidateCoverage` status + end condition; `resolve([B,A])` retained at the seal and cited by the B-first rows; seal rows persisted on creation; exact optional-cell key; `Expect.expectedHead` input (10 fields, negatives for an older head and for B's head under A); positive test split into two helpers; NatSpec on `rawTotal`; header overhead corrected. Core unchanged at `ca1a228`; `evidence/`, `vectors/` untouched. `node --check` OK, `node --test` 23/23; Solidity desk-checked (no compiler outside a lease): expected Forge suite 43 + 4 new = 47 tests.
- **Ready for a compile-only slot (build + Forge tests, no Anvil, ≈10 min) whenever Root grants one after its source review; not launching anything and not requesting a measurement.** The sealed paid run afterwards needs the coordinator inputs listed above (cell pin — proposed `joined/paid-slice,failure-rows`, optionally `joined/a1-without-placement`; the independent expectation manifest, arm-input manifest and post-B1 basis seal; the log-overhead choice).
- Bounded C counterpart and the C mandatory-rule identity gap remain Root's; nothing in this pin assumes their outcome.

## 09:50 handoff acknowledged (2026-09-13 09:51:39 UTC) — B light/source-only; Root's C slot 09:50–10:00 respected

- Acknowledged: keep the concrete `PaidResult` diagnostic log and disclose its instrumentation gas as paid-row consumer overhead (not an SDK ABI, not unavoidable EFS overhead — the runner row text and MANIFEST already say so); the next selected run is `--cells joined/paid-slice,failure-rows,joined/a1-without-placement` with the independent inputs (expectation manifest, arm-input manifest, post-B1 basis seal) prepared by the run controller before it; no measurement is granted by that selection and none is requested. B stays light/source-only until an explicit post-review compile handoff; Root's consumer/runner reviews of `980a017` are awaited.
- During Root's 09:50–10:00 C slot: no heavy process from my side (anvil/forge/solc count observed 0; runner/launcher processes 0 — an earlier figure of 13 was the checking command matching its own shell text, re-verified at 09:51:58 UTC by executable name); nothing scheduled.
- Standing by for Root's review findings on `980a017` (will apply them source-only and re-pin) and, after that, a compile-only handoff.

## Root's source review of `980a017` acknowledged (2026-09-13 09:54:38 UTC) — four fixes in progress, source-only

- Accepted as written: compile-eligible, no ABI blocker; the four comparison fixes are being applied before any paid-slice run request: (1) sealed `observedAt` and note-commitment expectations added to `Expect` and compared on chain, with focused negatives (Item Type checks stay sufficient; no Item payload semantics added); (2) after a failed `paidResultCheck` every derived success claim (hardcoded `COMPLETE` coverage, selected labels, presence/support/admission/selection) becomes UNKNOWN unless a separate retained observation establishes that field; raw observations and mismatches preserved; injected replay/result tests added; (3) each paid row must be transaction index 0 in its block with the block's transaction list retained and checked, and the next-block timestamp is set deterministically after every revert so the four rows share block number, parent hash AND timestamp, asserted and recorded in `executionBasis`, with ordering tests for the new violations; (4) bounded public-ABI controls with malformed/missing Pair/Item and admission/Evidence ACTUAL replies via a small test-only faulty reader, named distinctly from the wrong-expectation negatives.
- Scope accepted: only owned `--anvil` runs; the paid-slice cells will refuse to run without `--anvil`, documented; the external `--rpc` path is not used for this work.
- No build or chain lease inferred; the corrected pin will be returned here for Root, with a compile-only slot request only after that.

## CORRECTED PIN returned for Root: `2859147` (B paid point/list slice, four review fixes applied; source-only, UNRUN; 2026-09-13 10:09:18 UTC)

- `2859147` = `980a017` + Root's four fixes (+405/−89; new `test/FaultyReads.sol`; Core untouched at `ca1a228`; `evidence/`, `vectors/` untouched):
  1. `Expect` pins sealed `observedAt` (uint64) and `noteCommitment` (bytes32) as INPUTS (12 fields); `_closure` compares both on chain (`SelectionMismatch` fields 3/4); Item checks stay Type-only; negatives for a wrong `observedAt` (point) and a wrong note (list); runner inputs from the fixture map, labelled candidate-side mirror.
  2. `deriveAbstractResult({check, replay, evidenceFor})` is a pure function: after any failed `paidResultCheck` (missing/undecodable log, replay ≠ log, field mismatch) presence/support/admission/selection, selected labels, closure checks, candidate and page coverage are `UNKNOWN` with the reason; raw log/replay/mismatch retained; only the seal replies (provenance, basis) and the receipt/block (executionBasis) keep values, each naming its source; three injected tests (replay differs, log missing, match).
  3. After each `evm_revert` to the seal the runner sets `evm_setNextBlockTimestamp(seal+1)` (envelope retained), fetches the row's block, and asserts transaction index 0, a single transaction equal to the row hash, timestamp = seal+1 and equal across the four paid rows; `executionBasis` carries block/parentHash/timestamp/txIndex/txCount; ordering tests add wrong-timestamp, txIndex≠0, extra-tx and wrong next-timestamp cases.
  4. `test/FaultyReads.sol` forwards the public read ABI to the real contracts with one armable fault; six `test_faulty_actual_reply_*` tests assert the specific refusal for corrupted/unavailable ACTUAL replies (missing Pair, wrong-Type Item, non-BIND admission, out-of-range publication, one-word evidence, PARTIAL claiming ended), named apart from the wrong-EXPECTATION negatives; the consumer takes consumer-local read interfaces (constructor ABI unchanged: addresses).
  - Scope: `joined/paid-slice` and `joined/a1-without-placement` refuse to run without `--anvil` before any chain call (unit-tested; MANIFEST `paidSlice.scope`, README, TODO); the external `--rpc` path is not used for this work.
- `node --check` OK; `node --test` **28/28** (16 original + 12). Expected Forge suite 52 (43 at `ca1a228` + 3 + 6); desk-checked only. Author-listed compile risks (interface functions returning `LensReader` struct types; assembly `return` from an unnamed 13-tuple in the faulty reader; empty-revert assertion for short return data; `Ledger.evidence.selector` on the contract type) are under a focused independent desk review now; any finding is applied source-only and re-pinned.
- No build or chain lease requested. When Root's re-review passes, the next step is a compile-only slot (build + 52 Forge tests, no Anvil), then the sealed `--anvil` paid run with the run controller's independent inputs.

## Compile-only lease 10:10–10:40 UTC accepted (2026-09-13 10:10:11 UTC) — launch gated on my focused review of `2859147`

- Accepted as written (README 10:08): build/sizes + Forge tests only, offline pinned solc 0.8.30, two threads, watchdog bounded by 10:40, no Anvil/deployment/measurement, failed runs kept, actual UTC/exit/test counts/sizes/cleanup reported; no late launch after 10:40.
- **Intended pin: `2859147`** (the corrected successor of `980a017`, pushed), launched only if the focused independent desk review now running returns READY; if it finds a blocker, the fix is committed first and the new SHA recorded here before launch; if that cannot complete before the deadline, no launch.
- Run-owned paths (recorded before launch): clean detached worktree `/Users/james/Code/EFS/planning-road-b-pin` at the pin; artifacts/cache/logs under `…/scratchpad/build/lab-b-pin-<sha>/{out,cache,logs}`; commands `forge build --sizes --root <pin>/lab-b --out … --cache-path … --threads 2` then `forge test -vv … --threads 2`, each under `perl alarm` capped by the deadline, `FOUNDRY_OFFLINE=true`; launcher preflight at 2026-09-13 10:10:11 UTC: solc 0.8.30 cached, heavy processes 0, disk free ≥ 265 GiB.

## Compile slot 10:10–10:40 UTC — RESULT for `2859147` (slot released 2026-09-13 10:17:49 UTC)

- Focused independent desk review of `2859147` returned READY-FOR-COMPILE at ~10:15 (no blocker or major; all four fixes verified met; every interface function checked against the real `Ledger`/`LensReader` signatures; three minors noted: selector-only assertions in the F1 negatives, ordering asserted end-of-cell rather than per row, and a note that `evm_setNextBlockTimestamp` relies on Anvil resetting its last timestamp on revert — fails loudly, never silently).
- **Launched 10:16:46 UTC** (launcher pid 437), clean detached worktree `/Users/james/Code/EFS/planning-road-b-pin` at `285914736c064c2c71641c1cf32972668aef5869`, artifacts/cache/logs under `…/scratchpad/build/lab-b-pin-2859147/`, `FOUNDRY_OFFLINE=true`, cached solc 0.8.30, `--threads 2`:
  - `forge build --sizes`: **exit 0**, 10:16:46 → 10:17:02 UTC (`perl alarm 300`).
  - `forge test -vv`: **exit 0**, 10:17:03 UTC. **52 passed, 0 failed, 0 skipped** across 6 suites (7 + 11 + 5 + 12 + 3 + 14): the 43 of `ca1a228` plus the 3 paid-slice tests and the 6 faulty-actual-reply controls.
  - Heavy processes before/after 0/0; new scratch 3.6 MB; pin worktree removed after the run; nothing left running. Ended 10:17:03, inside the 10:40 deadline.
  - Runtime sizes: FaultyReads           5,587             B;  IndexModule           4,127             B;  JoinedConsumer        13,852            B;  Ledger                17,280            B;  LensReader            9,445             B;  TypeRegistry          3,270             B; (Core sizes unchanged from `ca1a228`; `JoinedConsumer` grew with the paid entry points; all under EIP-170.)
- No Anvil, no deployment, no measurement. The B paid slice is now a COMPILED, TESTED pin. Per the 10:08 handoff the next work is source-only: B's comparison inputs / runner mapping (the arm-local fixture map from semantic labels to physical coordinates, and the mapping of the appendix's seven expectation rows to runner rows) so the run controller can author the independent expectation and arm-input seals; an eligible receipt run still needs those seals and its own lease.

## B comparison inputs delivered: `b2aa07d` (source-only, for the run controller's independent seals; 2026-09-13 10:29:03 UTC)

- On top of the compiled pin `2859147` (Core unchanged): `lab-b/FIXTURE-MAP.md` + `lab-b/vectors/fixture-map-b.json` — B's arm-local semantic→physical map for the paid slice as FORMULAS with cited source lines and worked values (domain tags; ITEM/PAIR/QUOTE descriptors and the `typeId` fold; record bodies with every field incl. `observedAt`/`noteCommitment` and the record-id domain; `FILE_QUOTE` subject and creator salt; origin-qualified principals; FOLDER/HEAD/TAG binding keys and scope keys; `LENS_A_FIRST`/`LENS_B_FIRST` ids; revision labels A1/A2/B1 → 1/2/1; expected ordinals under this runner — placement admission 7 / publication 2, post-B1 frontier 12, registry epoch 8; evidence categories ↔ proof-kind and v/r/s shape; caller roles by mnemonic index 0–3 with addresses, no secrets). `vectors/verify-fixture-map-b.mjs` recomputes every derivable value offline from the formulas and raw inputs: **PASS 63/63, exit 0** (re-run by me with the pinned ethers 6.13.7). Values that need the controller's own BUILD (the `MinBodyAcceptor(96)`/`QuoteAcceptor` rule codehashes and the Ledger codehash, hence PAIR/QUOTE_J ids, the Quote ids that embed the Pair id, `AUTHOR_B`'s principal and B's keys) are `null` and named; `--codehashes <file>` derives them from the controller's build. Values that only the RUN observes (sealed block hash/timestamp/snapshot, tx hashes, gas, signature bytes) are listed as observations, not inputs.
- `lab-b/EXPECTATION-ROWS.md`: the appendix's seven rows (A1/A2/B1 setup; paid point/list × A-first/B-first) mapped to `joined/paid-slice` rows; per row the realising transaction(s), the `abstractResult` fields retained, the raw replies that establish each field, what the controller seals BEFORE the run (labels/outcomes only), and what B discloses separately (once-only placement cost as ESTIMATE unless `joined/a1-without-placement` is pinned; deployment/setup/code/storage; `PaidResult` instrumentation gas). Arm-input manifest fields B can supply are listed with the rule that artifact/initcode/runtime hashes come from the controller's own build of the pin, not from these files.
- Everything is labelled CANDIDATE-AUTHORED: the controller re-derives independently and copies no expected answer from these files or from any packet. One correction surfaced: the registry epoch after runner setup is 8 (six `register` + two `activate`, observed in `measure3-ca1a228.json`), not the 6 an older TODO note said — fixed.
- Ready for Root's review of these inputs; the sealed `--anvil` paid run (`--cells joined/paid-slice,failure-rows,joined/a1-without-placement`) still needs the controller's expectation manifest, arm-input manifest and post-B1 basis seal, and its own lease. No heavy process running; nothing requested.

## 10:50 handoff acknowledged (2026-09-13 10:51:31 UTC) — two source-only lanes

- **Lane 1 (C paid consumer/runner):** ownership taken in `planning-road-c-lab` at published `97e84c9` (verifying tracked-clean and preserving untracked `out` before any edit; Root does not edit C concurrently). Scope exactly the reviewed C follow-through + the paid-slice appendix: one A placement (B1 binds no FOLDER), independent content selection under both Lenses, the same Quote/Pair/two-Item checks in paid point and list, placement provenance separate from selected authorship, bounded ACTUAL-bad-reply negatives, unrelated paid caller, restored post-B1 state/time/first-transaction controls, concrete raw observations; measurement-local fixtures kept apart from the broader two-placement tests; no Core/index/table change to align encodings — C's native MUD layouts stay what is compared. Order: test-first source preparation → internal independent review → committed pin → request a finite compile slot. No compile or Anvil before a lease.
- **Lane 2 (B controller integration):** waiting for Root's runner reviewer's exact hook finding (pre-fixture and post-B1/pre-paid handoff for independently sealed inputs) before coding any B delta; the current script is not run and its self-produced packet is not blessed; Root owns the expectation/vector/seal side; B inputs/code stay available read-only.
- Acknowledged: candidate maps are declaration inputs, never independent expected answers; a requirements-only author freezes the seven-row manifest without opening candidate code/results.

## 10:53 B review GO / controller delta acknowledged (2026-09-13 10:54:29 UTC)

- Noted: independent consumer review confirmed 52/52; Root reproduced 28/28 Node tests; the four findings are closed. Two reporting/input omissions being fixed now (source-only): the B README's stale `JoinedConsumer` size → **13,852 B** (measured at `2859147`), and the three non-joined Type descriptors (QUOTE/BINARY/LABEL) added to the declaration input map alongside ITEM/PAIR/QUOTE_J with the verify script extended.
- **Controller delta — interface first, code after.** As instructed, I will return the exact proposed JSON interface here BEFORE any runner change: an opt-in pinned controller module (`--controller <path>:<sha256>`, `--expectations <path>:<sha256>`, `--arm-input <path>:<sha256>`) with two awaited hooks — `beforeFixture(context)` after deployment/registry setup and before the first selected cell (failure rows precede paid rows, so before whichever cell is first), and `afterB1(context)` immediately after the post-B1 snapshot and before the placement/paid reads. Context payloads carry run ID, stage, the three file hashes, the full 12-field `Expect` and 6-field `PlacementExpect` physical inputs (consumed or strictly compared field-by-field, never echoed as a hash), both Lens arrays, roles with derivation indices, fixture bodies/IDs/Types, fixed ordinals, and (afterB1) the ACTUAL post-B1 checkpoint. Acks are separate retained files (never inside the result packet); the runner proceeds only on a well-formed `ACK` with matching run ID/stage/hashes within a bounded timeout — otherwise no downstream send and a non-zero exit; ungated runs stay explicitly `diagnostic`. Root owns the controller module and the independently derived vectors; I own only the candidate runner integration, tests and docs. Concrete `PaidResult` log and the three selected cells kept; no new oracle framework, no Core change, no candidate verifier as controller, no retrospective sealing; no heavy slot.
- The C lane runs in parallel (specialist in `planning-road-c-lab`, source-only, test-first).

## PROPOSED CONTROLLER INTERFACE returned for Root to bind — `7c292e0` (design only, no runner code; 2026-09-13 11:00:53 UTC)

- Files (lab branch, pushed): `lab-b/CONTROLLER-INTERFACE.md` (the contract, every field with type / source / comparison rule) and `lab-b/vectors/controller-interface.example.json` (one fully worked example of each of the four messages; derivable values from the map, build/run-dependent values as labelled placeholders, hashes internally consistent). Also in `7c292e0`: README sizes corrected (JoinedConsumer **13,852 B** at `2859147`; 7,985 B was the `ca1a228` build), all six registered Type descriptors (QUOTE, BINARY, ITEM, PAIR, QUOTE_J, LABEL) in `vectors/fixture-map-b.json` / `FIXTURE-MAP.md`, verify script **PASS 71/71** (QUOTE/LABEL ids and everything folding a rule or Ledger codehash stay `null` until the controller's own build supplies `{quoteRule, pairRule, quoteAcceptor, labelAcceptor, ledger}`).
- **Operator pins / CLI:** `--controller <path>:<sha256>`, `--expectations <path>:<sha256>`, `--arm-input <path>:<sha256>` — all three or none (`CONTROLLER_PINS_INCOMPLETE`, exit 2, before Anvil); sha256 of the file bytes checked before any chain call (`CONTROLLER_PIN_MISMATCH`); the expectations and arm-input files are pinned and echoed, never parsed by the candidate runner; optional `--run-id`, default derived from the three hashes + start time. Without the flags: `report.gating = "diagnostic"`, `report.controller = null`.
- **Module:** ESM default export `{ beforeFixture, afterB1 }` (both async; anything else `CONTROLLER_MALFORMED_MODULE`); each hook RETURNS its ack; 120 s per hook (`CONTROLLER_TIMEOUT`). Hashing: canonical JSON (recursively sorted keys, no whitespace, UTF-8, lowercase hex, EVM quantities as decimal strings) → sha256.
- **When:** `beforeFixture` after `deployAll` + registry setup + the sealed initial state and BEFORE the first selected cell (gates `failure-rows` when it is first); `afterB1` inside `joined/paid-slice` immediately after the post-B1 `sealState` + basis reads and BEFORE the placement replies and any paid row.
- **Context → controller (top level):** beforeFixture: `schema, runId, stage, sentAtUtc, pins, chain, source{commit, dirtyDiffSha256, sourceHashes}, build{solc, viaIR, optimizerRuns, evm, artifacts.<C>.{artifactSha256, initcodeHash, runtimeCodehash, runtimeBytes}}, roles, deployment, typesObserved (6, from `TypeRegistered` logs ≡ `typeIdOf`), registry.epochAfterSetup, sealedInitialState, plannedCells, firstCell, mirror (the candidate-side mirror, never sent to chain), ackPath`. afterB1: `schema, runId, stage, sentAtUtc, pins, inputsSha256, checkpoint{blockNumber, blockHash, timestamp, snapshot, frontier{admissions, records, bindings, publications}, indexGeneration, registryEpoch, coreCodeCommitment, realmId} (ACTUAL post-B1 seal), setupReceipts{step1,A1,A2,B1} (+ `B1.folderBind=false`), observedOrdinals, noBPlacement{bHeadStateAtPlacement, bSwapsScopeCount, bOnlyLensStatus} (all must be 0), ackPath`.
- **Ack → runner (top level):** beforeFixture: `schema, runId, stage, decision (ACK|REFUSE), reason, issuedAtUtc, pins (echo), contextSha256, inputs{types (6), fixture.<6>.{typeId, body, id}, subject{FILE_QUOTE, salt, creatorPrincipal}, roles, lenses{LENS_A_FIRST, LENS_B_FIRST}, expect{A_FIRST, B_FIRST} (12 fields each), placementExpect (6 fields), ordinals{placementAdmission, placementPublication, aHeadAdmission, aHeadRevision, bHeadAdmission, bHeadRevision, postB1Frontier, registryEpoch, indexGeneration}}, inputsSha256`. afterB1: `schema, runId, stage, decision, reason, issuedAtUtc, pins, contextSha256, inputsSha256 (must equal the first ack's), sealedCheckpoint (must equal `context.checkpoint` field by field)`.
- **Consume vs compare:** `lenses`, `expect.*`, `placementExpect` are CONSUMED verbatim as the paid calls' inputs after strict field-by-field comparison with the mirror; `types`, `fixture`, `subject`, `roles`, `ordinals` are strictly compared (lowercase-hex / decimal-string equality, arrays order-sensitive); `observedOrdinals`, `frontier.admissions`, `registryEpoch`, `indexGeneration` are compared against `inputs.ordinals` at afterB1. Any difference `CONTROLLER_INPUT_MISMATCH:<dotted path>`; missing/extra/wrong-typed field `CONTROLLER_ACK_MALFORMED:<path>`; runId/stage/pins/hash mismatch `CONTROLLER_ACK_MISMATCH:<path>`; `REFUSE` → `CONTROLLER_REFUSED`. Every failure ⇒ NO downstream send, the cell recorded with that code, exit 3.
- **Retention:** `<scratch>/controller/pins.json`, `context-beforeFixture.json`, `context-afterB1.json` (as sent), `ack-beforeFixture.json`, `ack-afterB1.json` (as received, verbatim) — separate files written before the next step, never copied into `measure.json`; the packet carries only `report.gating`, the pins and the comparison results.
- Out of scope, as instructed: no new oracle framework; no candidate verifier (Reconstructor, `verify-fixture-map-b.mjs`) as controller; no retrospective sealing of `5960336` or any completed run; consumer, `PaidResult` log and the three selected cells unchanged; no heavy slot.
- **Waiting for Root's binding (accept / amend the interface) before any runner code.** Once bound: runner integration + injected-controller unit tests (ack/refuse/timeout/mismatch paths, no chain) + docs, source-only, then an internal review and a compile-only slot request.

## C one-placement paid slice committed: `55bd523` → pin **`d4e913e`** on `fable/2026-09-13-road-c-lab` (source-only, UNRUN; 2026-09-13 11:26:12 UTC) — internal reviews next

- On top of `97e84c9`; `src/`, `vendor/`, `evidence/`, `measurement-results.md`, `test/FixtureSeeder.sol` untouched; untracked `lab-c/out` preserved. Runner `typed-joined`: B1 = RECORD + HEAD only (FOLDER bind + empty body removed, asserted in calldata); post-B1 seal (snapshot, block/hash/timestamp, basis reads); A placement provenance and "no B placement" proven from raw public reads (B's binding row rev 0, empty binding history, `[B]`-only lens ABSENT); four paid rows first-from-revert with `evm_setNextBlockTimestamp(seal+1)`, unrelated pinned caller (index 3, asserted unrelated + funded at a pinned block), block tx list + `PaidObserved` log + same-caller `eth_call` replay retained before the next revert; ordering/agreement/cost-disclosure asserted; 27-field `abstractResult` (RPC_OBSERVED; UNKNOWN collapse on any failed self-check); optional `typed-joined/a1-without-placement` paired control (exact name only); `--anvil` required before any chain call. Consumer (`test/MeasurementConsumer.sol`, stateless, no candidate id/table-lib imports): shared basis/selection/admission/evidence/closure checks with value/scale/observedAt/note compared against INPUTS; list-only COMPLETE one-row window + placement provenance (no equality with the content author); point does no directory lookup; consumer-local decoders over public `getRecord`; framed c32 surface kept. `test/FaultyReads.sol` (one armable bad ACTUAL reply) + `MeasurementFixture.b1HeadOnly()`; negatives for wrong/missing Pair/Item, wrong author/proof category, wrong head/revision/fields, second placement (via the unchanged `b1()`), mixed bases, incomplete coverage, point/list disagreement, wrong placement coordinate; faulty-reply controls. `node --check` clean; `node --test` 22/23 — the one failure is the PRE-EXISTING artifact-pin helper test against the stale retained `out` symlink (fails identically at `97e84c9`; Root's 6/6 used fresh artifacts) — left alone, flagged. Expected Forge 64 (+17).
- Same standard as B applied (Root's four B findings: value/observedAt/note inputs, UNKNOWN collapse, txIndex 0 + single tx + pinned timestamp, actual-bad-reply controls; anvil-only). C's native MUD layouts and its 288-byte Quote frame are unchanged — still the thing being compared.
- Correction (2026-09-13 11:27:34 UTC): `55bd523` accidentally captured the untracked `lab-c/out` symlink (it points at a session scratch directory); `d4e913e` untracks it again (`git rm --cached`, follow-up commit, no force-push); the symlink stays on disk untracked as instructed; reviewed content unchanged.
- Two internal read-only reviews (consumer/tests; runner) start now against `d4e913e`; findings applied source-only and re-pinned, then the pin is handed to Root and a finite compile-only slot requested. Instrumentation: `PaidObserved` ≈ 43 words (ESTIMATED 12–14k gas) kept as disclosed consumer overhead, mirroring the B decision.

## C reviews of `d4e913e` returned (2026-09-13 11:38:13 UTC); findings being applied before the handoff pin

- **Consumer/tests review: READY-FOR-COMPILE.** Verified against C's source: `RECORDS/ADMISSIONS/EVIDENCE` table ids, layouts and every decoded offset byte-identical to `LedgerTables.sol`; `getRecord` selector; absent-row semantics; proof-category rule matches how `ActionLib`/`ImportLib` record native vs signed vs imported evidence; evidence bounds exact; `highWater == basisAdmission` is the right C basis pin; `_window` COMPLETE/ended/rawTotal semantics match `LensReader.list`; `FixtureSeeder.b1()` untouched (its two-placement state is the "second placement" negative); ordinals/revisions/publication ids derived from the seeder are correct; provenance ≠ authorship (the old equality is gone); +17 tests. One MAJOR (coverage, not a wrong-pass): the evidence cross-checks that keep provenance independent of the reader (`AuthorMismatch`, `EvidenceBounds`, `AdmissionShape`, `ProofShape`, `RealmMismatch`, `NotAtBasis`, `MalformedFrame`, `ShortReply`, `ReaderLedgerMismatch`) are unreachable with the honest Ledger → faulty-reply modes and one test each being added. Minors being applied: a `reason` code on `PlacementWindow` so the PARTIAL-claims-COMPLETE control discriminates; `rawTotal` semantics documented; a misnamed test renamed; framed-surface drift noted; the sealed test contract pre-split to stay under EIP-170.
- **Runner review: GO-FOR-RUN-AFTER-COMPILE.** Verified: B1 = RECORD + HEAD from the retained raw calldata; seal reads persisted before the first revert; A provenance and no-B proofs are direct state reads at the sealed block; revert → re-snapshot → pinned next timestamp → head/nonce asserts; block tx list retained; ordering assertions cover block/parent/timestamp/index/single-tx/retained-before-revert with tests; replay from the paid caller at the receipt block; 27-field `abstractResult` with pure UNKNOWN collapse; paired control non-default; `--anvil` refusal before the lazy provider; ABI matches by name and order. One MAJOR being applied: the self-check now also pins the logged `kind` per operation and recomputes the commitment from the decoded fields (pure helper + tests, parse errors retained). Minors being applied: `web3_clientVersion` retained and required to be Anvil when an anvil-only cell is selected; `txpool_status` retained at each restore; provenance labels derived from observed actor/publication (throw on unknown); the `--prune-history 256` recipe bound stated; MANIFEST wording on when the unrelated-caller assertion runs.
- Next: re-pin, then hand the C pin to Root and request a finite compile-only slot (build + Forge tests, no Anvil).

## C HANDOFF PIN for Root's source review: `9a4e766` on `fable/2026-09-13-road-c-lab` (source-only, UNRUN; 2026-09-13 11:47:05 UTC) + compile-only slot REQUEST

- `9a4e766` = `d4e913e` + both reviews' findings applied (+~350/−60 across `script/measure.mjs`, `script/measure-helpers.mjs` + tests, `test/FaultyReads.sol`, `test/MeasurementConsumer.sol`, `test/MeasurementConsumer.t.sol`, `MANIFEST.draft.json`, `TODO.md`): self-check pins the logged `kind` per operation and recomputes the commitment from the decoded fields (pure helper, tests for wrong kind / forged commitment / replay≠log / no log / undecodable); `web3_clientVersion` retained and required to be Anvil before any state call; `txpool_status` retained and asserted empty at each restore; provenance labels derived from observed actor/publication; nine new faulty-ACTUAL-reply controls (`AuthorMismatch` via laundered `selectedBy`, `EvidenceBounds`, `AdmissionShape`, `ProofShape` ×2, `RealmMismatch`, `NotAtBasis`, `MalformedFrame`, `MalformedRecord`, `ReaderLedgerMismatch`; `ShortReply` is unreachable behind the exact static-length checks and is stated as such, not faked); `PlacementWindow(uint8 reason, …)` so the PARTIAL-claims-COMPLETE control asserts `WINDOW_NOT_ENDED` on an otherwise well-formed page; sealed test contract split (negatives separate) to stay under EIP-170. `src/`, `vendor/`, `evidence/`, `FixtureSeeder.b1()` untouched; `out` untracked. `node --check` clean; `node --test` 25/26 (the failure is the pre-existing artifact-pin helper test against the stale retained `out`, unchanged since `97e84c9`); expected Forge total **73** (47 at `97e84c9` + 26). Desk-checked only.
- **REQUEST: a finite compile-only slot for C pin `9a4e766`** — `forge build --sizes` then `forge test -vv` from a clean detached worktree at the pin (`git worktree add --detach`; the existing `lab-c/out` is NOT used or touched — run-owned `--out`/`--cache-path` under `…/scratchpad/build/lab-c-pin-9a4e766/`), offline pinned solc 0.8.30, two threads, alarm watchdogs bounded by the slot deadline, no Anvil/deployment/measurement, <1 GB new scratch, actual UTC launch/end, exit codes, test counts, sizes and verified cleanup reported; failures kept. ≈10 min. Any 20-minute window Root grants; I will not launch before the lease is recorded. Compile risks the author listed (a `MALFORMED_FRAME` control that assumes `abi.decode` tolerates a trailing byte — a panic would still be a refusal; the 7-tuple `PlacementWindow` decode in tests; interface params typed `LensReader.Lens/Cursor calldata`; the UDVT-as-bytes32 `getRecord` selector; test-contract sizes) are exactly what the build settles.
- Launcher prepared for C (2026-09-13 11:48:42 UTC): C's tests read the Ledger artifact JSON from `FOUNDRY_OUT` and `foundry.toml` only permits reads under `./out` (the issue Root hit), so the launcher generates a RUN-OWNED Foundry config in the scratch dir — identical to the pin's `foundry.toml` except `out`/`cache_path` point at the run-owned scratch and `fs_permissions` additionally permits reading that directory — and exports `FOUNDRY_CONFIG`/`FOUNDRY_OUT`; nothing tracked in the pin is modified (verified offline with `forge config --json`, no compiler run: effective out/cache/fs_permissions/remappings/solc 0.8.30/via-IR as intended; heavy 0 after).
- The B controller integration still waits for Root's binding of `CONTROLLER-INTERFACE.md` (`7c292e0`); no B code has been written for it.

## 11:50 handoff acknowledged (2026-09-13 11:51:34 UTC) — interface bound; C slot 11:55–12:15 gated on Root's GO

- **Controller interface bound with four clarifications — accepted and being implemented (B, source-only):** (1) `chain.rpc` in the beforeFixture context; (2) `placementRevision` added to the sealed ordinals so afterB1 compares against an existing input; (3) afterB1 stays immediately after the checkpoint/basis reads and its context drops `noBPlacement` and the placement-derived `observedOrdinals` (Root's controller reads those independently before ACK; the candidate's own placement/no-B checks still run afterwards); (4) the pinned arm-input file lists the controller's local module dependencies as hashes and the runner verifies every one before importing the controller (the only part of that file the runner parses), or the module is self-contained. Controller retains its own observations/acks; runtime/artifact/role inputs are verification inputs, not candidate-authorized truth. Implementation: CLI pins, canonical-JSON hashing, the two awaited hooks at the bound points, strict comparison + consume-from-ack, fail-closed codes with 120 s timeout, separate retained files under `<scratch>/controller/`, injected-controller unit tests (no chain); contract doc and example updated first. Root implements the controller/vector side in `planning-warroom-oracle`.
- **C compile slot 11:55–12:15 UTC at `9a4e766` accepted as written:** launch ONLY after Root's two source reviews return GO here; clean detached checkout, fresh run-owned out/cache (run-owned Foundry config as noted above), cached solc 0.8.30, two threads, finite watchdog, no Anvil/deployment/measurement, the old untracked `out` symlink untouched (the pin worktree is separate). The launcher also runs the fresh-artifact Node helper tests (`OUT_DIR=<run out>`, ethers pinned) after the Forge suite; if the artifact-pin helper test fails on AST-id drift, immutable ids will be mapped by variable name in a new recorded pin — never guessed to make the test pass, and no source fix inside the slot. Actual counts/exits/sizes/cleanup will be reported, not the expected 73. No late launch.
- Noted: the independent seven-row semantic manifest `paid-neutral-expectations.json` sealed 10:54:16 UTC (sha256 `ff7c4fc7…7795`) fixes meanings only; the matched rollback control's exact input is still to be sealed; no finalist gate waived; no comparison receipt authorized.

## 11:54 C cross-review acknowledged (2026-09-13 11:52:41 UTC) — diagnostic build of `9a4e766` launching at 11:55; fixes go into a successor pin

- Decision: the 11:55–12:15 window is used for the already-reviewed **`9a4e766`** diagnostic build (compile/sizes/Forge tests/fresh-artifact Node helpers; no Anvil), recorded here before launch as the pin; its result retains Root's open qualifications. A corrected successor could not be applied, reviewed and pinned inside the window, so the three fixes are being applied now (source-only) for a NEW recorded pin and a later slot: (X1) `_select`/`_provenance` compare the returned `Resolution.basis` with the pinned basis, with actual wrong-basis reply controls (not an admission/high-water change); (X2) `_window` compares the returned cursor's index generation, rules epoch, Core commitment, scope and Lens hash with the requested pinned values, one control per field; (X3) `paidObservationMatch` compares the entire log and replay tuples including unpredicted fields such as `hydrated` and recomputes the replay commitment as well as the log's, with a per-field mutation regression. Consumer/runner only, no Core.
- Launch record: launcher started 2026-09-13 11:52:41 UTC gated on 11:55:00 UTC real clock; clean detached worktree `/Users/james/Code/EFS/planning-road-c-pin` at `9a4e766`; run-owned `…/scratchpad/build/lab-c-pin-9a4e766/{out,cache,logs,foundry.run.toml}`; cached solc 0.8.30; `--threads 2`; alarms bounded by 12:15; the old untracked `lab-c/out` symlink untouched. Actual times/exits/counts/sizes/cleanup follow.

## Routine questions for Codex (not for James)
4. For the checker owner: the B script reads all seven storing-Consumer slots at the paid-read receipt block, while `rpc-observed-expectations` freeze exactly {lastTarget, lastRevision, lastValue} for `paid-quote-read` and {lastCount} for `listing`. I am changing the script to read only those at the receipt block (the rest one block later). Confirm that is the intended frozen call set, or relax it, before pinning targets.


1. For the B-vs-C probe, is the Evidence cell shape above acceptable as the *shared* authorship closure for both arms, so the comparison holds the closure constant?
2. Do you want the by-Type and by-author lists inside the mandatory IndexModule set for the probe (my v1.1 says yes) or measured as the protocol's optional-index premium?
3. `ebc7d54` has no worktree; shall I leave that checkout to you (Road A) rather than create one from my side?

## Log
- 2026-09-13 11:52:41 UTC: C cross-review (3 fixes) acknowledged; `9a4e766` diagnostic build launching at 11:55; fixes in progress for a successor pin.
- 2026-09-13 11:51:34 UTC: interface bound (4 clarifications) → B runner integration started; C slot 11:55–12:15 accepted, launch gated on Root's GO; launcher runs Node helpers on fresh artifacts too.
- 2026-09-13 11:47:05 UTC: C handoff pin `9a4e766` (reviews applied, 73 Forge tests expected) pushed; compile-only slot requested for it.
- 2026-09-13 11:38:13 UTC: C reviews READY/GO (one MAJOR each: log-kind/commitment self-check; evidence cross-check coverage); fixes in progress.
- 2026-09-13 11:27:34 UTC: C pin is `d4e913e` (`out` symlink untracked again after `55bd523` captured it); reviews running.
- 2026-09-13 11:26:12 UTC: C one-placement paid slice committed `55bd523` (source-only, unrun); internal reviews starting.
- 2026-09-13 11:00:53 UTC: controller interface proposal `7c292e0` returned for Root to bind; sizes fixed; six Type descriptors in the map (verify PASS 71/71). No runner code until bound.
- 2026-09-13 10:54:29 UTC: 10:53 GO acknowledged; README size + three descriptors in progress; controller JSON interface being drafted for Root to bind before any runner code.
- 2026-09-13 10:52:47 UTC: C worktree verified at `97e84c9` (tracked clean, untracked `out` preserved); C paid consumer/runner one-placement work started (source-only, test-first, uncommitted until my review).
- 2026-09-13 10:51:31 UTC: 10:50 handoff acknowledged — C paid consumer/runner ownership at `97e84c9` (source-only, test-first); B controller delta waits for the hook finding.
- 2026-09-13 10:29:26 UTC: `road-b.md` §10 (v1.3, uncommitted for Codex to publish) summarises the paid slice's state: compiled/tested, inputs delivered, unmeasured.
- 2026-09-13 10:29:03 UTC: comparison inputs `b2aa07d` pushed (fixture map + verify PASS 63/63, expectation-row mapping); awaiting Root's review and the controller's seals.
- 2026-09-13 10:18:32 UTC: lab docs `9c19164` record the compiled pin; source-only comparison inputs started (B fixture map with formulas + verify script, expectation-row mapping, arm-input fields) for the run controller's independent seals.
- 2026-09-13 10:17:49 UTC: compile slot for `2859147` done — build exit 0 (10:16:46–10:17:02 UTC), 52/52 tests, heavy 0, slot released.
- 2026-09-13 10:10:11 UTC: 10:10–10:40 compile-only lease accepted; intended pin `2859147` pending the focused review verdict.
- 2026-09-13 10:09:18 UTC: corrected pin `2859147` returned for Root (four fixes + anvil-only scope); focused compile-risk review running.
- 2026-09-13 09:54:38 UTC: Root's review of `980a017` acknowledged (compile-eligible; four comparison fixes + anvil-only scope in progress).
- 2026-09-13 09:51:39 UTC: 09:50 handoff acknowledged (keep PaidResult log + disclose; three-cell selection; B light-only); Root's C slot respected, heavy 0.
- 2026-09-13 09:44:30 UTC: handoff pin `980a017` (paid slice with all review findings applied) pushed; awaiting Root's source review; ready for a compile-only slot when granted.
- 2026-09-13 09:39:03 UTC: reviews of `5c61d22` READY/GO (one MAJOR label fix in the runner); fixes being applied; handoff pin to follow.
- 2026-09-13 09:29:54 UTC: B paid point/list slice committed `5c61d22` (source-only, unrun); two reviews running; Root's source review to follow.
- 2026-09-13 09:06:32 UTC: Root accepted the receipt review (GO for retained RPC/signature evidence); row-2/mock narration corrected in my texts.
- 2026-09-13 09:04:10 UTC: B counterpart of the point/list slice started (source-only; consumer + runner + negative tests).
- 2026-09-13 09:02:51 UTC: `road-b.md` §9 now carries the measured six-cell costs and refusals (qualified: repaired profile, not parity); reading `matched-cost-scope-review.md` and the sdk-fixture appendix for the A1/A2/B1 point/list slice.
- 2026-09-13 09:02:27 UTC: receipt diagnostic at `ca1a228` done (09:00:00–09:00:05 UTC, 6/6 cells, 0 mismatches); evidence `5891cc5` pushed; slot released.
- 2026-09-13 08:56:30 UTC: 09:00–09:20 receipt-diagnostic lease accepted; six-cell run launching at 09:00:00 UTC.
- 2026-09-13 08:53:00 UTC: lab docs `afa8392` record the compiled pin; bounded 4-cell receipt run at `ca1a228` requested (own handoff).
- 2026-09-13 08:51:18 UTC: compile slot for `ca1a228` done — build exit 0 (08:50:24–08:50:39 UTC), 43/43 tests, heavy 0, slot released.
- 2026-09-13 08:29:16 UTC: reviews of `3a30fe2` READY/GO; hardening + runner fixes committed `ca1a228`; NEW compile slot requested for `ca1a228`.
- 2026-09-13 08:19:45 UTC: `road-b.md` v1.2 (uncommitted, for Codex to publish): §9 adds the second falsifier round (F1–F5 outcomes and repairs, codehash-pinning limit stated, cost claims marked UNBUILT).
- 2026-09-13 08:18:37 UTC: F5 repair + stack fix + runner fixes committed as `3a30fe2`; two reviews running; new compile slot to be requested on READY.
- 2026-09-13 08:00:38 UTC: compile of `3c6947d` failed (stack too deep in the publish path); slot released; fix folded into F5.
- 2026-09-13 07:56:14 UTC: runner NO-GO acknowledged; fixes queued behind F5.
- 2026-09-13 07:49 UTC: 07:50 instructions acknowledged; F5 confirmed by construction, repair assigned; 08:00–08:30 compile slot accepted.
- 2026-09-13 07:31 UTC: runner patch `3c6947d` committed and pushed; tidy-ups in progress.
- 2026-09-13 07:31 UTC: review of `aaecfed` READY-FOR-COMPILE; tidy-ups and runner patch in progress; slot request to follow.
- 2026-09-13 07:21 UTC: falsification `71e689b` and repair `aaecfed` committed and pushed; review and runner adaptation running; 07:05 slot unused.
- 2026-09-13 06:55 UTC: 06:50 handoff acknowledged; runner repairs cherry-picked; authority/Type-identity falsify-then-repair scope running.
- 2026-09-13 05:24 UTC: review fixes committed `e77f36d`; B compile + chain lease requested.
- 2026-09-13 05:19 UTC: review of `691e341` received (no blocker; one major, minors) and forwarded for fixes; checker call-set question posted.
- 2026-09-13 05:08 UTC: B measurement scope committed `691e341`; independent review running; no lease requested yet.
- 2026-09-13 04:23 UTC: your four B-run qualifications mapped into the running source scope.
- 2026-09-13 04:23 UTC: resumed; checkpoint and worktrees read; no build; B next scope assigned (source only).
- 2026-09-13 02:06 UTC: session rate limit hit; C helper split not written (diagnosis recorded); handover state above.
- 2026-09-13 02:00 UTC: C Ledger fits (23,145 B), tests blocked by the 88 KB test helper (splitting); B diagnostic clean, evidence `322b320`; lease released.
- 2026-09-13 01:29 UTC: C decomposition committed `731200d`; combined C build #3 + B run lease requested.
- 2026-09-13 01:26 UTC: B hang diagnosed (ethers receipt race), script fixed `a16d7d4`; new B lease requested.
- 2026-09-13 01:18 UTC: lease released; C compiles (`bc6beee`) but Ledger 456 B over EIP-170; B diagnostic hung and was stopped, partial evidence retained.
- 2026-09-13 01:05 UTC: C review received (blocker + 4 majors) and forwarded to the running C repair.
- 2026-09-13 01:04 UTC: B measurement evidence persistence committed `6104a6f` (run pin).
- 2026-09-13 01:00 UTC: C build #1 failed on via-IR stack depth (vendored assembly unannotated); repair cycle running; handoff answers adopted.
- 2026-09-13 00:52 UTC: measurement controls committed `369b770`; bounded Anvil run requested.
- 2026-09-13 00:51 UTC: Road C lab committed `f2e4051` and pushed; C compile lease requested; process-count line corrected.
- 2026-09-13 00:50 UTC: compiler lease used and released early; `dcc7b94` green (24/24, Ledger 16,699 B); Anvil run request to follow after script fixes.
- 2026-09-13 00:38 UTC: Road B lab committed `727291a` on its branch and pushed; lease requested above; independent code review running.
- 2026-09-13 00:17 UTC: four pre-seal checks relayed to both builders; awaiting source-ready returns.
- 2026-09-13 00:08 UTC: Codex handoff (`b2cd79a`) applied; Road C source prep assigned with owned worktree; questions 1–3 answered.

- 2026-09-12 23:40 UTC: acknowledged (`6219a98`); specialists launched.
- 2026-09-12 23:52 UTC: `road-b.md` v0 recorded (`c8ec8d1`), written before reading Road A/C material.
- 2026-09-12 23:55 UTC: publication rule adopted; further edits uncommitted for Codex integration.
- 2026-09-12 ~23:50 UTC: Road C recorded by the specialist; reviewed above. Cross-read Road A, the Road B review, sdk-fixture and files-journey.
- 2026-09-12 23:56 UTC: Codex checkpoint answered; road-b v1.1 fixes; C desk-kill withdrawn.
- 2026-09-12 ~23:58 UTC: evidence ledger received; Road B §2 now carries the qualified control/native rows.
- 2026-09-12 ~23:52 UTC: `road-b.md` v1 written in place (uncommitted) answering the five falsifiers; lab builder re-briefed with the same deltas.
- 2026-09-12 ~23:47 UTC: Road B lab worktree created (`fable/2026-09-12-road-b-lab`), source preparation delegated; no build. Waiting on extractor and Road C results to finish Road B cost centers and to cross-read.

- 2026-09-13 00:46:47 UTC: LEASE START (slot 00:45–01:15 UTC, owner EFS v2 Dev/Fable). Build #1 from `727291a`: `timeout 600 forge build --use <cached solc 0.8.30, sha256 738dcdc6…> --offline --threads 2 --out /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/out --cache-path /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/cache`; watchdog = timeout(600s); log /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/logs/build1.log; no Anvil.
- 2026-09-13 00:46:58 UTC: build #1 retry — `timeout` absent on macOS (exit 127, nothing ran); watchdog is now `perl -e "alarm 600; exec @ARGV"`.

- 2026-09-13 00:57:47 UTC: SERIAL LEASE ACK (00:55–01:25 UTC, owner EFS v2 Dev/Fable): C compile/tests/size first from `f2e4051`, then the B diagnostic Anvil run; never simultaneous. C build #1: `perl alarm 600 forge build --use <cached solc 0.8.30> --offline --threads 2 --out /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-c/out --cache-path /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-c/cache`; log /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-c/logs/build1.log; launch state anvil 0 / forge 0 / solc 0.
- 2026-09-13 01:05:43 UTC: B DIAGNOSTIC RUN START (reordered ahead of C build #2: C repair not back, nothing compiling, never simultaneous). `perl alarm 1500 node script/measure.mjs --anvil` at `6104a6f`, FOUNDRY_OUT=/private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/out, scratch /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b, ethers 6.15.0 (fable worktree), log /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/logs/measure1.log.
- 2026-09-13 01:16:41 UTC: B DIAGNOSTIC RUN STOPPED by me — hung at block 19 for 10 min after setup + 2 persisted cells (Anvil responsive, no script output); node 25963 and anvil 25989 killed, partial report retained as /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/measure1-partial.json; diagnosis after the lease (suspect wallet nonce cache after evm_revert). Proceeding to C build #2 with the slot.

- 2026-09-13 01:46:46 UTC: LEASE ACK (serial C build #3 then B diagnostic, through 02:15 UTC). C at `731200d` + the reviewer-flagged `EncodedLengths.wrap` repair in RawWriteDenial.t.sol; launch state anvil 0 / forge 0 / free 265 GiB; watchdog perl alarm per command.
- 2026-09-13 01:59:09 UTC: C build #3c: deployables fit (Ledger 23,145 / ImportLib 19,861 / IndexModule 10,303 / LensReader 10,585) but the test helper `Fixture` is 88,623 B runtime, so C tests still cannot deploy; B DIAGNOSTIC RUN START at `a16d7d4`, outer watchdog perl alarm 900, log /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b/logs/measure2.log.
- 2026-09-13 08:00:09 UTC: COMPILE SLOT START (08:00–08:30, compile/tests only). Pin `3c6947d` built from a clean detached worktree /Users/james/Code/EFS/planning-road-b-pin (no uncommitted files); `forge build --sizes` + `forge test`, cached solc 0.8.30, 2 threads, watchdogs perl alarm 480/300, scratch /private/tmp/claude-501/-Users-james-Code-EFS/089e21d8-6171-40d6-9cac-1d2e941506f9/scratchpad/build/lab-b-pin-3c6947d; heavy processes at start: 0.
