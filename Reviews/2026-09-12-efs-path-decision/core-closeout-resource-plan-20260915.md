# Core closeout: final resource envelope and whole-operation economics

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Close the remaining packed-count defect and report what the integrated prototype can actually write/read at ordinary gas limits, including whole Files operations rather than marginal index costs.

**Architecture:** One bounded final runner reuses the reviewed compact SDK, receipt recorder, full required index, application and query fixtures. Receipts establish total gas; bounded call trees and known-slot readbacks qualify attribution without pretending to establish an irreducible minimum.

**Spec:** [[core-closeout-results-20260915]], packets3/6; [[core-design-audit-20260915]]. Run final measurements after index replay, query, Type/authority, live Files, the [[core-closeout-tags-plan-20260915|bounded stance-tag experiment]] and SDK changes are reviewed. Pin the final source before reporting.

## Global Constraints

- Existing prototype and one source/build/chain owner; parent owns canonical main documents/publication. No owner-demo mutation, production repo, public transaction/deployment, package install, Fable, unbounded tracing/history or architecture tournament.
- Ordinary transaction gasLimit15,000,000, hard16,777,216 unchanged; runtime24,576/initcode49,152. Failures remain failures, not silently retried under a larger cap or weaker index/validation profile.
- Preserve mandatory rules, required index families, complete rollback, exact identities, retained authority/history/guard evidence and qualified reads. Neither small fixtures nor isolated maxima prove a universal joint envelope.
- Whole user operations include every transaction needed by the actual recipe; distinguish inline and descriptor-backed content, transaction count and atomicity. Do not preseed required artifacts to lower a named operation's reported price or sum separate fixture transactions and call that an atomic File creation.

## Task 1: Fail closed at the public Record occurrence width

**Files:** minimal `src/Ledger.sol` existing-record mutation guard and focused `test/RecordOccurrenceBounds.t.sol`; source/size report. No width redesign.

- [ ] Reproduce current packed-count truncation using test-only storage injection over a real retained short Record. At the documented32-bit count maximum, duplicate PUBLISH/REUSE currently carries beyond the public getter's width.
- [ ] Before the common existing-record increment, reject when the full shifted count word is at least`type(uint32).max`; do not cast before comparing. Keep new Record creation, body/identity, zero-occurrence retention and ordinary reuse unchanged. Use an unused exact bounds code after checking the final source.
- [ ] MAX refuses with unchanged nonce, counters, evidence/admissions, low metadata/body and required postings. MAX−1 permits one increment; a two-occurrence publication then refuses on its second leaf and rolls the first prefix back. An injected high count bit must refuse rather than pass a truncating check. Ordinary fresh1→withdraw0→reuse1 remains valid.
- [ ] Report these as arithmetic/atomicity falsifiers, not billions of real transactions. Record occurrence and existing binding-revision widths are explicit prototype capacities, not100-year layout choices. Future widening needs coordinated ABI/raw-decoder/layout review.
- [ ] Focused checks and normal deployable sizes, exact commit/report and independent task review; no broad rerun or public-state mutation.

### Measured fit extension — September15

At reviewed replay source7af2695, the checked repair is24,603runtime bytes; the proven-unchecked increment after the full-word guard is24,595, still19over24,576. Both pass the eight injected arithmetic controls but are not deployable. The earlier7-byte margin is exhausted. Keep these diagnostic results; do not count them as a completed repair.

**Additional files:** existing `src/PublicationSupport.sol` and one focused `test/PublicationPreparation.t.sol` if existing foundation/import/digest tests do not cover the seam. A small declarations-only source is permitted when needed to share wire shapes, but no new storage base or mutable module registry. Existing publication entrypoint signatures, domains, roots0–15 and historical codecs stay intact.

- [ ] Extract the coherent native/signed/guarded/import **preparation** unit into the existing constructor-created, codehash-pinned support contract. Ledger keeps `_run`, `_beginPublication`, nonces/retries, all canonical writes, publication locking, source-evidence retention, ordered apply/index/final checks and execution/epoch rechecks. Preparation cannot emit admitted effects or write Ledger storage.
- [ ] Use explicit typed fixed-helper DELEGATECALL for this context-sensitive preparation, never a caller-selected target or prepared-publication ingress. `address(this)` is the actual Ledger/proxy and `msg.sender` remains the original caller. Derive the canonical source from `address(this)`, not the relayer: today's normal-STATICCALL `checkReadSet` caller inference cannot be reused unchanged in delegate context. Keep public pure/view digest/read-set hash getters on their existing bounded read-only path.
- [ ] Share one bounded static preparation result, at most the17existing pre-mutation fields: author,author32,creator,imported,proofKind,v,nonce,deadline,r,s,acceptanceProfile,indexObligations,actionsHash,execution,intentHash,readsHash,format (544ABI bytes). Reject wrong-size output and bound error copying. Counters/ordinals/publication-ID/first-admission are initialized only by Ledger. Encode the guarded read-set preimage once in Ledger and retain that exact byte array; do not return it or the whole mutable `Pub` through a second dynamic helper codec.
- [ ] Shared preparation covers native batch/convenience paths, legacy signed, guarded native/EOA and source/destination import validation. Native identity uses the actual caller; imported CREATE retains the verified source creator. Source signature is evidence, destination authorization remains separate. Keep legacy direct-only restrictions and all existing digest bytes, exact signature checks and named error precedence, especially read-set hash before stale-coordinate errors. No removal of supposedly dead legacy paths: current SDK/archive use them.
  - Admission preparation may reject zero or more than64actions with existing `E_BOUNDS(0)` before hashing/profile/signature loops. Preserve realm/code/deadline and in-bound hash-before-stale checks; a multiply-invalid oversized-plus-bad-signature candidate need not retain its old error precedence. Public `acceptanceProfileOf` candidate hashing is unchanged. Bound encoded input before expensive decoding/copy where the typed boundary permits, without inventing a general ingress decoder.
- [ ] Preparation only performs bounded STATICCALL reads, hashing and signature verification. No SSTORE, CREATE or authority-bearing CALL occurs in preparation. Preserve fixed source/helper pins and explicit external return bounds. The existing bounded actions/read sets constrain work; use a fixed12,000,000 outer preparation ceiling without demanding that unused ceiling be present or paid, alongside existing narrower dependency caps. Wallet support is not implemented in this task: preserve a seam where its later bounded STATICCALL can observe the actual Ledger/proxy. Do not widen the shared9.8M index budget or15M transaction ceiling.
- [ ] First measure the complete extraction with the count guard under unchanged compiler/settings, including constructor-created helper initcode and every affected real deployable. If it still fails, report the remaining deficit before trying another extraction. No iterative unrelated byte-golf. Future wallet/carrier feature fit remains unproven until those exact implementations are measured.
- [ ] Focused controls compare unchanged independent legacy/guarded digest vectors, native caller/relayer separation, proxy origin and stale execution, imported source creator and wrong destination authorization, duplicate nonce/retry, helper wrong-code/returndata, hash-before-head refusal and no state after later rule/final-index failure. Retain injected count RED/GREEN. Run covering foundation identity/guard, import, digest and ordered-acceptance files once, not the whole prototype. Capture exact warning diagnostics on that ordinary compile.
- [ ] Preserve existing populated-upgrade/archive interpretation using the current focused fixtures; this extraction changes runtime/execution identity, not storage roots or historical bytes. Signature, nonce, current policy and archive authority cannot be inferred merely from helper output. Final native-proof pins and whole-recipe gas measurements use later final reviewed code, not the old7af2695 runtime. Do not relabel old paid receipts as the extraction's measurements.

This is a reversible implementation boundary needed to complete the authorized count repair, not a permanent architecture promotion. Later carrier and ERC1271 implementations retain their own task/review gates.

## Task 2: Cold integrated receipts, limited attribution and feasible combinations

**Files:** one `script/core-final-resource.mjs` and bounded evidence packaging using existing environment/SDK/fixtures. Evidence in `core-closeout-resource-20260915/`. Do not build a new benchmark framework.

- [ ] Record exact source/artifact/runtime/execution/manifest/profile hashes, pre/post block hashes/counters, raw signed inputs and receipts, body/ref/field counts, full read-set preimage/dimensions, fresh/hot/dedup conditions, transaction/signature counts and canonical effect readback. Separate deployment, Directory/Type/target registration and all setup totals. All failed receipts remain retained.
- [ ] Actual named41-byte Files create/edit, fresh stable-File and selected-revision tags, existing-Concept controls, fresh rename, remove/restore and tag removal. Measure the SDK's actual atomic publication recipe. Also exercise its small descriptor-backed content recipe when distinct: count every backing-byte transaction and state its visibility/atomicity boundary. Do not label an INLINE document a descriptor upload or a descriptor index entry fetched-byte verification.
- [ ] Carry the separately reviewed new-purpose stance-tag whole-action/query measurements into the final table, with exact source/profile equivalence checked; refresh only affected representative rows if later code changed them. Keep legacy mask removal distinct from new SILENT/retraction, and closed-Lens Concept lookup distinct from all-author discovery. No automatic UI-default or permanent-profile adoption.
- [ ] Real native application: final-index nine-leaf Character+7Items+Equipment acceptance and small update, plus later-rule/app rollback. Reuse the existing Files consumer where compatible, accurately distinguishing native`execute` after app-side read checks from Ledger`executeGuarded`. No new app architecture solely for this benchmark.
- [ ] Include one small paid exact-Record typed read using the existing `NotePointReader` or an already-compatible final consumer, alongside the qualified mounted/path read. Label the different guarantees: known immutable Record plus identity/admission evidence does not perform path/Lens/HEAD selection, discover candidates or establish current write authority. Refresh the small representative row on final sources; the earlier122,495gas Note receipt is historical context, not a final-source result. No new reader architecture or separate benchmark campaign.
- [ ] Joint body case: fresh8,192-byte Record with8checked refs,4scalar fields and one digest; nonzero-byte density disclosed. Price first/second/later shared-key inserts and a normal small guarded version. These are whole Records, not whole Files. Registered Type/eight target Records are explicit setup.
- [ ] Actual atomic File create or rename with Name255 and its normal complete guard set. Earlier separate Name retention and binding receipts do not satisfy this row.
- [ ] Actual64-principal×4-coordinate guard plus small mutation, complete retained preimage/readback, same-preimage dedup control and a populated/changed-head miss. Submit one maximum-body/eight-ref/full-field plus64×4 combination as a deliberate joint falsifier. Keep its success/refusal under15M; no claim that individual maxima imply combined support.
- [ ] Small low-selectivity paid folder/tag scans: real selected HEADs/Names/tag negatives, short and255-byte Names, Lens widths1/64, candidate budgets1/4. Use the owned accumulator to qualified exhaustion; report every page and total gas, not just the first page. Interleave unrelated writes and preserve relevant-folder-change refusal. Reuse reviewed query evidence when exactly source/profile-equivalent, rather than rerunning large seeds.
- [ ] Replay the largest actually admitted publication by leaf count and the heaviest body/ref publication if different. The existing55CREATE and nine-leaf Equipment recipes are starting controls;64-action archive retention is not a64-leaf Ledger admission. Keep old write, replay, catchup and cutover costs separate. Failure of whole-publication replay under15M remains an explicit rebuild limit, not a complete replacement claim.
- [ ] Reuse SDK3's matched old-mapping/additive-carrier results for empty/1×1/8×4/64×4 guards, first/repeat/miss/readback/upgrade/archive. Signatures/addresses/execution pins differ between builds, so compare logical guarantees and retain calldata density instead of claiming byte-identical transactions. Include all constructor/CREATE/pointer costs once.

### Attribution without false precision

- [ ] Exact top line is receipt gas. For at most six ordinary create/tag/control transactions, collect bounded callTracer output: timeout,512KiB streaming wire cap and256nodes; unavailable/oversize stays`TRACE_UNAVAILABLE`. No opcode fallback, max-query tracing or raised limits. Check the tracer's gas convention on one tiny control; never add nested frames twice.
- [ ] Report inclusive acceptor/index/final/support frames and explicit unallocated residual. A callback includes storage reads, extraction and validation; it is not exclusively index SSTORE gas. Internal body/authority/hash work cannot all be exactly separated from a call tree.
- [ ] Derive a bounded known-key inventory from final compiler layout/actions/declared posting keys, at most2,048candidate slots per selected ordinary transaction. Read exact before/after blocks: new, changed, cleared and unchanged. Separate Record/body, authority/history/guard, current heads/nonces/counters and index buckets. This is exact net change over the enumerated set, not proof of every intermediate/reverted write. Packed fields, zero writes, warmth and refunds prevent a naive slot-count pie chart. State any storage-gas estimate and its omissions explicitly.
- [ ] Optional prestate/state-diff support needs one separately bounded capability probe; it is not currently established. If unsupported, keep known-key attribution rather than inventing precision or building a profiler. Isolated quote overhead may remain unallocated if whole-action deltas already bound its practical impact.

### Final handoff

- [ ] Three small tables: whole-operation receipts; measured versus estimated/unallocated attribution; successful and refused joint workloads at15M. Keep cold SDK HTTP/logical-call/request/response-byte work separate from paid gas and setup.
- [ ] Refresh [[core-closeout-network-preflight-20260915|Base fee inputs and public-provider controls]] for representative final recipes. Base local-gas+oracle-data/operator totals are labelled cross-venue models until deployed there. Era execution/ergs/pubdata remain unmeasured without a qualified Era run; do not fabricate a cost by multiplying Anvil gas by Era price. No public write or emulator installation is implied.
- [ ] End with a cold Files/native-contract journey on final sources, focused integrated regressions and independent scoped review. State what is proven within this prototype, what is practical but slower, what fails the chosen envelope, and what remains a deployment/owner choice. Do not call the six-packet closeout complete while an implemented guarantee still lacks its decisive test or a known correctness defect is left open.
