# Role-owner convergence findings

**Status:** first-wave read-only reports, integrated 2026-09-04; workflow proposals, not executed product tests
**Source basis:** September `12ef4c5`; August evidence remains separately qualified as in the [run checkpoint](./README.md).

## Convergence recommendation

Use the authorized September local Files control for the first write/recover
experiment, retaining August as comparison evidence. Do not require general
Explorer, a game runner, a package manager, media search or native mounts to
complete that experiment. Reuse shared exact/page/verified-byte/effect evidence
instead of allowing each product to invent a second Core or SDK.

These seven completed role reports did not demonstrate a new generic Core
primitive requirement. They do not prove all application requirements are met.
In particular, the sealed Files-only C0 inventory intentionally cannot admit
new Git/package/media/tag Types after genesis. Local adapter metadata is not
onchain application conformance.

| Role | Workflow and decisive requirement | Smallest useful follow-on |
|---|---|---|
| Project Manager | Fresh local run; create a small file; independently recover path, revision, verified bytes and complete directory. Transaction success alone is insufficient. | One joined evidence capsule, then exact SDK integration; thin File Browser only after its separate implementation authorization. |
| Git / Forge PM | Two same-Principal signers race an observed ref predecessor; stale CAS and ABA replay reject. Lost acknowledgement must distinguish a prior committed-but-superseded write from an uncommitted one. | Opaque small Git recovery packet carried as an ordinary File; stock-Git verification remains separate from Core admission. |
| Open Web App Store PM | Two attributed catalogs disagree while a publisher disappears; retained exact release/dependencies still verify, currentness may be unknown, no install/grant/execute effect occurs. | Read-only package adapter outside the C0 Type inventory and outside Files MVP. |
| Arcade PM | Explicit Play pins selection and complete closure; corrupt primary can fall back only to the same exact bytes. Missing selection/authority evidence cannot select another release. | Synthetic launch-preflight adapter with a stub runner and zero execution/live grants. No actual game assets or distribution clearance implied. |
| OS Drives PM | A timed-out second directory page cannot finalize a native alias table or negative-cache unseen names; mixed basis rejects. Open handles pin revision and verified cached bytes, never silently follow new heads. | No-mount callback fixture for publication gating, verified slicing, cache loss and read-only rejection; not three-host conformance. |
| Booru PM | One ocean concept applies to image/video; retain separate authors/polarities, revisions and complete same-basis page coverage. Missing denial is not agreement and live counts are not unique-post counts. | Profile-discriminated assertion/denial adapter; do not make global search a Files MVP requirement. |
| Media Library PM | A seek stays on the selected edition/representation/commitment after head drift. Wrong-generation bytes reject; equivalent byte commitments may reuse proofs without equating semantic identities. | Local media-selection adapter and private resume overlay; not onchain media admission, playback-codec or privacy conformance. |

## Concrete deltas needing integration, not another broad review

1. **Profile/sequence mismatch:** August split Type/Query and `ResultV0` are not
   September B0-bundled Types and five operation-specific SDK seams. August's
   completed source-lock check should not be listed as next work or relabeled
   September runtime evidence. SDK/Explorer lane reports are still pending.
2. **Replay/read-back intersection:** Git's trace makes prior acceptance lookup
   versus current expiry/revocation an explicit joined test. Recovering a prior
   receipt must not reapply effects or claim current authorization. This is a
   named implementation test, not a proven new protocol defect.
3. **App Store label identity:** the App Store PM reports conflicting inclusion
   of human version labels in Release identity in `Designs/open-web-app-store/architecture.md`.
   The proposed repair is separate attributed labels, preserving the same exact
   release bytes. Verify against that design's identity law before editing.
4. **App Store evidence wording:** broad “Stage B unrun” text must distinguish
   its historical checkpoint from later bounded experiments, without declaring
   the product implemented. Confirm exact evidence before making the small edit.
5. **Booru exact-probe mismatch:** the query sketch assumes a two-field
   `(target, concept)` RecordId while the tag-claim sketch includes more
   identity-bearing fields. Resolve the application profile or query plan;
   never derive a record ID from only part of its body.

## Important profile and presentation limits

- September B0's `expectedType=ANY` is an existence/reference control with
  consumer validation, not a promise of arbitrary shape/authority. August's
  closed exact/self reference codec needs target-specific relation variants
  or an explicitly shared subject. Neither profile's behavior can be silently
  assumed for the permanent Type system.
- Web presentation may show qualified partial entries. Native directory alias
  publication needs complete effective resolution, not merely raw postings.
- File found, bytes obtained, bytes verified, execution prepared and execution
  authorized are separate outcomes. Carrier fallback cannot repair missing
  semantic authority by choosing a different subject or basis.
- Complete retrieval or adapter reconstruction is not independent custody,
  runner isolation, cross-browser support, publication rights or permanent
  availability. None of those was newly demonstrated by these reports.

## Lane status and provenance

The seven reports above are completed task replies from the IDs in the run
checkpoint. They performed read-only review, not new runtime experiments.
SDK and Explorer are working on their owned isolated branches and will return
their own artifacts and test results.

The Web Client / OS PM confirmed that James superseded the nine-test mapping
with a newer direct extension-system research request. Do not mark that lane
delivered or interrupt the new assignment. The v2 PM has commissioned the
missing bounded mapping from the temporary contracts-handoff reviewer; this
does not transfer long-term product ownership.

No product-specific new owner question is needed for the bounded adapter
recommendations. This is not authorization to ship those products or combine
their scopes into MVP-C0.

## Follow-up integration — 2026-09-04

The pending statements above describe the first checkpoint at `5d75309`.
SDK and Explorer have now completed their owned handoffs; exact commits,
five-seam synthesis and probe limits are in the [converged handoff](./README.md#converged-handoff--2026-09-04).

All three application-document deltas were checked independently against the
actual sources before the following narrow draft changes:

- **App Store Release:** architecture's OWS-R4 already excluded labels from
  identity, while its PackageRelease paragraph included them. Align that
  paragraph with separate attributable version-label claims, as modeled by
  the August 22 fixture's PackageReleaseType/VersionLabelClaimType. No Type
  bytes, authority/succession policy or product profile is adopted.
- **App Store evidence:** date-qualify the Stage A report's unrun statement and
  point to later bounded application evidence. Do not rewrite historical
  STATUS or claim the full Stage B program or deployed product completed.
- **Booru query:** keep both illustrative candidates. The two-field assertion
  is not a partial encoding of the richer polarity/scope/confidence/evidence
  claim; an exact probe needs every canonical field. Otherwise enumerate and
  evaluate bounded complete candidates at one basis/policy. The application
  profile is still not selected.

These are source-coherence repairs, not newly executed App Store/Booru tests.
The SDK's separate pre-submit/Core-receipt counterexample is also repaired in
the integrated five-seam draft; its nine tests are representation-only.
