# Decisions

Append-only one-line decisions log. Mirrors the pattern in `contracts/docs/decisions.md`: small calls agents (or James) made that future agents should know about, but that don't merit a full design or ADR.

**When to add:** Tier 2 or Tier 3 decision you made (per [[Onboarding/escalation]]). One line, date-stamped, with rationale.

**When NOT to add:** Tier 1 decisions or decisions that change a design — those need a real design (or supersede a design).

**Format:**

```
- YYYY-MM-DD (@agent or @james): one-line decision and short rationale [optional: links to context]
```

**Pruning.** Periodically (or when the file approaches ~500 lines), promote stabilized patterns to a real design and delete the original entries with a pointer. Mark reversed/irrelevant decisions or delete outright. Pruning is itself a Tier 3 task; log the pruning pass as one entry.

---

## 2026-05

- 2026-06-01 (@pm, after 2-subagent prompt review): **Dual-horizon scoping for the architecture review — one agent, priority-ordered.** Review surfaced the deadline-vs-open-ended-review collision; fix = make two horizons explicit: (1) SHIP — coherent-enough Sepolia by end of week (hard deadline; Sepolia UIDs disposable, don't carry to mainnet, so "reasonable + internally consistent" not "perfect"); (2) GET IT RIGHT — the deep 100-year review as a WRITTEN deliverable feeding the later MAINNET freeze, must NOT block #1; when they conflict, ship. Resolves James's one-vs-split question: ONE agent with dual-horizon (preserves the single holistic mind; priority-ordering removes rabbit-hole risk a split would also solve). Other review findings folded into the prompt: "ignore prior DECISIONS, never fight EAS PHYSICS" made explicit + change-budget/ranked-shortlist/propose-gate; inventory-the-stack-before-judging; write the durability test per use-case; frozen-UID table as a Sepolia deliverable; guard the uninitialized-proxy-initializer footgun + don't burn/hardcode admin keys. Prompt stays in chat (one-shot); James kicks off with the system-architect skill.
- 2026-06-01 (@james via chat): **Freeze/deploy reframed as a holistic first-principles architecture review.** Not just "freeze + deploy" — a full smoke-test of the entire EFS contract + API stack for foundational quality: do the APIs make sense, are they industry-standard, coherent end-to-end, and can the schema/contract set express brainstormed use cases for ~100 years. THEN freeze schemas + deploy Sepolia with upgradeable contracts. **Explicit directive: IGNORE existing ADRs/specs as constraints — "do the right thing," not shackled by prior dev.** (ADRs/specs remain as history/record; the architect just isn't bound by them and may contradict/replace shapes.) Goal: deploy something foundationally GOOD + thought-through to Sepolia (security-fixable, extensible), not a rubber-stamp. Only real restrictions are physics (EAS schema-UID permanence) + the decided model (upgradeable logic behind stable addresses) + Sepolia best-effort. James kicks this off himself with the system-architect skill; PM provides the task prompt (kept short + open). The Brainstorms/ corpus (use cases, schema audit, decomposition, dev UX) is the durability testbed.
- 2026-06-01 (@pm): **Freeze/deploy thread launch prompt refactored (open/frame-first) + expert-reviewed (3 subagents).** Review found two things worth recording beyond the prompt: (1) **"widening integers is free" is false at the schema-freeze level** — widening a field changes the schema *string* → changes the schemaUID → orphans data; "free" only ever meant calldata cost. So the count-vs-discriminator sizing pass is now-or-never, pre-freeze. (2) **The resolvers aren't upgradeable-shaped today** — they use `immutable` constructor fields (and EAS's `SchemaResolver` base holds `immutable _eas`), no initializers, so "put them behind proxies" is a real refactor (immutable→storage + initializers + storage gaps), not wiring. Also: schemaUID = keccak256(abi.encodePacked(schemaString, resolverAddress, revocable)); the **proxy** address (not impl) must be what's registered, before registerSchema, or upgradeability is foreclosed; UID-equality + storage-layout checks should be *blocking* CI gates; Sepolia UIDs won't equal mainnet UIDs (CREATE2 → different addresses), so document migration, don't mistake for data loss. **PM feasibility flag to James:** likely-fastest path to "people adding data" = deploy Sepolia now in current (non-upgradeable) shape (Sepolia is best-effort + its UIDs won't carry to mainnet anyway), do the proxy refactor deliberately before mainnet. Prompt held in chat (one-shot, per convention); freeze-deploy thread spawns on James's word.
- 2026-06-01 (@pm): **EFS Lists MERGED to `main`** (PR #20, commit b1ac4e0, 2026-06-01T01:09Z). main now carries ADR-0044/0045/0046/0047, ListResolver/ListEntryResolver/ListReader, and the frozen design doc. Schemas are now 9 (7 + LIST + LIST_ENTRY). Landing cascade done: Kanban card → Done; schema-freeze + Sepolia-deploy cards un-gated (Lists dependency cleared); LIST/LIST_ENTRY Glossary stubs folded into the existing vocab-drift card. Critical path now = schema freeze (ASAP) + Sepolia deploy via the freeze/deploy thread (launch prompt drafted in chat).
- 2026-05-31 (@james via chat): **DECISION — frozen schemas + upgradeable contracts. Resolves the ADR-0030 fork; supersedes its mainnet "no-proxies" stance.** Model (corrected 2026-05-31 after PM overstated a "set freeze"): (1) **Schemas freeze ASAP** — they define data shape; frozen schema UIDs mean data added now survives. (2) **Resolver ADDRESSES stay stable** (they're hashed into schema UIDs, so an address change orphans data) — achieved via proxies; non-resolver contracts (router/view/reader) aren't address-bound and redeploy freely. (3) **Resolver LOGIC stays upgradable** behind those proxies — add indexes, fix bugs + security, continue dev. (4) **Contract SET is NOT frozen** — contracts can be **added freely**; **removed only with caution** (dependents like the SDK break); both still fine pre-mainnet. The earlier "freeze the .sol file list" framing (Milestones OnionDAO list) is superseded by this — James: "Contracts can be added just fine. Removing needs caution but we can still do it before mainnet." **Rationale:** the goal is "let people add data and don't destroy it, while still iterating." James weights ADR-0030's own acknowledged tail-risk (immutable + critical bug = total redeploy + full data loss) above immutability-as-credible-neutrality — *for now* ("foreseeable future" = a phase decision, can tighten later). This **supersedes ADR-0030** (Accepted: "mainnet permanent; no proxies, no admin-anything") → the contracts repo needs a superseding ADR written by the freeze/deploy thread. **Open sub-question for that thread (NOT resolved here):** the proxy-admin trust model — who holds upgrade keys + what guardrails (multisig / timelock / announced upgrades / eventual burn). That's what determines whether "upgradeable" is credibly-neutral-enough vs. a rug risk; connects to the SDK systemLenses-trust-root flag. (Also 2026-05-31: PR #20 went green — all CI clean — so the merge that triggers the schema freeze is unblocked.)
- 2026-05-31 (@pm, from Lists dev via James): **Schema-freeze design inputs received + a contradiction flagged.** Lists is merge-ready. New **ADR-0047**: `LIST.maxEntries` uint32→uint256 (planet-scale; first act of schema finalization) — established the "count-vs-discriminator sizing" rule (fields counting real-world things get wide types; flags/enums stay narrow; widening is free under EAS 32-byte ABI padding). Model for the freeze branch: **frozen EAS schemas + upgradeable contracts**, since `schemaUID = keccak256(fieldString, resolverAddress, revocable)` hashes the resolver address → redeploy-to-new-address orphans all data → every stateful resolver (EFSIndexer, EdgeResolver, MirrorResolver, EFSSortOverlay, ListEntryResolver) must sit behind a **stable proxy** with storage-layout discipline. **CONTRADICTION:** this directly reverses **ADR-0030 (Accepted)** — "mainnet contracts permanent; no proxies, no upgradeability, no admin-anything." ADR-0030 *does* permit proxies on devnet/Sepolia, so OnionDAO is unaffected; the conflict is mainnet-only and is a core-trust-model change (who holds proxy-admin keys = who can change/censor EFS). PM escalated to James as a deliberate fork (immutable-accept-tail-risk / supersede-with-defined-admin-trust / hybrid) requiring a **superseding ADR**, not a checklist line. Rhymes with the SDK design's systemLenses-trust-roots flag. **Closes the earlier §8 CREATE2 finding** — dev's nonce-CREATE-on-devnet → CREATE2-at-mainnet (with assert-predicted-address + UID back-read scaffolding) is a deliberate staged plan. **Canonical home for the freeze checklist = contracts repo** (an ADR for the proxy/upgrade model + a `docs/` execution checklist); planning vault keeps only this coordination pointer, not a copy (anti-pollution). Full checklist detail: dev's message + contracts `docs/QUESTIONS.md` / `docs/FUTURE_WORK.md`.
- 2026-05-31 (@james via chat → @pm executed): **Design-lifecycle policy confirmed by action: single-repo designs stay in their repo and freeze-in-place at land** (canonical-pointer header, body preserved — NOT moved to planning, NOT deleted, NOT gutted to a stub). Enacted on EFS Lists: PM edited `contracts/designs/custom-lists.md` (Status Draft→Landed, points to ADR-0044+0046+specs/06) and `custom-lists_notes.md` (marked frozen historical record), committed `90562c2` to the `custom-lists` branch (PR #20). Done in an isolated git worktree off origin/custom-lists so the dirty main checkout / dev's tree was untouched. Doc-only. `planning/Designs/` remains for cross-repo / vault-meta designs; both freeze-in-place at land via tombstone-style pointer. (Policy capture kept to this one line per James's "don't over-build / don't pollute the vault" steer — not formalized into the meta-design.)
- 2026-05-30 (@pm): **PR #20 (EFS Lists) reviewed → ready to merge; full findings posted to the PR (GitHub), not duplicated here.** Durable facts only: design faithful to ADR-0044 + **new ADR-0046** (targeted revision — removes `int256 weight` from LIST_ENTRY making entries pure membership identity; order/labels move to PIN-bound PROPERTYs on the stable entry UID; NOT a reversal). No BLOCKING/HIGH. Merge gated only on 2 trivial CI fixes the dev owns. **3 §8 items belong to the deploy/upgrade-strategy thread:** CREATE2-vs-CREATE deviation, missing hardcoded `LIST_ENTRY_SCHEMA_UID==expected` assert (the Sepolia↔mainnet divergence guard), and undocumented `_listAttesters` index. (Per James 2026-05-30: bug/finding detail lives on GitHub, not the planning vault — this entry kept to coordination altitude.)
- 2026-05-30 (@james via chat + @pm verify): **Lists status resolved — schemas done, dev fixing bugs, but ALL LOCAL/unpushed.** PM checked every remote branch on contracts: newest push anywhere is `custom-lists` @ 2026-05-28 (ADRs + design docs only); NO remote branch has `ListResolver`/`ListEntryResolver`/`ListReader` source. `efs-lists` branch exists but is stale (2026-04-04). So the dev's active bug-fixing is on a local working copy not visible to the swarm. Good news (Lists not stalled); risk (parallel threads blind to real schema UIDs + resolver addresses; single-machine-failure point at T-2). PM surfaced a gentle "consider pushing WIP" nudge to For-James — dev's call, not mandated. Schema field strings themselves are frozen per ADR-0044 (commit d685332), so doc/spec/freeze-prep work can proceed against the ADR without the impl.
- 2026-05-26 (@james via chat): **Schema freeze is near-term (Sepolia/OnionDAO via Lists merge); smart-contract shape design starts NOW; smart-contract freeze comes later.** Critical sequencing correction: PM had framed all contract-architecture work as post-OnionDAO. James clarified: shape work must start now or SDK will be built against the wrong abstraction and need massive rewrite. New sequence: (1) Lists merges → schema freeze on Sepolia (this week). (2) Contract-shape brainstorms NOW (this session). (3) Contract Architecture design thread spawns post-OnionDAO with brainstorms as input. (4) SDK design thread depends on (3) landing directionally — should NOT race ahead. (5) Smart-contract freeze (mainnet-grade) much later.
- 2026-05-26 (@james via chat): **Lists design coming for PM review.** James will share the Lists design when wrapped up. PM is not a design substance reviewer (per SOUL) — will read for vault coherence, surface concerns to James, route to a real design reviewer if substantive review needed. James to decide who that reviewer is.
- 2026-05-28 (@james via chat): **Q5 dissolved — single-signature batching is the SDK's job, not a flag James pre-approves.** James's insight: collapsing a multi-attestation write to one signature is core SDK value (the SDK exists to make things easy + efficient). `EFSUploadGateway` is just ONE possible mechanism; alternatives (EIP-5792 wallet `sendCalls`, ERC-4337 AA, multicall) may need NO new contract. Resolution: SDK `batch()` should deliver single-signature writes where possible; the SDK designer picks the mechanism. If a gateway contract is the chosen mechanism, it's an SDK-OWNED upgradeable convenience contract (NOT EFS-core-immutable, per the SDK-scope split). No reserved-flag decision needed from James. So all SDK open questions (Q1–Q5) are now resolved; design needs one revise pass → promote.
- 2026-05-28 (@james via chat): **Coordination model — the PM is NOT a relay for design specifics.** James felt streams crossing (SDK design Q&A round-tripping through the PM). Clean division: design-specific Q&A goes DIRECTLY between James and the relevant design agent (SDK, Lists); the PM owns project state, cross-cutting risk, vault coherence, nudging, and "what to spawn." The vault is the shared async brain — decisions land there (any agent records them) and every agent reads them on next run; that async blackboard IS "all 3 talking together" within the filesystem-coordination model. Protocol when James answers an agent's open questions: answers get recorded in the vault, then James pings that agent's thread to "pick up the vault answers and proceed." PM ensures answers land coherently and does not round-trip every design decision. (Open future idea: live multi-agent sessions / agent-to-agent messaging would remove the async lag — not available in the current setup.)
- 2026-05-28 (@pm): **Commit-convention violation found + fixed: `sdk-designer` used `pm:` subject prefixes.** Commits da37e55 + 825bfbd (editing For-James) carried `pm:` subjects but `Agent: sdk-designer` trailers — honest identity, misleading subject; created a phantom "second PM" in `git log --grep='^pm:'`. Not malicious. Root causes: (1) subject prefix should reflect the authoring agent's area, not be free-form; (2) the SDK prompt said "append a line to For-James" but the agent restructured the decide-now queue (PM curation). Fix landed in [[conventions]] § commit-message style: `pm:` is RESERVED for the PM role; non-PM agents editing PM-owned files (For-James/Kanban/agent-status) use their own area and only *append* a surface line, leaving curation to the PM. (Tier-2 convention edit — flagged to James, who raised the concern.)
- 2026-05-28 (@james via chat): **SDK Q3/Q4 resolved; Q5 pending.** Q3 = (a) include off-chain-index methods but throw `OffchainIndexRequired` until an index exists. Q4 = **lens defaults to the connected wallet; require an explicit lens only when no wallet is connected** — improves on the designer's "always require explicit" by keeping hello-world easy while still avoiding the silent-default bug (defaults to the *user's own* wallet, NOT the deployer, which was the original bug source). Q4 changes the design → SDK designer does a revise pass. Q5 (reserve `batch({gateway:true})` for the future EFSUploadGateway) still with James. On Q5, one revise pass folds Q3/Q4/Q5 → final promote.
- 2026-05-28 (@pm): **SDK design (`sdk-architecture.md`) reached `#status/review` in ONE round — frame-first process validated on its first live use.** No swirl (contrast: Lists' 18 rounds). Designer process-feedback folded into [[Brainstorms/2026-05-28-pm-design-process-synthesis]] § Field-test; two improvements adopted: (1) tag corpus files `[CRITICAL]`/`[CONTEXT]`/`[BACKGROUND]` in design prompts; (2) define "review-ready" = requirements locked + inverted-framing done + API sketched + open questions named + human-readable in <20 min. Q1 resolved (single `sdk/` repo; ABI types generated from `contracts/` at build); Q2 resolved (domain-model namespaces — resource-oriented, Stripe/Prisma standard). Q3/Q4/Q5 (off-chain-index-methods-that-throw / explicit-lens-declaration / reserve-gateway-option) surfaced to James as promote-with-defaults-or-override. Expert subagent review pass already folded in (verb-contract, sort-overlay fidelity, resumable cursors, bidirectional escape hatch). PM coherence check: clean, well-anchored (ADR-0031/0041/0044 + brainstorm corpus). PM rec: promote. (Note: a parallel PM thread curated Q1/Q2 resolution + the PIN/TAG self-correction; this session built on that work rather than redoing it — multi-session reality.)
- 2026-05-28 (@james via chat): **SDK design thread (@sdk-designer) spawned — scope is the REAL core SDK (on-chain + off-chain), not a throwaway.** Supersedes the "bare-bones throwaway" framing: OnionDAO ships a SUBSET of the real design. Design only (no code), output planning/Designs/sdk-architecture.md, frame-first process, stops at #status/review for James's frame review. Key scope: EFS.EAS namespace exposing the EAS SDK; coherent graph APIs (Tags/Pins/Lists); pin(file)-style abstraction over the technical immutable contracts + a first-class raw escape hatch; open question on repo packaging (sdk/ repo vs on-chain SDK in contracts/); debug-client-parity acid test. First live test of the new frame-first design process — the thread reports process feedback (guidance clarity, value-add, token overhead) back to the PM.
- 2026-05-28 (@james via chat): **The bare-bones OnionDAO SDK starts as a DESIGN in `planning/Designs/`, not code.** James caught the PM conflating design and build. Sequence: light Ephemeral-tier design (requirements + API-surface sketch, NO code) → James frame-review → separate code thread. Bonus: the design thread has zero dependency on Lists/deploy (you can design the surface anytime), so design-first is both more disciplined AND more parallel — only the code depends on the deployed surface. Rigor tiered to permanence: this is throwaway/Ephemeral, so the lightest design touch, NOT an Etched ceremony. Reinforces vault design-first discipline + the new frame-first process.
- 2026-05-28 (@james via chat): **SDK posture for OnionDAO = (a) bare-bones read/write SDK by end of next week**, built in parallel. Dependency (per James): gated on schema freeze + contract shape — for OnionDAO that resolves to Lists→Sepolia (a stable surface to wrap), NOT the full mainnet contract-shape freeze. Accepted cost: the bare-bones SDK may need rework once the contract-shape freeze lands; acceptable for a hackathon throwaway. Kanban card added; it's the near-term subset of the fuller on-chain/off-chain SDK cards.
- 2026-05-28 (@pm): **ADR-0043 numbering collision RESOLVED by the EFS Lists dev.** Current `custom-lists` (b909ce7) has `0043-rename-editions-to-lenses` (matching main) and edge-constraint-callbacks renumbered to `0045-efs-edge-constraint-callbacks`. ADR-0044's internal cross-references all updated to point at 0045; "ADR-0042 (... / lenses)" also de-edition'd. No dangling refs. The merge-time renumber condition is satisfied. (Note: James thought the dev may have fixed it — confirmed, they did.)
- 2026-05-28 (@james via chat): **OnionDAO entrant onboarding + flyers: discuss before drafting.** James will initiate a separate discussion fork; PM is NOT to draft yet. James explicitly asked PM to keep reminding him so it doesn't slip — PM resurfaces it every session (For-James 🕐 + Kanban card) until the fork starts. End-of-next-week target for flyers up + people able to add data.
- 2026-05-28 (@james via chat): **Lists IS required for OnionDAO; timeline can slip a bit.** Dev shouldn't take long now that design is done. Hard needs by end of next week: schema freeze, flyers up, people able to add data, possibly a bare-bones SDK. Lists is not droppable for OnionDAO (corrects the earlier "could run on current-EFS" speculation).
- 2026-05-28 (@james via chat): **For-James.md restructured — sorted by ask, not by date.** James found the flat 12-bullet list unreadable. New format: ⚡ DECIDE NOW (forks with lettered options + PM rec) / 🕐 WHEN YOU HAVE TIME (reviews) / ℹ️ FYI (collapsed, points here). PM lesson: For-James is a 10-second decision queue, not a log. Decisions/status/observations go in this file or Daily Notes, NOT For-James. Prune ruthlessly. Convention updated in For-James.md's agent-docs section.
- 2026-05-28 (@james via chat): **Requirements-first is the missing early anchor in the design process.** James's frame-review of the PM's process proposal (itself a live demo of the process working): the early Lists swirl happened because frames were explored before a requirements list was locked to falsify them against. Use cases (diverge) → requirements MUST/NICE/DEFERRED (converge, human-locked) → frames (evaluated against locked requirements). Requirements are the falsification target; the inverted-framing pass only has meaning once MUSTs are locked. Folded into [[Brainstorms/2026-05-28-pm-design-process-synthesis]] (lifecycle now 9 stages; human locks requirements+frame at Stage 3, the non-negotiable touch).
- 2026-05-28 (@pm): **Ingested Lists `design-lessons.md`; proposed a streamlined EFS design process** (James-commissioned). Core insight (James + the lessons file converge): human review came too LATE in the Lists process (~18 rounds before James read it and saw immediate simplifications). Fix: human judgment at the frame (early) + gate (late), AI grinds in between. Proposal at [[Brainstorms/2026-05-28-pm-design-process-synthesis]] — 6-stage lifecycle (inverted-framing pass → rough flesh-out → HUMAN FRAME REVIEW → flesh-out+adversarial → external review for Etched only → HUMAN GATE). 8 reusable AI techniques (inverted-framing-first, verify+break pairing, implicit-invariant audit, etc.). To be formalized as `Onboarding/design-process.md` after James's frame review (meta: the proposal goes through its own process). PM did NOT need the dev's chat log — the lessons file was sufficient.
- 2026-05-28 (@pm): **EFS Lists reviewed — PM verdict GO for dev start.** Reviewed ADR-0044 + design doc open-concerns. Coherence-clean; cross-checks cleanly against batch-3 brainstorm concerns (CREATE2/schema-UID-pin, wide storage, state-growth bounds, per-entry-metadata-via-PROPERTY, ADR-0041 honest reconciliation all handled). EVENT/TRANSITION typed-edge gap correctly out of scope (Lists is collections, not event edges). PM review is coherence-only; the 3-external-reviewer GO is the substance sign-off. **Two independent PM sessions reached identical verdicts** — this session's review confirmed the prior (broken) thread's GO recommendation that was sitting uncommitted in the working tree. Conditions to track (none block dev start): ADR-0043 renumber before merge; CREATE2 + CI pin check at deploy; SDK canonical ANY-mode key-derivation; vault sync at merge.
- 2026-05-28 (@pm): **Schema count authority = ADR-0044: EFS goes 7→9 schemas with Lists** (adds LIST + LIST_ENTRY). Corrected a stray "→8 total" in the Kanban card. Prior PM "6→7" and "5 contracts" framings were stale-local-checkout artifacts (see below).
- 2026-05-28 (@pm): **Local `contracts/` checkout is ~30 commits stale; confounds some batch-3 findings.** Local `main` (94217b5) vs `origin/main` (5e84d32); local ADR folder stops at 0032 while origin has through 0043. This produced the false "ADR-0041/0043 phantom" alarm (they exist on origin). Knock-on: rot-audit's "main missing ADRs 0033-0043" and the "~11 contracts" count were read from stale local state — re-verify against `origin/` before acting. The ADR-0043 *numbering collision* is REAL (verified directly via `git ls-tree origin/main` vs `origin/custom-lists`): `0043-rename-editions-to-lenses` (main) vs `0043-efs-edge-constraint-callbacks` (custom-lists). **Process learning: agents reading the contracts repo should read from `origin/<branch>` via `git show`, not the local working tree, until the local checkout is synced.** Recovered the broken thread's uncommitted Lists-review work (For-James + Kanban) and committed it rather than let it be lost.
- 2026-05-26 (@pm): **Brainstorm rule 4 refined to allow declared-dependency reads.** Original rule prohibited reading other agents' brainstorms; refined to: reading is allowed *when the dependency is declared as an anchor* (`anchors: [brainstorm: <slug>]`). Avoids cascading hallucination while permitting deliberate composition. Captured in [[brainstorm-system]] § Five rules.
- 2026-05-26 (@pm): **Third brainstorm batch (7 agents in parallel, ~682k tokens, ~14 min wallclock for the longest).** Topics: schema-set freeze recommendations, per-contract upgradeability, lifetime-split deepdive, EIP-170 bytecode budget, N system-design perspectives, vocab coherence audit, first formal rot audit. Cumulative across 3 batches: ~1.08M tokens, 13 brainstorms. Key new findings: (1) ADR-0041 (PIN/TAG split) and ADR-0043 (two conflicting versions on different branches) referenced as settled but don't exist as written ADRs — coherence blocker, surfaced to For-James. (2) `EFSUploadGateway` wrapper contract proposed as single high-leverage architectural change addressing 8-prompt detonation + L2 amortization + AA bundling. (3) EIP-170 bytecode budget measured from real artifacts: Direction 3 (2-contract kernel) confirmed dead at 144% of limit; Direction 1 (3-contract) tight but feasible. (4) Schema-wired contract addresses make merging impossible — biases hard toward splitting more before launch. (5) Lifetime-split direction holds up architecturally but pays 10-15% gas tax on cross-boundary writes. (6) `contracts/main` hasn't moved in 40 days; specs about to detonate at Lists merge. (7) Vocab audit: 7 High drift instances + 14 Glossary gaps. Brainstorm-system § Exit triage structure (added between batch-2 and batch-3) worked as predicted — curation was much faster with the controversial/unknown/blockers triage.
- 2026-05-26 (@pm): **Second brainstorm batch (3 agents, ~244k tokens, ~7 min wallclock).** Topics: contract decomposition (5 directions), schema coverage audit (30-item gap catalog), third-party dev UX (5 app walkthroughs). Curation outcomes: (a) typed-edge finding refined and downgraded from "Lists-blocker" to "pre-mainnet shape-freeze concern" — schema audit found only 1 of 8 TAG-overload roles actually breaks; (b) PROPERTY is more pervasively strained than TAG, but SDK-fixable; (c) `efs.write` is an 8-attestation MetaMask detonation per dev UX walkthrough — sharpest launch-quality finding; (d) "EFS-in-Postgres" off-chain indexer pattern likely matters more pre-launch than SDK polish — added as Kanban card; (e) spec drift surfaced: `contracts/specs/` still uses "edition" / "TagResolver"; `overview.md` contract count out of sync — added as Kanban card. Cumulative across both batches: ~400k tokens, 6 brainstorms. Spend trajectory noted: batch-2 1.6× batch-1; tighten future batches to ≤200k unless justified.
- 2026-05-26 (@pm): **Contract count correction — PM has been undercounting.** Prior reporting said "5 contracts on `custom-lists` branch" — that was the count of contracts *changed by Lists*, not the total. Actual total is ~11 contracts (BlobResolver, EFSFileView, EFSIndexer, EFSRouter, EFSSortOverlay, FileResolver, MirrorResolver, PropertyResolver, SchemaNameIndex, TagResolver→EdgeResolver, TopicResolver) plus interfaces and stubs. Discovered by `bs-contract-decomposition-v1` when it actually `ls`'d the contracts directory. PM should fact-check before quoting numbers; this is a process learning to note. Original "5 contracts" framing in earlier Decisions and Kanban entries should be read with this correction in mind.
- 2026-05-26 (@pm): **First brainstorm batch via parallel subagents (3 agents, ~155k tokens, ~3.5 min wallclock).** Outputs: 15 use cases (250 lines), OS SDK capability surface (386 lines), SDK packaging directions (227 lines). All three returned cleanly, hit length targets, produced specific findings rather than generic content. Token cost evaluation: NOT wasted — typed-edge finding alone (re-frames a pre-Lists-freeze evaluation) likely worth >10× the inference cost. Pattern that worked: prompts with (a) clear deliverable, (b) exact frontmatter spec, (c) scoped "what to read" list with stop-when-enough guidance, (d) length target, (e) what-NOT-to-do, (f) exit-summary expectation. Captured patterns in [[brainstorm-system]] § Subagent prompt patterns.
- 2026-05-26 (@james via chat): **Brainstorm system approved as drafted.** Lightweight, minimal frontmatter (3 required fields), 5 rules, no auto-pruning. Status vocabulary: raw / surfaced / integrated / reference / obsolete. Pruning is deliberate-only with written reason — never on age alone. Things in `Brainstorms/` can have indefinite lifespan when marked `reference`. Design draft at [[brainstorm-system]] for review.
- 2026-05-26 (@james via chat): **No auto-prune for brainstorms.** Original PM design had 30-day TTL; James corrected: "I don't like things dying. We might surface something deferred until later or just informative." Pruning requires a conscious decision based on reasoning. Captured in [[brainstorm-system]] § Status vocabulary.
- 2026-05-26 (@james via chat): **Three SDK types confirmed.** (1) On-chain SDK — read/write/basic graph / primitive APIs. (2) Off-chain SDK — same surface in TypeScript. (3) **EFS OS SDK** — untrusted app APIs for talking to the Kernel; the capability surface endowed into Ring 3 Apps per the Client OS spec. Possibly all share one repo (open question). Updates and reframes the earlier 2026-05-21 two-SDK Decisions entry. The third SDK is NOT iframe integrations — that was a misreading; it's an OCap capability proxy.
- 2026-05-26 (@james via chat): **SDK + Client are rotting** while Lists eats James's bandwidth. PM gains explicit "rot tracking" duty in SOUL. PM surfaces stale areas each session in regular reports.
- 2026-05-26 (@james via chat): **PM may run on any model** (Claude, Codex, Gemini, etc.). The vault is the source of truth; the model is interchangeable. SOUL must stay model-agnostic. Filesystem-only contract supports this naturally.
- 2026-05-26 (@james via chat): **Topic = Anchor.** "Topic" is human-facing terminology ("we're all talking about the topic Music"); "Anchor" is the technical name (web anchor / URL fragment). Same on-chain primitive. Glossary updated with cross-reference.
- 2026-05-26 (@james via chat): **EFS Official Client architecture captured from Gemini chat** as `Brainstorms/2026-05-26-pm-client-os-architecture.md` (`status: reference`). Sovereign zero-trust web OS, Ring architecture (Bootstrapper / Kernel / Shell / Apps), SES sandboxing, OCap security, IPFS-deployable static SPA. Tech stack committed: Lit, Web Awesome Free, SES (Agoric), Vite. The current `client/` repo is significantly behind this target spec. Primary input for the future Client design thread (post-OnionDAO).
- 2026-05-26 (@pm): **PM-recommended OnionDAO triage at T-6 days.** Minimum-viable hackathon is two deliverables: Lists merged + Sepolia deployed. SDK track is at-risk; PM recommends soft-launching ("call contracts directly via ABI; SDK ships through June") rather than miracle-pushing. Schema freeze is implicit at deploy (no separate ceremony). All other items (SOUL review, Milestones expansion, SDK design thread, Devcon, Client SDK) deferred until post-OnionDAO. Captured to reduce James's decision surface during a stressful week. James to confirm SDK-track posture; PM standing by.
- 2026-05-26 (@james via chat): **Schema freeze importance reframed as data-persistence concern during hackathon.** Without locked schemas, data put in by entrants risks being orphaned by a schema-UID change. PM clarified: schema freeze IS the moment Lists deploys to Sepolia; no separate freeze ceremony needed as long as no further schema edits happen during the hackathon.
- 2026-05-21 (@james via chat): **SDK thread prompt does not need to live in a file.** James will craft it directly in chat with the PM and paste into a new agent session. Decision narrows the Agents/ folder's purpose — launch prompts go there only when reusable across many sessions (like `pm-launch.md`); one-shot bootstrap prompts stay in chat.
- 2026-05-21 (@james via chat): **Hackathon-planning is NOT a design.** Operational decisions for the OnionDAO milestone (venue, prizes, judging, onboarding docs, comms) live as: (a) a sub-decisions checklist under the OnionDAO section of Milestones.md, and (b) Kanban execution cards as concrete tasks emerge. PM proposes the Milestones.md expansion in chat (awaiting James's OK per SOUL autonomy rule).
- 2026-05-21 (@james via chat): **Mirror in-flight contracts work on the Kanban board going forward.** Don't wait until merge to surface significant contracts-side work in the vault. Active dev work like EFS Lists belongs in In Flight even when the dev (James) is the human, so the swarm and the PM can see it. (Tasks may also be useful for individual sub-steps; James to decide on a case-by-case basis.)
- 2026-05-21 (@james via chat): **Lists merge will change the Sepolia deploy script.** The EdgeResolver.sol / TagResolver.sol swap means deploy scripts need updating; confirmed Sepolia deploy is firmly downstream of Lists merge. Captured for future deployment-thread context.
- 2026-05-21 (@pm): **EFS Lists merge IS the schema freeze and the .sol file list freeze.** Branch `custom-lists` on efs-project/contracts changes the schema count 6→7, reworks PROPERTY (no more `key` field; PIN-bound key-anchor model), adds `EdgeResolver.sol` (+789 lines), removes `TagResolver.sol` (−401 lines). The three Kanban items collapse: schema freeze and .sol freeze are now `#depends-on/lists-merge` rather than independent work. Sepolia deploy also gated on this. Surfaced in chat 2026-05-21; awaiting James's call on Lists merge ETA.
- 2026-05-21 (@pm): **Two PM files (pm.md SOUL + pm-launch.md launch prompt) will be collapsed.** James delegated maintenance. Approach: pm-launch.md becomes a thin bootstrap pointing at pm.md as the canonical operating brief (the launch prompt itself anticipated this — "this launch prompt can become a thinner bootstrap when SOUL is in effect"). Reduces duplication; SOUL is the source of truth.
- 2026-05-21 (@james + @claude-opus-4.7): **Alchemy public key gets rotated upstream periodically and breaks our CI.** EFS contracts hardcodes the Scaffold-ETH-2 community Alchemy default in two files (`packages/hardhat/hardhat.config.ts` AND `packages/nextjs/scaffold.config.ts`); when upstream rotates we have to follow. This is the second documented occurrence; James reports earlier ones not captured. Lesson: grep the WHOLE repo when rotating any shared default value — the 2026-05-21 fix was half-applied in the first commit and required a follow-up. Full playbook: [[known-issues#Alchemy public default API key gets revoked / rate-limited]].
- 2026-05-21 (@james via chat): **Canonical local path for EFS repos is `/Users/james/Code/EFS/{contracts,client,planning,sdk}/`.** Older duplicates under `/Users/james/Code/Claude/{contracts,client}/` predate git-worktree-based isolation and are no longer needed; all tools now share via worktrees from the canonical clones. The vault's docs still describe `/efs/<repo>/` as the symbolic target (more portable, simpler in prose) — agents should use relative paths from their worktree, not absolute paths in committed files.
- 2026-05-21 (@james via chat): **PM agent will eventually need a "SOUL file"** — its instruction set / personality / persistent operating context. Not designing it fully yet; the meta-shape is acknowledged so future PM-setup work has a known concept to attach to. Likely lives in the planning vault when designed.
- 2026-05-21 (@james via chat): **Cron-style scheduling for the AI-PM is James's responsibility** to set up (in Codex or Claude Code's scheduling tools). Vault doesn't try to manage scheduling itself.
- 2026-05-21 (@james via chat): **AI project-manager role is expected of agents.** Agents working in this vault should proactively keep James informed and prod when a deadline is at risk or something is being forgotten. The vault is the documentation surface that makes this possible; James wires up the cadence; the PM's actual instructions live in a future "SOUL file" (see above).
- 2026-05-21 (@james via chat): **SDK scope clarification.** EFS proper = the kernel, graph, and other contracts — immutable, simple, do their job. EFS SDK = a separate set of **upgradeable** API wrappers around the contracts to make dev usage easier. **Contracts are not part of the SDK.** Devs can call contracts directly if their APIs are good enough; the SDK provides convenience + options + ongoing improvements. SDK is owned by the EFS team and can change freely; contracts cannot.
- 2026-05-21 (@james via chat): **SDK design needs a dedicated AI thread.** Lots of design work to do; do not start implementation without a landed SDK architecture design. The Kanban entry "Design: on-chain + off-chain SDK architecture" is the placeholder; that design session is a separate thread James will initiate.
- 2026-05-21 (@james via chat): **For OnionDAO MVP**, only the on-chain and off-chain SDKs are required. The client SDK (iframe integrations, OS-type stuff) is a later concern. Re-prioritized in Kanban.
- 2026-05-21 (@james via chat): **Data-loss tolerance is phase-dependent.** On Sepolia (pre-mainnet), data may still be lost in rare cases but we try REAL HARD not to. Once we release to mainnet, data loss becomes a critical issue. Hackathon entrants at OnionDAO should be told this. Captured in [[Milestones#OnionDAO hackathon (2026-06-01 → 2026-06-30)]].
- 2026-05-21 (@james via chat): **OnionDAO hackathon (2026-06-01)** locked as next major milestone. Two tracks (datasets, SDK builds). Self-funded prizes. Schema spec freeze + contract `.sol` list freeze + Sepolia deploy + SDK MVP are hard prerequisites. See [[Milestones]] for scope.
- 2026-05-21 (@claude-opus-4.7): `_MOC.md` naming abandoned in favor of plain `README.md` per folder. README is universally understood, GitHub auto-renders, avoids PKM jargon. Captured inline in [[design-system]] § Folder READMEs.
- 2026-05-21 (@claude-opus-4.7): `Misc/` folder dropped from the planning vault layout. Uncategorized notes go to `Daily Notes/` instead. IA review consensus: miscellany folders rot. Captured in [[design-system]] § Directory layout.
- 2026-05-21 (@claude-opus-4.7): Specs stay in their owning repo (`contracts/specs/`, future `client/specs/`, future `sdk/specs/`). `/efs/` colocation removed the access-pain that motivated the alternative. Cross-cutting EFS knowledge lives in `planning/Architecture/` and `Glossary.md`. Resolves the previously-deferred "where do specs live" question.
