# Agent status log

Active agents append once per work-session. One line per active card. Newest at top.

Format: `- @<agent>: <task-or-design-slug> — <state> / <next step>`

---

## 2026-06-10

- @hackathon: onboarding+flyer thread started (James's fork, finally). Read the full state (Milestones, Decisions, holistic review UX findings, SDK scaffold, dead client, explorer pages, whitepapers dataset). Honest read: participant-add-data path does NOT exist today — Sepolia gated on James (freeze-table sign + EOA/Safe), SDK = stubs, explorer = debug-grade (UX-1/2/13 gaps). Drafted the full participant one-pager + flyer copy + tiered dataset/app idea lists at `/Users/james/Code/EFS/hackathon/onepager-draft.md` (staging, like datasets/). Surfaced 2 forks to For-James ⚡: entry path (rec: concierge-first via Discord + seeding script) + prize structure (rec: one pool both tracks). Announce gate proposed: Sepolia live + whitepapers seeded + Discord template pinned. Next: James's picks → final flyer visual + comms plan.

## 2026-06-01

- @pm: Refactored the freeze/deploy launch prompt to be open/frame-first (per James: inform + give ideas, don't shoehorn) and ran 3 expert-review subagents (autonomy / safety / domain). Domain pass caught two real things: "widening integers is free" is false at freeze (changes schema string → UID → orphans data; sizing is now-or-never), and the resolvers aren't upgradeable-shaped (immutables + no initializers → proxy refactor is real scope). Folded fixes into the prompt (accurate "physics" section, upgradeable-shape context, blocking UID/storage guards, register-the-proxy-not-impl). Feasibility flag to James: fastest path to data-add = deploy Sepolia now in current shape, proxy refactor before mainnet. Prompt ready in chat; thread spawns on James's word.
- @pm: **EFS Lists MERGED** (PR #20 → main, commit b1ac4e0). Verified on origin/main (ADRs 0044-0047, Lists resolvers, frozen design doc all landed). Ran landing cascade: Kanban Lists card → Done; un-gated schema-freeze + Sepolia-deploy cards; folded LIST/LIST_ENTRY Glossary stubs into the vocab-drift card; logged in Decisions. Drafted the freeze/deploy thread launch prompt in chat (the now-critical path: freeze schemas ASAP, proxies for stateful resolvers, superseding ADR for ADR-0030, CREATE2-at-mainnet, sizing audit). For-James item 0 → Lists merged + spawn-the-thread; Milestones .sol-freeze-wording Tier-1 flag still pending James.

## 2026-05-31

- @pm: CORRECTION — James clarified the freeze model: NOT a frozen contract set. Correct model = frozen schemas + **stable resolver addresses (proxies) + upgradable logic**; contract set stays flexible (add freely, remove cautiously) pre-mainnet. PM had overstated "freeze the .sol set / no add-remove" — fixed in Decisions/Kanban/For-James. Flagged that the Milestones OnionDAO "(.sol file list freeze)" wording is now stale → asked James (Tier-1) before editing milestone scope. Sepolia deploy soon with "try real hard to keep data, but it's testnet" framing (consistent with the earlier best-effort decision).
- @pm: James decided the ADR-0030 fork: **frozen schemas + upgradeable contracts** (data survives; dev/indexes/bugfixes continue). Supersedes ADR-0030 mainnet no-proxies → contracts repo needs a superseding ADR (freeze/deploy thread writes it). Open sub-q deferred to that thread: proxy-admin trust model. Resolved For-James 0b. Also: PR #20 went fully GREEN (ci + contract-tests + deploy-pin-check all pass) — CLEAN/OPEN, one merge from done; that merge triggers the schema freeze. Surfaced "merge when ready" to James. Updated Kanban freeze card with the decided model + the superseding-ADR task.
- @pm: Lists dev forwarded schema-freeze design inputs (via James). Verified ADR-0047 (maxEntries→uint256) on branch; read ADR-0030. **Caught a contradiction:** the dev's "frozen schemas + upgradeable proxies" model directly reverses ADR-0030 (Accepted: "no proxies, no upgradeability, no admin-anything" on mainnet). Sepolia/OnionDAO unaffected (ADR-0030 permits devnet proxies); mainnet-only core-trust-model fork → escalated to For-James 0b as a deliberate decision needing a superseding ADR (immutable / upgradeable-with-defined-admin / hybrid), connected to the SDK trust-root flag. Closed my earlier §8 CREATE2 finding (dev's staged nonce→CREATE2 plan). Answered the dev's routing question: freeze checklist's canonical home = contracts repo (ADR + docs/ checklist), vault keeps only a pointer (anti-pollution). Kanban freeze card updated with inputs + the ADR-0030 gate.
- @pm: Per James's go-ahead, edited the contracts repo (first PM code-repo write, explicitly authorized): froze `designs/custom-lists.md` (Draft→Landed, canonical pointer to ADR-0044/0046+specs/06) + `custom-lists_notes.md` (frozen historical marker). Committed `90562c2` → `custom-lists` (PR #20), via an isolated worktree off origin/custom-lists so the dirty main checkout (2 unrelated uncommitted files, not mine) + dev's tree stayed untouched. Also cleaned up a stale `/tmp` PR-review worktree. Confirms the design-lifecycle policy (single-repo designs freeze-in-place at land). Noted: dev is actively pushing (custom-lists b64c437→17bc5f2→ now 90562c2).

## 2026-05-30

- @pm: PR #20 (EFS Lists) review via 3 parallel subagents (design-fidelity / code-correctness / CI-diagnosis; ~136k subagent tokens, context-protected per James). Dev pushed the impl (was local this morning). Verdict: design FAITHFUL to ADR-0044 + new ADR-0046 (entries pure-identity, weight removed); code correct, no BLOCKING/HIGH (skeptic reviewer couldn't break enforcement); CI reds cosmetic (prettier auto-fixable + known Alchemy-key noise; contract-tests + freeze-invariant + schema-UID checks all PASS). Surfaced to For-James as "READY, 2 tiny fixes + 3 conscious §8 calls." The 3 calls (CREATE2-vs-CREATE, missing UID-equality assert, undocumented _listAttesters) connect directly to James's stated next step (upgrade strategy + real Sepolia deploy) — flagged that linkage. Kanban Lists card → PR-20-open-reviewed-ready; Decisions logged full findings.
- @pm: State-check session. Honest correction logged: I'd been computing T-N from 2026-05-28; actual today is 2026-05-30 (T-2 to OnionDAO). Audits caught both In Flight cards stale (Lists + PM SOUL, expired 2026-05-29). Cross-repo: custom-lists has ZERO pushed commits in 3 days (last 2026-05-28); contracts/main untouched 3 days; client untouched 9 days. SDK designer has been extremely active in parallel: Q2–Q6 all resolved, major reframe (on-chain SDK = Solidity library; off-chain = TS only; no indexer/Graph baggage), on-chain identity decided + adversarially reviewed, multi-team shared-namespace conventions added, 3 contracts-side ADR candidates surfaced for future work. SDK design at #status/review with TWO picks pending (Q1 reopened + promote/revise; PM rec 1a + promote). Added a Lists-status query to ⚡ DECIDE NOW — biggest risk at T-2 is the 3-day silence.

## 2026-05-28 (sdk-designer)

- @sdk-designer: sdk-architecture — design complete, at #status/review. Read corpus (10+ files), distilled MUST/NICE/DEFERRED requirements, ran inverted-framing pass (what does SDK add over raw EAS?), sketched full API surface: `efs.fs` / `efs.graph` / `efs.props` / `efs.lists` / `efs.lenses` / `efs.batch()` / `efs.EAS` / `efs.raw` / constants. 5 open questions surfaced (repo layout, namespace names, off-chain index in v1, explicit-lens requirement, EFSUploadGateway reservation). For-James updated with promote/revise fork. Process feedback section included per PM request.

## 2026-05-28

- @pm: Q5 dissolved (James's insight: single-sig batching is the SDK's job, not a flag — EFSUploadGateway is just one mechanism; SDK designer picks it; gateway-if-needed is SDK-owned not EFS-core). All SDK Q1–Q5 now resolved → SDK agent does one revise pass → promote. Bigger outcome: James flagged "crossing streams" (PM relaying design Q&A). Adopted coordination model — design specifics go James↔design-agent directly; PM = state/risk/coherence/nudging, NOT a relay; vault is the shared async brain. Captured in [[Decisions]] + PM SOUL. For-James DECIDE NOW cleared; 🕐 now says "hand the SDK answers to the SDK agent directly."
- @pm: Investigated the "phantom second PM" James flagged: commits da37e55 + 825bfbd were @sdk-designer using `pm:` SUBJECT prefixes while editing For-James — honest `Agent: sdk-designer` trailers, misleading subjects. Not malicious; convention gap. Fixed in [[conventions]]: `pm:` reserved for PM role; non-PM agents use own area + only append a For-James surface line (no queue curation). James answered SDK Q3 (include-but-throw) + Q4 (lens defaults to connected wallet, require explicit only if no wallet — better than designer's rec, avoids deployer-default bug). Q5 (reserve gateway batch option) explained + still with James. SDK design now needs ONE revise pass (fold Q3/Q4/Q5) then final promote; updated For-James + Kanban accordingly.
- @pm: SDK design review-curation. `sdk-architecture.md` hit #status/review in ONE round (frame-first process validated — vs Lists' 18 rounds). Read the designer's process-feedback (James-flagged) and folded 2 improvements into the design-process proposal: corpus-file priority tags + a one-line "review-ready" definition. Augmented the For-James SDK entry with Q3/Q4/Q5 (a parallel PM thread had surfaced only promote/revise; these 3 product decisions were baked in unseen). Coherence check: clean, well-anchored (ADR-0031/0041/0044 + brainstorm corpus); expert subagent pass already validated substance. Built on the parallel PM thread's Q1/Q2 + PIN/TAG-fix curation rather than redoing it. PM rec to James: promote with the 3 defaults (Q3a / Q4 strict / Q5 reserve).
- @pm: SDK design thread (@sdk-designer) spawned by @james for the real core SDK (on-chain + off-chain; OS SDK deferred) → planning/Designs/sdk-architecture.md. DESIGN ONLY (no code), frame-first process, stops at #status/review. This supersedes the earlier "bare-bones throwaway SDK" framing — OnionDAO now ships a SUBSET of the real design, not discarded work. Prompt included: EFS.EAS namespace, graph APIs (Tags/Pins/Lists), pin(file)-style abstraction + raw escape hatch, 8-prompt-write-UX problem, read/query surface, lenses, repo-packaging open question, debug-client-parity acid test, reference well-designed SDKs, + PM-requested process feedback section (first live test of the frame-first process). Kanban reconciled: SDK design → In Flight @sdk-designer; bare-bones cards folded into "Implement OnionDAO subset of [[sdk-architecture]]". PM watching for design to hit #status/review.
- @pm: Decision round + For-James cleanup. James found For-James unreadable (12 mixed bullets) → restructured into ⚡DECIDE NOW / 🕐WHEN YOU HAVE TIME / ℹ️FYI; convention + SOUL updated to keep it a decision queue. James's 3 picks: (1a) bare-bones read/write SDK by end of next week, parallel, gated on Lists→Sepolia; (2) ADR-0043 — verified the dev already RESOLVED the collision (custom-lists now 0043=rename-editions-to-lenses, edge-callbacks→0045, ADR-0044 cross-refs clean); (3) onboarding/flyers — discuss first, James initiates a fork later, PM to keep reminding. DECIDE NOW is now empty (goal state). Kanban: added bare-bones-SDK card + onboarding-discussion card. Lists confirmed required for OnionDAO; timeline can slip a bit.
- @pm: Design-process synthesis (James-commissioned). Ingested the Lists `design-lessons.md` retrospective from custom-lists branch. Wrote [[Brainstorms/2026-05-28-pm-design-process-synthesis]] proposing a frame-first lifecycle: human judgment at convergence points (early) + gate (late), AI between — fixes the "18 rounds before human read it" waste. James then did a live frame-review of the proposal and added the key missing piece: **requirements-first** (use cases diverge → requirements converge & human-lock → frames evaluated against locked requirements; requirements are the falsification target that stops frame swirl). Folded in — lifecycle now 9 stages, human locks requirements+frame at Stage 3 (non-negotiable). 8 reusable techniques, internal-vs-external review guidance, cost discipline. Did not need the dev's chat log. Surfaced to For-James; Kanban card to formalize as Onboarding/design-process.md (blocked on James). Cumulative brainstorms now 15.
- @pm: Lists design review (re-run after a prior PM thread broke mid-task). Read ADR-0044 + design-doc open concerns. Verdict: **GO for dev start** — coherence-clean, cross-checks cleanly against batch-3 brainstorm concerns (CREATE2, wide storage, state-growth, per-entry metadata, ADR-0041 reconciliation all handled). Two independent PM sessions reached identical verdicts. Recovered the broken thread's uncommitted For-James + Kanban edits (would've been lost) and committed them. Fixes/additions: corrected Kanban schema count to 7→9 per ADR-0044 (was a stray "→8"); removed obsolete "Lists design coming" For-James line; discovered local contracts checkout is ~30 commits stale (root cause of the earlier "phantom ADR" false alarm — ADRs exist on origin); flagged that some batch-3 rot/count findings are stale-confounded and need re-verification against origin. ADR-0043 numbering collision confirmed real via direct origin ref comparison. Conditions tracked, none block dev start.

## 2026-05-26

- @pm: session-6 (continuation). Dispatched batch-3 — 7 parallel subagents on James's "more thinking isn't bad" green light: bs-schema-freeze-recommendation-v1 (426 lines, per-schema freeze calls), bs-contract-upgradeability-v1 (251 lines, per-contract upgrade stories), bs-lifetime-split-deepdive-v1 (517 lines, stress-test of Direction 4), bs-bytecode-budget-v1 (175 lines, REAL artifact measurements), bs-system-design-perspectives-v1 (402 lines, 8 system perspectives), bs-vocab-coherence-audit-v1 (230 lines, 7 High drift instances + 14 Glossary gaps), bs-rot-audit-v1 (210 lines, 16 rot items). Cost ~682k tokens / 14 min wallclock (longest agent). Curation: surfaced 1 to For-James (ADR-0041/0043 phantom-ADR problem — referenced as settled in many places but don't exist as written ADRs; dual-meaning conflict on ADR-0043 across two branches). 6 new Kanban cards. Sharpest architectural finding: EFSUploadGateway wrapper as single change addressing 8-prompt detonation + L2 amortization + AA bundling. Bytecode confirmed Direction 3 dead; Direction 1 (James's 3-contract model) tight but feasible at 77-90% of EIP-170. brainstorm-system § Exit triage structure (added pre-batch) worked: all batch-3 brainstorms produced clean controversial/unknown/blockers triage. Cumulative: ~1.08M tokens across 3 batches, 13 brainstorms.
- @pm: session-5 (continuation). Dispatched batch-2 (3 subagents): bs-contract-decomposition-v1 (288 lines, 5 decomposition directions), bs-schema-coverage-audit-v1 (399 lines, 30-item gap catalog), bs-third-party-dev-ux-v1 (590 lines, 5 app walkthroughs). Cost ~244k tokens / ~7 min wallclock. Curation: (1) typed-edge finding refined and DOWNGRADED — not a Lists-blocker, just a pre-mainnet schema-design concern; (2) discovered contract spec drift (edition→lens, TagResolver→EdgeResolver, overview.md contract count out of sync) — surfaced to Kanban; (3) "8-prompt MetaMask detonation" identified as launch-quality concern; (4) "EFS-in-Postgres" off-chain indexer pattern surfaced to Kanban as higher-leverage than SDK polish. Contract count correction logged: PM had been saying "5 contracts" — actually ~11. brainstorm-system § rule 4 refined to allow declared-dependency reads. Decisions log captures sequencing correction from James (shape design starts NOW, not post-OnionDAO).
- @pm: session-4 (continuation). Dispatched 3 parallel subagents to seed Brainstorms/: bs-divergent-usecases-v1 (15 use cases, 250 lines), bs-os-sdk-capability-surface-v1 (~50 capabilities across 9 namespaces, 386 lines), bs-sdk-package-layout-v1 (3 equal-weight directions, 227 lines). Total cost ~155k tokens / 3.5 min wallclock. All three returned cleanly with clear exit summaries. Curated: surfaced 1 finding to For-James (typed-edge schema gap from bs-divergent-usecases-v1 — flagged for James to evaluate before Lists locks schemas). Other findings held as post-OnionDAO material. INDEX.md updated with all three. brainstorm-system.md gained "Subagent prompt patterns" section + 6th rule (exit-summary expectation) based on what worked. Decisions log captures the batch-1 process outcome and token-cost evaluation (not wasted — typed-edge finding alone justifies the spend many times over).
- @pm: session-3. Drafted [[brainstorm-system]] per James's green light (lightweight, 5 rules, no auto-prune, status=raw/surfaced/integrated/reference/obsolete). Created `Brainstorms/` scaffolding (README + INDEX). Captured the Gemini-collaborated EFS Official Client OS architecture as the first reference brainstorm (`2026-05-26-pm-client-os-architecture.md`) — Ring architecture, SES sandboxing, OCap, IPFS-deployable static SPA, Lit+Web Awesome+SES tech stack. PM SOUL gained curation duty, rot tracking, model-portability section (James confirmed PM may be run on Codex/Gemini/etc). Glossary gained Topic=Anchor cross-reference. Decisions bundle: 3 SDK types (third = OS SDK = OCap proxy, not iframe integrations), SDK+Client rot acknowledged, model portability, Topic=Anchor, no-auto-prune principle, brainstorm-system intent. Kanban card renamed Client App SDK → EFS OS SDK. For-James gained brainstorm-system review item.
- @pm: T-6 days to OnionDAO. James in chat: panicking, wrapping up Lists, asked for "whatever thoughts." PM delivered triage: minimum-viable OnionDAO is Lists merge + Sepolia deploy; SDK track soft-launch recommended; schema freeze = deploy event (no separate ceremony); 4 things only @james can do (Lists, SDK posture, announce post, logistics confirm); everything else deferred. Refreshed stale In Flight cards (both expired 2026-05-24) with 2026-05-29 expiries reflecting realistic next-nudge. No new commits on custom-lists since 2026-05-21 — work happening locally per James. For-James restructured into triage-list form. PM standing by for SDK-posture decision.

## 2026-05-21

- @pm: session-2 continuation. Surfaced EFS Lists (custom-lists branch on contracts) as schema-freeze + .sol-freeze keystone — was invisible in the vault before. Reframed Kanban: schema/.sol freeze cards now `#depends-on/lists-merge`; Lists itself moved to In Flight under @james. Slimmed pm-launch.md to thin bootstrap pointing at pm.md. Drafted SDK architect thread prompt in chat per James's choice (no file). Hackathon planning scoped as Milestones expansion + Kanban execution cards (not a design) per James. Decisions log gained five new entries capturing James's calls.
- @pm: first session. Ran all five audit scripts (all green). Surveyed 7-day commit log across planning/contracts/client. Drafted [[Agents/pm|pm.md]] SOUL (status: review). Moved "Draft PM SOUL" Kanban card to In Flight, claimed @pm, expires 2026-05-24. Updated [[For-James]] with two items: PM SOUL awaiting review + SDK architecture nudge. State: OnionDAO T-11 days; SDK design tallest pole and not started; schema freeze, .sol freeze, Sepolia deploy all unstarted.
- @claude-opus-4.7: design-system — drafted; awaiting James review. After review, request promotion as DESIGN-0001.
- @claude-opus-4.7: cross-repo-reference-mirror — renamed from `0001-cross-repo-reference-mirror.md` to name-only deferred draft. Blocked on a concrete CI need (see file).
- @claude-opus-4.7: tier-1 coordination additions landed — `For-James.md`, `Decisions.md`, `Tasks.md`, new conventions (Agent trailer, multi-repo card-per-PR rule, Under Review/Blocked TTL semantics, design-owns-truth, post-acceptance Open Questions), `_template.md` gains Reviewers/Last-touched fields, write-a-design.md §9 expanded with full landing ceremony.
- @claude-opus-4.7: tier-2 conventions landed — AGENTS.md (universal-brief filename), WIP limits (3/5/2), task-vs-Kanban division, Kanban-plugin format preservation rule.
- @claude-opus-4.7: research preserved as `Architecture/agent-coordination-prior-art.md` + five scripts under `scripts/` (stale-cards, tri-sync-check, agent-activity, designs-awaiting-promotion, promotion-check) — close the "rules without teeth" gap.
- @claude-opus-4.7: fix-up pass from two validation subagents — Glossary lifecycle now includes `rejected`; promotion section anchor renamed (parens are fragile); cross-repo path-depth rule clarified (file-relative, not vault-rooted); duplicated tri-sync/pre-promotion-checklist reduced to pointers; README gains Agent: trailer requirement; For-James and Tasks trimmed to high-value content only; outdated `[[0001-cross-repo-reference-mirror]]` reference fixed.
- @claude-opus-4.7: tier-4 pass — Milestones.md scaffolded, _Index.base seeded with starter queries, scripts/pre-commit-hook.sh shipped (optional install). End-to-end lifecycle + first-time reader subagents flagged: `/efs/` paths are aspirational vs. actual; bootstrap state not acknowledged anywhere; `#needs/james` tag was undocumented; landing ceremony step (d) used forbidden absolute path; Reviewers field missing from write-a-design's frontmatter snippet. All fixed in this commit. Remaining un-fixed items (length guidance, pseudo-code placement, reviewer-threading conventions) deferred as judgment calls vs. mechanical rules.
- @claude-opus-4.7: For-James.md restructured (actionable items at top, agent docs below separator) + DESIGN-0001 promoted (design-system → 0001-design-system). First promotion in vault. Trust token recorded in commit d88e8f7. promotion-check.sh passes.
- @claude-opus-4.7: Milestones populated from James's chat input — OnionDAO hackathon (2026-06-01, month of June, two tracks: datasets + SDK builds, self-funded prizes) is the next big target. Hard prerequisites: schema spec freeze, contract .sol file list freeze, core deployed to Sepolia, SDK MVP. Data-loss tolerance recorded as phase-dependent (Sepolia best-effort, mainnet strict). Devcon presentation 2026-11 noted as further-out, scope open.
- @claude-opus-4.7: Cross-repo follow-ups. (1) Opened https://github.com/efs-project/contracts/pull/19 — adds a "Cross-repo coordination" section to contracts/AGENTS.md pointing at the planning vault. (2) Planning vault updates: SDK scope clarified (on-chain + off-chain are OnionDAO-required; client SDK deferred; contracts NOT part of SDK), dedicated SDK design session expectation noted, AI-PM role expectation captured, Kanban Backlog re-prioritized with explicit OnionDAO-blocking items (.sol file freeze, schema freeze, Sepolia deploy, SDK MVP).
- @claude-opus-4.7: Created /Users/james/Code/EFS/client/AGENTS.md (direct push to client repo main, commit 973c527) — small entry point pointing at planning vault, acknowledging the repo is outdated. Created /Users/james/Code/EFS/AGENTS.md (local, not under version control) — orientation for new agents landing in the EFS workspace root with pointers to critical reads in each repo. Decisions log gained entries for: canonical path = /Users/james/Code/EFS/ (older /Users/james/Code/Claude/ duplicates deprecated), SOUL file concept noted (PM agent's future instruction set), cron scheduling is James's responsibility. Kanban gained "Design the PM SOUL file" Backlog item.
- @claude-opus-4.7: CI fix for efs-project/contracts PR #19. deploy-pin-check was failing pre-existing (also on main) with HH604 Alchemy 403; root cause was the Scaffold-ETH-2 community Alchemy key rotation upstream (their commit 69828b1). Fixed in two follow-up commits: 379d485 (hardhat config) and 4d5a5c3 (nextjs scaffold.config.ts — initial fix missed this one; James caught it). CI green after second commit. Captured playbook in Onboarding/known-issues.md so the next rotation is faster; Decisions.md gained an entry. Lesson recorded: grep whole repo when rotating shared default values.
- @claude-opus-4.7: PR #19 merged. Cross-repo coordination loop now closed at the git level — efs-project/contracts:main points at the planning vault.
- @claude-opus-4.7: Created planning/Agents/ directory for agent-specific institutional knowledge. First files: Agents/README.md (folder conventions) and Agents/pm-launch.md (full PM launch prompt with slug=pm, daily cadence, Agents/pm.md as SOUL destination). Main README gains Agents/ row. Kanban Backlog item updated to point at the new SOUL location. 0001-design-system.md gains a Post-acceptance note explaining the Agents/ extension (additive — no supersession required). PM thread is ready to launch.
- @claude-opus-4.8 (arch-review): Schema-freeze/Sepolia thread started. Reviewed contracts @ origin/main b1ac4e0 (local checkout was 226 behind — used a throwaway read worktree, left James's working tree + its 2 uncommitted PR-review-protocol edits untouched). Confirmed the prior freeze brainstorm's 3 blockers are CLOSED at HEAD (ADR-0041 PIN/TAG Accepted; Glossary↔spec PROPERTY drift reconciled; Lists ADRs 0044/0046/0047 committed). James's 4 calls captured: (1) freeze 8 schemas = ANCHOR/PROPERTY/DATA/PIN/TAG/MIRROR/LIST/LIST_ENTRY; (2) DROP BLOB + NAMING (redundant/tooling); (3) DEFER SORT_INFO — all deferrals additive, no orphaning; (4) proxy-first then freeze; (5) simple single-admin upgrade key for Sepolia. Wrote ADR-0048 + docs/SEPOLIA_FREEZE_TABLE.md on contracts branch `arch-review` (commit ca53d06). Key finding: ListEntryResolver derives its schema UID from address(this) in the CONSTRUCTOR → under a proxy that's the impl address, so the initializer refactor is mandatory, not optional. EVENT/TRANSITION is the one real schema gap (museum/supply-chain provenance) but additive → deferred to a separate mainnet proposal. Next: contract proxy refactor (5 resolvers) → CREATE2 deploy ordering → frozen-UID table fill → James sign-off gate → Sepolia register + round-trip.
- @claude-opus-4.8 (arch-review): Overnight deep-design pass done (two workflows: 91-agent schema-durability + 38-agent file-identity/proxy review). RESULTS: (1) all 8 frozen schemas validated solid-as-is — 28 candidate cracks, all refuted; revocable flags individually confirmed. (2) FILE IDENTITY (James's flag): converged recommendation = freeze DATA "bytes32 contentHash, uint64 size" UNCHANGED; reinterpret contentHash=0 as first-class "no inline hash" (zero-download remote/IPFS pins); hash/integrity moves to self-describing multihash/CID PROPERTYs. James's "hash-as-data" steer honored with NO schema change → ADR-0049. (3) keccak256 confirmed fine to hardcode (it's the whole EVM/EAS substrate; a hashAlg tag protects nothing). (4) Proxy: standard+safe behind EAS (Coinbase/Gitcoin precedent); corrected to register-LAST + atomic CREATE3 deploy+init; burn-to-immutable model (renounce ProxyAdmin) gated by full invariant suite + 14-day soak; supersedes ADR-0030 no-proxy clause → ADR-0048 r2. (5) Write-time re-lens found 2 resolver-logic bugs (MIRROR uri scheme allowlist too narrow; ANCHOR name encoding undefined) — NOT freeze blockers (fields already flexible). Wrote ADR-0049, ADR-0048 r2, updated freeze table + pre-burn checklist (contracts branch arch-review, commit 2c8a1c9); review-ready summary at planning/For-James-tomorrow.md. NOTHING irreversible done: no schemas registered, no contracts changed. Awaiting James's OK on the two decisions, then build the proxy refactor test-first.
- @claude-opus-4.8 (arch-review): File-identity + dedup design resolved with James over chat (3rd workflow, 30 agents). DECISIONS: (1) DATA → empty schema "" (pure identity); contentHash+size become trust-scoped reserved-key PROPERTYs ("never trust the client" — a hash is an unverifiable claim; also lets you pin a 10GB IPFS file with zero download). Real Tier-1 reshape (new DATA UID, drops dataByContentKey) — safe now, nothing frozen on Sepolia. (2) REDIRECT = 9th frozen schema "bytes32 target, uint8 kind" rev=true (Option B, James's pick) — first-class canonical/sameAs/supersededBy/symlink with AliasResolver guards; only uint8 kind frozen, taxonomy is upgradeable logic. Hardlinks already native (one DATA, many PINs). (3) find-by-hash → on-chain general property index (resolver logic, upgradeable, frozen at burn — does NOT block freeze; James wants it eventually). (4) RTBF explicitly out of scope (cypherpunk: data is forever). FROZEN SET NOW = 9 (added REDIRECT; DATA reshaped). ADR-0049 r2 + ADR-0050 + freeze table committed on arch-review (commit 2fa0f13). TRACKED (non-blocking): read-time redirect-resolution spec + conformance vectors (cycle→lowest-UID-in-SCC); client UX "never silently teleport"; on-chain property-index design. NOTE: /tmp worktree got cleared mid-session (lost uncommitted files; arch-review branch was safe in main repo) → recreated at /Users/james/Code/EFS/.wt-arch-review and now commit immediately after each write. Nothing registered; no Sepolia state.
- @claude-opus-4.8 (schema-freeze): Foundation analysis COMPLETE. Ran the contracts+API review (35 agents, 10 personas) + the Sepolia deployment-foundation/forward-compat analysis (18 agents) + empirical EIP-170 measurement. VERDICT: the Sepolia deployment is mainnet-forward + data-permanent — all 6 forward-compat lenses "safe-with-fixes", none "needs-rework"; CREATE3 makes Sepolia structure == mainnet structure (identical resolver addresses → identical schema UIDs → data persists, no rewrite Sepolia→mainnet). The one scary unknown — EIP-170 24KB limit on EFSIndexer — resolved EMPIRICALLY: 14,912 B = 61% (~9.6KB headroom), so NO forced split; decomposition stays optional/post-freeze behind the proxy. Contracts+API review found the schemas/binding sound (0 real freeze-blockers) but the DEV LAYER pre-MVP (missing Solidity SDK lib, no PIN/TAG indexer events, getter-naming sprawl) — non-blocking, mostly additive, the hackathon-adoption work. Scoping decided w/ James: THIS PR = schema+contract analysis (SDK stays a planning-repo reference, not built here). Capstone doc: contracts/docs/plans/2026-06-02-sepolia-deployment-blueprint.md (commit 3ff9fda); also committed: contracts+API review synthesis + critique synthesis + build plan r2 + ADR-0048/0049/0050 + freeze table, all on branch schema-freeze. NEXT = execution (build the proxy refactor test-first per plan r2, CreateX spike first); analysis has hit diminishing returns. Awaiting James's go + execution-mode + (i)-vs-(ii) DX sequencing.
- @claude-opus-4.8 (schema-freeze build): CORE PROXY REFACTOR COMPLETE. All 5 resolvers (EFSIndexer, EdgeResolver, MirrorResolver, ListResolver, ListEntryResolver) refactored to the EFSUpgradeableResolver pattern (EAS SchemaResolver immutable _eas + OZ Initializable + _disableInitializers + ERC-7201 namespaced config set in guarded initialize). Group A (DATA→empty) also done. Suite 392 passing / 0 failing throughout; every refactor independently storage-layout-verified (existing mappings byte-identical slots, ERC-7201 config in high namespaced slots — no collision). Two highest-risk refactors independently reviewed → APPROVED: EFSIndexer (the template) and ListEntryResolver (the critical address(this) self-UID bug — moved constructor→initialize so it derives against the proxy; proven end-to-end that a real LIST_ENTRY attestation through EAS doesn't revert WrongSchema; false-green-guard mutation-tested). MIRROR uri allowlist widened (ADR-0023 superseded). hardhat-upgrades pinned to v3 (HH2 line) + wired. ~12 commits on branch schema-freeze. DELIBERATELY DEFERRED to Phase D (so it lands coherently, not as transitional hacks): ALL deploy-script proxy-ification (CREATE3, register-last, rebind getContract("Indexer")→proxy across 02-09). REMAINING: B3 anchor-name canonical encoding (Durable convention), B4 upgrade-with-state corruption test, Group C REDIRECT+AliasResolver, Phase D (big deploy rewrite), Phase E (Sepolia deploy+freeze — NEEDS James's RPC key + funded deployer + freeze-table signature). Nothing registered on-chain; all reversible.
- @claude-opus-4.8 (schema-freeze build): SCHEMA+CONTRACT LAYER COMPLETE & PR-READY. Groups A+B+C done on branch schema-freeze: DATA→empty (ADR-0049); all 6 resolvers (EFSIndexer/Edge/Mirror/List/ListEntry/Alias) on the EFSUpgradeableResolver pattern (ERC-7201 storage + guarded initialize + _disableInitializers; self-UID-in-initialize for ListEntry+Alias); canonical anchor-name encoding; upgrade-with-state corruption guard + storage-layout snapshot gate; REDIRECT = 9th schema + AliasResolver (ADR-0050); MIRROR uri allowlist widened (ADR-0023 superseded). Suite 429 passing / 0 failing; next:check-types clean. Final independent review: AliasResolver APPROVED, whole-branch COHERENT — all 9 frozen field-strings+revocable flags agree across contracts↔freeze-table↔specs, 5 ERC-7201 namespaces verified unique, deploy-scripts honestly deferred (no test depends on them). ADRs 0048/0049/0050 stay "Proposed" — correct: the freeze is gated on James's signature. DELIBERATELY NOT in this PR (deploy concern, James scoped this PR = schema+contract): Phase D (CREATE3 deploy pipeline, register-last, rebind 02-09 to proxies, Safe-owner wiring) + Phase E (Sepolia deploy+freeze). KEY-CUSTODY DECISION (James, this session): deploy from a funded EOA; transfer ProxyAdmin+resolver ownership to the efs.eth Safe (upgrade authority = the Safe, supersedes the simple-single-admin assumption; burn later = ProxyAdmin.renounceOwnership via the Safe). RPC = SE-2 default Alchemy key. Phase E needs: funded deployer EOA + efs.eth Safe address + James's freeze-table signature. ~24 commits; nothing registered on-chain; fully reversible. Branch local — not pushed/PR'd yet (awaiting James's go).
- @claude-opus-4.8 (schema-freeze build): PHASE D COMPLETE — deploy foundation built + fork-proven. The frozen schema+contract layer (Groups A+B+C, 9 schemas, 6 upgradeable resolvers) is now backed by a turnkey CREATE3 deploy pipeline. Built: `deploy/lib/{schemas,create3,verify,orchestrate,views}.ts` + `00_efs_core.ts` (frozen core) + `10_efs_views.ts` (non-frozen views) + `tasks/deployEfs.ts`/`deployEfsViews.ts`. Mechanism: 6 resolver IMPLs → 6 CREATE3 TransparentUpgradeableProxies via CreateX (`0xba5Ed0…ba5Ed`, salts deployer-keyed → Sepolia↔mainnet parity for the same EOA) with atomic init-in-constructor → VERIFY GATE (realized==predicted, init locked, self-UID getters==computed UID, getEAS, golden vectors) → wire → 🔒 human freeze gate (`--until-freeze-gate` stops here) → register 9 schemas LAST against the proxies (`--after-freeze-gate`, re-asserts self-UIDs before the irreversible register) → transfer all ProxyAdmin+resolver ownership to the EFS.eth Safe (hard-fails if EFS_SAFE_ADDRESS unset on a real net) → per-schema smoke. Adversarial review found NO UID-corrupting bug (9 field strings byte-match; CREATE3/register-last/ownership sound); 4 must-fixes (runbook task, after-gate guard, Safe fail-fast, golden vectors) all landed. Views (EFSRouter/EFSFileView/ListReader, in no UID, redeployable) deploy post-freeze via `yarn deploy:efs-views`. Full round-trip e2e on the fork PASSES through all 3 views (anchor→DATA→PIN→LIST→LIST_ENTRY written, read back). Suite 457 passing + 28 golden-vector + 2 fork-e2e (self-skip unforked). Runbook = docs/DEPLOYMENT.md. 4 Phase-D commits (27c3654, 3134b59, 9b3f2db, 951288a); branch 28 commits, LOCAL — not pushed/PR'd (awaiting James's go). NOTHING on-chain; fully reversible. ONE Durable loose end flagged to James: committed `deployedContracts.ts` pin is stale (predates CREATE3 addresses) → CI deploy-pin-check red until a forked `yarn deploy` regenerates it (ADR-0037 still holds — local-fork uses account-0 CREATE3 addrs deterministically; real Sepolia uses James's EOA via the freeze table). Phase E (real Sepolia deploy+freeze) needs: funded deployer EOA + EFS.eth Safe address + James's FREEZE_TABLE signature.
